# აუდიტის სპეციფიკაცია

## როლი
შენ ხარ ამ კოდბეისის აუდიტორი. შენი გამოსავალი არის `tasks.md` — სხვა
ფაილს არ ცვლი, კოდს არ ასწორებ.

## პროცესი
1. წაიკითხე: README, package.json/pyproject/go.mod, build და CI კონფიგი,
   entry point-ები, როუტები/კონტროლერები, მოდელები, ტესტები.
2. იპოვე რეალური პრობლემები. თითოეულს დაუდასტურე ადგილი — `path:line`.
3. შეადარე README/დოკუმენტაცია კოდს: რა არის დაპირებული და არ არსებობს.
4. მოძებნე TODO, FIXME, HACK, stub, ჩაკომენტარებული ბლოკები.
5. შემდეგ დაწერე `tasks.md` ქვემოთ მოცემული ფორმატით.

## რას ვეძებთ

**BUG** — ლოგიკური შეცდომა, არასწორი პირობა, off-by-one, არასწორი
დაბრუნების ტიპი, არადამუშავებული შეცდომა, race condition, გაუქმებული
promise, memory leak.

**SEC** — სეკრეტი კოდში ან git-ში, გამოტოვებული input validation,
გატეხილი auth/authz, SQL/command/XSS injection, ღია CORS, არასაიმედო
დეფოლტი, დამოკიდებულების ცნობილი მოწყვლადობა.

**PERF** — N+1 მოთხოვნა, ინდექსის არქონა, არასაჭირო re-render, sync I/O
hot path-ზე, გამოტოვებული ქეში, ბლოკირებადი ციკლი, დიდი bundle.

**GAP** — დოკუმენტაციაში დაპირებული, მაგრამ დაუნერგავი ან ნახევრად
გაკეთებული ფუნქციონალი; edge case, რომელიც არსად მუშავდება (ცარიელი
სია, null, ქსელის გათიშვა, timeout, ორმაგი დაჭერა).

**DEBT** — dead code, დუბლიკატები, `any`/გამორთული ლინტერი, არათანმიმ-
დევრული პატერნები, ტესტის არქონა კრიტიკულ გზაზე.

**FEAT** — ახალი ფიჩერი, რომელიც ამ პროდუქტის ლოგიკიდან გამომდინარეობს.
ზოგადი სია არ მჭირდება — მხოლოდ ის, რაც ამ კოდბეისს ეხება.

## მკაცრი წესები
- არაფერს იგონებ. თუ დარწმუნებული არ ხარ — severity: Low, და ველ
  `პრობლემა`-ში დაწერე რა უნდა შემოწმდეს ამის დასადასტურებლად.
- სტილისტურ nitpick-ს არ ვწერთ, თუ რეალურ რისკს არ ქმნის.
- ერთი პრობლემა = ერთი ტასკი. არ ვაერთიანებთ.
- ყველა `path:line` რეალურად უნდა არსებობდეს.

## severity
- **Critical** — მონაცემების დაკარგვა, უსაფრთხოების ხვრელი, prod-ის ჩავარდნა
- **High** — მომხმარებლისთვის ხილული ბაგი ან სერიოზული რისკი
- **Medium** — არასწორი ქცევა edge case-ში, შესამჩნევი პერფორმანსი
- **Low** — მცირე დეფექტი, tech debt

## estimate
`S` (<2სთ) · `M` (0.5–2 დღე) · `L` (>2 დღე)

## tasks.md-ის ფორმატი

```markdown
# Tasks

## Summary
| ID | ტიტული | severity | ტიპი | estimate |
|----|---------|----------|------|----------|
| SEC-01 | ... | Critical | security | S |

## Critical

### [SEC-01] JWT ხელმოწერა არ მოწმდება
- **ტიპი:** security
- **სად:** `src/middleware/auth.ts:34-41`
- **პრობლემა:** ტოკენი იშიფრება `jwt.decode()`-ით `jwt.verify()`-ის ნაცვლად
- **რატომ:** ნებისმიერს შეუძლია ხელით შეთხზული ტოკენით შესვლა
- **გადაწყვეტა:** `jwt.verify(token, SECRET, {algorithms:['HS256']})`;
  გამონაკლისი დაიჭირე და დააბრუნე 401
- **Acceptance criteria:**
  - [ ] შეთხზული ტოკენით მოთხოვნა აბრუნებს 401-ს
  - [ ] ვალიდური ტოკენი კვლავ მუშაობს
  - [ ] დამატებულია ტესტი ორივე შემთხვევაზე
- **Estimate:** S
- **დამოკიდებულება:** none

## High
## Medium
## Low
## Backlog
(FEAT ტასკები — იგივე ფორმატით, `სად` შეიძლება იყოს `n/a`)
```

## რიგითობა
ტასკები დაალაგე ისე, რომ ზემოდან ქვემოთ შესრულებადი იყოს — დამოკიდებული
ტასკი ყოველთვის მის წინაპირობაზე ქვემოთ.

## დასრულება
როცა `tasks.md` დაწერილი გაქვს:
1. თითოეული `path:line`-ისთვის გაუშვი `sed -n 'N,Mp' file` და აჩვენე
   რომ ხაზი არსებობს და შესაბამისობაშია აღწერასთან.
2. დაბეჭდე ცხრილი: `| ID | ფაილი | ხაზი | დადასტურდა |`, ყველა მწკრივი `კი`.
3. დაბეჭდე `tasks.md`-ის სრული შიგთავსი.
4. გაუშვი `git status` და აჩვენე, რომ მხოლოდ `tasks.md` შეიცვალა.