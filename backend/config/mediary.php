<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Mediary — აპლიკაციის საკუთარი პარამეტრები
    |--------------------------------------------------------------------------
    */

    /*
     * **სად ცხოვრობს SPA** — FEAT-16-ის აღდგენის ბმულისთვის.
     *
     * ⚠️ backend-ს ეს დღემდე არ სჭირდებოდა: ყველა პასუხს SPA თვითონ
     * კითხულობს და მისამართს თვითონ აწყობს. აღდგენის ბმული კი **აპლიკაციის
     * გარეთ** მიდის (ჩატით, ხმით), ე.ი. სრული უნდა იყოს — და „სრული"
     * მხოლოდ სერვერმა იცის.
     *
     * ⚠️ `FRONTEND_URL` იმავე ცვლადია, რომელსაც CORS-ი კითხულობს — მეორე
     * სახელი ორ ჭეშმარიტებას შექმნიდა.
     */
    'frontend_url' => env('FRONTEND_URL', env('APP_URL', 'http://localhost:5173')),

    // ღია რეგისტრაცია (შეთანხმებული: ჩართული; მოდულები მოთხოვნით ირთვება)
    'allow_registration' => (bool) env('ALLOW_REGISTRATION', true),

    /*
     * Tasks SEC-15 — **პირველი რეგისტრირებული ანგარიში super_admin-ია.**
     *
     * ⚠️ ნაგულისხმევად ჩართულია, რადგან სუფთა ინსტალაციაზე ეს ერთადერთი გზაა
     * ბრაუზერიდან თვითონ დაიწყო (`mediary:bootstrap-admin` ტერმინალს ითხოვს).
     *
     * ⚠️ **საჯარო სერვერზე გამორთე** (`FIRST_USER_IS_ADMIN=false`): ღია
     * რეგისტრაციაზე პირველი ანგარიშის მოპოვება ინსტალაციის დაპატრონებაა —
     * ვინც `setup`-სა და შენს პირველ შესვლას შორის მოასწრებს, სუპერ-ადმინია.
     * გამორთულზე პირველიც ჩვეულებრივი `user`-ია და ადმინი მხოლოდ
     * `php artisan mediary:bootstrap-admin`-ით იქმნება.
     *
     * ⚠️ ჩართულზე „ეს პირველია?" **ჩაკეტილი წაკითხვაა ტრანზაქციაში**
     * (`User::accountsLocked()`), თორემ ორი ერთდროული რეგისტრაცია ორ
     * სუპერ-ადმინს ქმნიდა.
     */
    'first_user_is_admin' => (bool) env('FIRST_USER_IS_ADMIN', true),

    /*
     * Tasks §16.1 — საჯარო პროფილები (`/u/{username}`).
     *
     * ⚠️ ეს ერთადერთი ადგილია, სადაც აპლიკაციის მონაცემი ავტორიზაციის გარეშე
     * გადის გარეთ, ამიტომ **ერთი გადამრთველი მთელ მექანიზმს თიშავს**:
     * `PUBLIC_PROFILES=false` → `PublicProfileService::resolve()` ყოველთვის
     * `null`-ია, ე.ი. ყველა საჯარო endpoint 404-ია, ცალკეული პროფილების
     * პარამეტრებზე ხელის ხლების გარეშე.
     */
    'public_profiles' => (bool) env('PUBLIC_PROFILES', true),

    /*
     * **მომხმარებლის URL-ის ჩამოტვირთვა** (აუდიტი 2026-09-14, §A2) — `App\Support\SafeHttp`.
     *
     * ⚠️ ნაგულისხმევად **ლოკალური და დახურული ქსელი აკრძალულია**: ბუკმარკის
     * მეტამონაცემიც და ვებიდან ფოტოს ჩამოტვირთვაც მომხმარებლის ნაკარნახევ
     * მისამართს ხსნის, ე.ი. `http://127.0.0.1:3306` ან `169.254.169.254`
     * სერვერს შიდა ქსელის სკანერად აქცევდა.
     *
     * ⚠️ **გამორთვადია და არა მუდმივი**: პირად ინსტალაციაზე `192.168.x.x`-ზე
     * მდგარი გვერდის ჩაბუკმარკება სრულიად ლეგიტიმურია. ჩართვა ცხადი
     * გადაწყვეტილებაა — `SAFE_HTTP_ALLOW_PRIVATE=true`.
     */
    'safe_http' => [
        'allow_private' => (bool) env('SAFE_HTTP_ALLOW_PRIVATE', false),
    ],

    /*
     * Tasks §7.1 — ვიდეოს ლოკალურად ჩამოწერა.
     *
     * ⚠️ **PHP-ით ეს არ კეთდება** — გარე პროგრამა სჭირდება. გასაღების ნაცვლად
     * აქ ბილიკია, და მისი **არქონა ღილაკს ჩუმად არ ტეხს**: `YtDlp::available()`
     * false-ს აბრუნებს და endpoint 503 `ytdlp_unavailable`-ს — ზუსტად ის წესი,
     * რაც `rawg_unavailable`/`bgg_unavailable`-ს აქვს („წყარო მიუწვდომელია"
     * და „ვერაფერი ვიპოვე" ერთი და იგივე არ არის).
     *
     * ⚠️ `ffmpeg` **სურვილისამებრია, მაგრამ ხარისხს წყვეტს**: მის გარეშე
     * ცალკე ვიდეო- და აუდიო-ნაკადი ვერ ერწყმის, ე.ი. „საუკეთესო
     * ხელმისაწვდომი" ერთფაილიან (progressive) ვარიანტამდე ეცემა. რა მივიღეთ
     * სინამდვილეში, `videos.download_format`-ში იწერება და UI-ზე ჩანს.
     */
    'ytdlp' => [
        'binary' => env('YTDLP_BINARY'),
        'ffmpeg' => env('FFMPEG_BINARY'),
        // წამები: ერთი ვიდეო ნახევარ საათამდეც შეიძლება ჩამოდიოდეს
        'timeout' => (int) env('YTDLP_TIMEOUT', 1800),
        // მეტამონაცემის წაკითხვა სწრაფია — ცალკე, მოკლე ლიმიტი
        'probe_timeout' => (int) env('YTDLP_PROBE_TIMEOUT', 60),
    ],

    /*
     * Tasks §22 — ბაზის დამპი და აღდგენა ადმინიდან.
     *
     * ⚠️ **გასაღების ნაცვლად აქ ბილიკია** (`ytdlp`-ის ზუსტი წესი): ორივე
     * პროგრამა MySQL-ს მოჰყვება, მაგრამ ამ მანქანაზე XAMPP-ის `bin/`
     * **PATH-ზე არ დგას**. ცარიელი მნიშვნელობა ნიშნავს „მოძებნე": ჯერ
     * PATH, მერე ცნობილი საქაღალდეები (`DatabaseDumper::COMMON_PATHS`).
     *
     * ⚠️ **არქონა ავარია არაა** — `available()` false-ს აბრუნებს და
     * endpoint 503 `mysqldump_unavailable`-ს, ე.ი. ღილაკი ჩუმად კი არ
     * ტყდება, არამედ მიზეზს ამბობს.
     */
    /*
     * FEAT-12 — **დაგეგმილი ასლი თვითონ აპიდან.**
     *
     * ⚠️ **ეს გარე PowerShell-ტასკს ცვლის და მისი ისტორია მიზეზია.** ის
     * ტასკი დაწერილი, დოკუმენტირებული და დარეგისტრირებული იყო — და
     * მაინც არ ეშვებოდა (ბატარეაზე უარი, `StartWhenAvailable = False`,
     * ერთი ტრიგერი): ექვსიდან ოთხ დღეს ასლი საერთოდ არ გაკეთებულა.
     * აქ ასლი იმავე scheduler-ზეა, რაზეც შეხსენებები, და მისი შედეგი
     * ჩვეულებრივ `/backups`-ის სიაში ჩანს.
     *
     * ⚠️ **გადამრთველი `.env`-შია და არა ღილაკი `/backups`-ზე.** ეს
     * ინსტალაციის პარამეტრია და არა მომხმარებლის: ორი სუპერ-ადმინის
     * შემთხვევაში „ვისი პარამეტრია" კითხვას პასუხი არ აქვს, ხოლო ცალკე
     * გლობალური ცხრილის დაბადება ერთი ბულევისთვის ზუსტად ის მექანიზმის
     * გამრავლებაა, რომელსაც ეს პროექტი ყოველთვის უარყოფს. გვერდი მას
     * **მხოლოდ აჩვენებს** — იგივე წესი, რაც `UploadLimitsCard`-ს და
     * `/credentials`-ის `meta.installation`-ს აქვს.
     *
     * ⚠️ **`keep` მხოლოდ დაგეგმილ ასლებს ჭრის** (`source = 'schedule'`):
     * ხელით აღებული ასლი და ატვირთული ფაილი ადამიანის გადაწყვეტილებაა და
     * ავტომატურად არასდროს იშლება.
     */
    'backup' => [
        'mysqldump' => env('MYSQLDUMP_BINARY'),
        'mysql' => env('MYSQL_BINARY'),
        // წამები: სრული დამპი ან აღდგენა წუთებს გრძელდება
        'timeout' => (int) env('BACKUP_TIMEOUT', 900),

        'auto' => (bool) env('BACKUP_AUTO', true),
        // რამდენი ბოლო დაგეგმილი ასლი რჩება (0 — ყველა)
        'keep' => (int) env('BACKUP_AUTO_KEEP', 7),
        'at' => env('BACKUP_AUTO_AT', '03:00'),
    ],

];
