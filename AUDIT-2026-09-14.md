# ტექნიკური აუდიტი — Mediary, 2026-09-14

> ეს არის `Tasks.md`-ის თანმხლები **აღწერითი** ფაილი: თასქებში მოკლე პუნქტებია,
> მიზეზები, ზუსტი ადგილები და გამოსწორების გეგმა კი — აქ.
>
> მეთოდი: წაკითხულია `backend/app` (270 ფაილი), `routes/api.php`, 72 მიგრაცია,
> `frontend/src` (229 ფაილი). ყველა დასკვნა **ნანახ კოდს** ეყრდნობა — ზუსტი
> ფაილი და ხაზი თითოეულთან წერია. ბაზისური მდგომარეობა გადამოწმებულია ცოცხლად:
> `php artisan test` → **456 ტესტი, 2395 assertion, გავიდა**; `npm run lint` → 0
> შეცდომა; `python frontend/src/i18n/audit.py` → **OK**.
>
> ✅ **სტატუსი: აუდიტი დახურულია (2026-09-14).** ყველა პუნქტი გასწორდა —
> §A1 · §A2 · §A3 · §A4 · §A5 · §A7 · §B1 · §B2 · §B3 · §B4 · §B5 · §C-ის სამივე ·
> §D-ის ლოგირება, ტესტების ორი ხვრელი, CI, ჯვარედინი ძებნა და სერვერული რიგი.
> ერთადერთი ღია პუნქტი **კოდი არ არის**: MariaDB Windows-ის სერვისად
> (ადმინისტრატორის shell-ს ითხოვს — იხ. `Tasks.md`).
>
> ახალი ტესტ-ფაილები: `RateLimitTest` (7) · `SafeHttpTest` (12) ·
> `DictionaryKeyTest` (12) · `RecordOwnershipTest` (9) · `AuthTest` (22) ·
> `MovieModuleTest` (20) · `GlobalSearchTest` (10) · `BatchQueueTest` (10),
> პლუს დამატებები `VideoDownloadTest`/`ChatTest`/`PublicProfileTest`/
> `StorageManagementTest`/`RegistryConsistencyTest`-ში.
>
> **სუიტა: 571 ტესტი, 2900 assertion, გავიდა** (აუდიტამდე 456).
> `pint` · `npm run build` · `npm test` · oxlint · i18n audit — ყველა მწვანე.

---

## 1. პროექტის მოკლე მიმოხილვა

Mediary არის ორენოვანი (ka/en) მედია-კატალოგი: Laravel 13 JSON API (PHP 8.3 /
MySQL) + React 19 + TypeScript SPA, ერთ რეპოში. ბირთვი მრავალმომხმარებლიანია —
Sanctum-ის cookie-ავტორიზაცია, per-user ბიბლიოთეკები `BelongsToUser`-ის ერთი
global scope-ით, ჩართვადი მოდულები (`modules` + `module_user`) და როლებზე
დაფუძნებული უფლებები. ცოცხალია **11 მოდული** (`movie`, `series`, `anime`,
`video`, `song`, `book`, `board_game`, `game`, `note`, `bookmark`, `gallery`) და
მათ ზემოთ ჯვარედინი ფენები: საცავის კვოტები, აუდიტ-ლოგი, საჯარო პროფილები,
დამთხვევები, ჩატი, ველების კონსტრუქტორი და მასობრივი წაშლა.

კოდის ხარისხი **მაღალია და ეს გულწრფელი შეფასებაა**: ჯვარედინი რეესტრები ერთ
ადგილასაა (`MediaDomain`, `StorageFolder`, `PublicDomain`, `StatusDomain`,
`AuditRegistry`) და `RegistryConsistencyTest` მათ სქემიდან ამოწმებს; მასობრივი
შევსება არსად არ არის ნედლი request-იდან; ფაილს გამცემი ყველა მარშრუტი
route-model binding-ზე დგას, ე.ი. **path traversal არსად არ მოიძებნა**;
ფრონტზე არც `dangerouslySetInnerHTML` და არც ტოკენი localStorage-ში.

ხარვეზები, რაც მოიძებნა, ორ ჯგუფშია. პირველი — **პლატფორმული ფენა, რომელიც
არასდროს აშენდა**: rate limiting საერთოდ არ არსებობს, ლოგირება ფაქტობრივად
არ არსებობს, სერვერული რიგი არ არსებობს (მიუხედავად `QUEUE_CONNECTION=database`-ისა),
და 17 Policy კლასი დაწერილია, მაგრამ **არცერთხელ არ გამოიძახება**. მეორე —
რამდენიმე კონკრეტული ლოგიკური ბაგი, რომლებიც ჩუმად ტყდება.

---

## 2. მოდულების რუკა

### Backend (`backend/app`)

| ფენა | სად | რას აკეთებს |
|---|---|---|
| **მარშრუტები** | `routes/api.php` (≈1000 ხაზი) | ერთადერთი მარშრუტების ფაილი; `/health` + ორი საჯარო პროფილის endpoint-ის გარდა ყველაფერი `auth:sanctum`-ის უკან |
| **Middleware** | `app/Http/Middleware/` (4 ფაილი, 167 ხაზი) | `module:` (მოდული ჩართულია?), `permission:` (შიდა CRUD), `admin_access:` (ადმინის სექცია), `super_admin:` |
| **რეესტრები** | `app/Support/` (17 ფაილი) | `MediaDomain` · `StorageFolder` · `PublicDomain` · `StatusDomain` · `AuditRegistry` · `FieldCatalog` · `CustomFields` · `GalleryParent` · `UploadLimits` · `VideoUrl` |
| **მოდელები** | `app/Models/` (60+) | `BelongsToUser` (მფლობელობა), `StoredFile` (დისკი+კვოტა), `HasStatus`, `HasGallery`, `HasCustomFields` |
| **კონტროლერები** | `app/Http/Controllers/Api/` (60+, 13 268 ხაზი) | ყველაზე დიდი — `GalleryController` (1424), `WebSearchController` (667), `GameController` (474) |
| **სერვისები** | `app/Services/` (22 ქვესაქაღალდე, 11 933 ხაზი) | გარე წყაროები (Tmdb · Rawg · Igdb · OpenLibrary · Bgg · SerpApi · Serper · Wikimedia · Gemini · yt-dlp), დომენური ლოგიკა (Storage · Purge · Chat · Profile · Audit · Gallery · Sync · Translation) |
| **Policies** | `app/Policies/` (17 ფაილი + `OwnedRecordPolicy`) | ✅ ცოცხალია 2026-09-14-დან — `EnsureRecordOwnership` |
| **Jobs** | `app/Jobs/RunBatchItem.php` | ✅ 2026-09-14 — სერვერული პარტია (`POST /api/batches`) |
| **ტესტები** | `backend/tests/` (34 ფაილი, 456 ტესტი) | Feature-ზეა აგებული; `RegistryConsistencyTest` რეესტრებს სქემიდან ამოწმებს |

### Frontend (`frontend/src`, 229 ფაილი, 50 894 ხაზი)

| ფენა | სად | რას აკეთებს |
|---|---|---|
| **API კლიენტი** | `src/api/` (22 ფაილი) | ერთი axios instance (`src/lib/api.ts`), cookie-რეჟიმი, 401-ზე გლობალური logout event |
| **გვერდები** | `src/pages/` (40+) | ყველა `lazy()`-ით, `ErrorBoundary` + `Suspense`-ის უკან |
| **გაზიარებული ლოგიკა** | `src/lib/` (40 ფაილი) | `media` · `modules` · `statuses` · `filters` · `paged` · `player` · `embed` · `dates` · `galleryGroups` · `photoActions` · `reminders` |
| **UI პრიმიტივები** | `src/components/ui/` | `PageHeader` · `EmptyState` · `ModalShell` (მოდალების სტეკი) · `PhotoGrid` · `NumberPick` · `Chip` · `DataTable` · `queue` |
| **ტესტები** | 12 `*.test.ts` (94 ტესტი) | `lib/`-ის სუფთა ფუნქციები + 3 კომპონენტის ტესტი |

---

## 3. ბაგები და ხარვეზები

### A. უსაფრთხოება

| ფაილი:ხაზი | პრობლემა | რატომ არის საშიში | სავარაუდო გამოსწორება | სიმძიმე |
|---|---|---|---|---|
| `backend/routes/api.php:101-103`, `backend/bootstrap/app.php:21-33` | **rate limiting საერთოდ არ არსებობს.** `grep -rn throttle routes/ app/ config/` მხოლოდ `config/auth.php`-ის უკავშირო პარამეტრს პოულობს. `/auth/login`, `/auth/register`, ჩატი, ძებნა, თარგმანი — არაფერს აქვს ჭერი | რეგისტრაცია ღიაა (`ALLOW_REGISTRATION=true`), ე.ი. პაროლის ბრუტფორსს არაფერი აჩერებს. ერთი ანგარიშისვე შეუძლია დახარჯოს **გაზიარებული** SerpApi-ს 250 ძებნა და Gemini-ის 1500 დღიური გამოძახება — ისინი ინსტალაციისაა და არა მომხმარებლისა | **გაკეთდა:** `AppServiceProvider::rateLimiters()` — `login` (5/წთ, გასაღები **IP + შეყვანილი login ერთად**), `api` (600/წთ ანგარიშზე — ⚠️ განზრახ ფართო: SPA-ს რიგი `syncDelayMs=200`-ით წუთში ~300 მოთხოვნას აგზავნის), `web-search` (20/წთ), `translate` (30/წთ), `download` (10/წთ). `bootstrap/app.php`-ში `throttleApi('api')` | 🔴 → ✅ |
| ✅ `backend/app/Services/Bookmarks/LinkMetadata.php:45-55`<br>`backend/app/Services/Serp/WebImageImporter.php:156-165` | **[გასწორებულია]** **SSRF.** მომხმარებლის მოწოდებულ URL-ს სერვერი თვითონ ტვირთავს, `allow_redirects: true`-ით, ჰოსტის/IP-ის ფილტრის გარეშე. ვალიდაცია მხოლოდ `'url'` წესია (`BookmarkController.php:89`), რომელიც `http://127.0.0.1:3306`-საც უშვებს | ავტორიზებულ მომხმარებელს შეუძლია სერვერი გამოიყენოს შიდა ქსელის სკანერად: `POST /api/bookmarks/metadata` სხვადასხვა შედეგს აბრუნებს ღია და დახურულ პორტზე. cloud-ზე `169.254.169.254` მეტამონაცემების endpoint-ია | **გაკეთდა:** `App\Support\SafeHttp` — სქემა მხოლოდ `http`/`https`, `user:pass@` უარყოფილი, DNS-ის ყველა პასუხი მოწმდება `FILTER_FLAG_NO_PRIV_RANGE\|NO_RES_RANGE`-ით, redirect-ები **ხელით** (ყოველი ნახტომი მოწმდება), IP **მიბმულია** `CURLOPT_RESOLVE`-ით (DNS rebinding). ლოკალური ქსელი გამორთვადია — `SAFE_HTTP_ALLOW_PRIVATE`. დაბლოკვა ლოგში იწერება | 🔴 → ✅ |
| ✅ `backend/app/Services/Bookmarks/LinkMetadata.php:62`<br>`backend/app/Services/Serp/WebImageImporter.php:181-184` | **[გასწორებულია]** **ზომის ჭერი ტყუილი იყო.** `$res->body()` მთელ პასუხს **უკვე** მეხსიერებაში კითხულობს; `substr(..., MAX_BYTES)` და `strlen($body) > MAX_BYTES` ამის **შემდეგ** მოქმედებს. `LinkMetadata`-ის დოკბლოკი (ხაზი 16-18) პირდაპირ ამტკიცებს, რომ „ერთი უზარმაზარი გვერდი `artisan serve`-ს არ დაბლოკავს" — კოდი ამას არ აკეთებს | 2 GB-იანი ფაილის მისამართის გაგზავნა PHP-ის `memory_limit`-ს ამოწურავს და ერთნაკადიან dev-სერვერს ჩამოაგდებს. **ეს კომენტარისა და ქცევის შეუსაბამობაა** (ეტაპი 2) | **გაკეთდა:** `stream => true` + ნაკადის ნაჭრებად კითხვა ჭერამდე; `Content-Length` ცალკე მოწმდება. `truncated` ცხადად ბრუნდება — მოჭრილი **სურათი** აღარ ინახება (გატეხილი ფაილი იქნებოდა). ცოცხლად გაზომილი: 100-ბაიტიანი ჭერი → ზუსტად 100 ბაიტი | 🟠 → ✅ |
| ✅ `backend/routes/api.php:586-590`<br>`backend/app/Http/Middleware/EnsureModulePermission.php:22,47-52` | **[გასწორებულია]** **`POST /media/sync/{type}/{id}` არასწორ უფლებას ამოწმებდა.** ჯგუფს `permission:@type` მოქმედების გარეშე აქვს, ე.ი. `actionFor()` ბოლო სეგმენტს (`{id}` — რიცხვი) `UPDATE_ENDPOINTS`-ში ვერ პოულობს და **`create`**-ს გამოიყვანს. სინქრონი კი არსებულ ჩანაწერს გადააწერს (`ItemSyncer`, `mode: overwrite`) | ორმხრივი შეცდომაა: (1) როლი „ვქმნი, მაგრამ არ ვცვლი" **შეუძლია სხვისი გადაწერა** — სათაური, აღწერა, პოსტერი, ჟანრები, მსახიობები; (2) როლი „ვცვლი, მაგრამ არ ვქმნი" ცრუ 403-ს იღებს. ზუსტად ის ხაფანგი, რომელსაც `routes/api.php:603` (`permission:@type,update`) და `/translations/{type}/{id}` (ხაზი 629) **ცხადად** არიდებენ — ერთ ადგილას დარჩა გამორჩენილი | **გაკეთდა:** `/media/sync/{type}/{id}` ცალკე, `permission:@type,update`-ით (+ `whereIn`/`whereNumber`); `/lookup`, `/lookup/candidates` და `/discover` — `permission:@type,view` | 🟠 → ✅ |
| `backend/app/Policies/` (17 ფაილი)<br>`backend/app/Providers/AppServiceProvider.php:69-72` | **Policy-ები არასდროს გამოიძახება.** `grep -rn "authorize(\|Gate::\|->can(" app/Http/Controllers` → **0 შედეგი**. `Gate::define('use-module', …)` (ხაზი 72) ასევე 0-ჯერ. კონტროლერებს `AuthorizesRequests` trait საერთოდ არ აქვთ | `CLAUDE.md` ამტკიცებს, რომ „Policies … არიან **მეორე ფენა**" — ეს ფენა არ არსებობს. სინამდვილეში მფლობელობას მხოლოდ `BelongsToUser`-ის global scope იცავს, რომელიც **94 ადგილას ცხადად ითიშება** (`withoutGlobalScope('owner')`). დღეს ყველა 94 გადამოწმებულია და სწორია, მაგრამ სათადარიგო ბადე არ არსებობს: 95-ე დავიწყება ჩუმად გაუშვებდა **გაკეთდა (შენი არჩევანი — ვამუშავებთ):** `EnsureRecordOwnership` middleware, ⚠️ **და არა `$this->authorize()` ასეულ კონტროლერში** — ცხადი გამოძახება ერთ ახალ endpoint-ზე დავიწყებისთანავე ხვრელია (იგივე მიზეზი, რის გამოც აუდიტ-ლოგი observer-ის ერთი ციკლით ებმევა). იბმევა `SubstituteBindings`-ის **შემდეგ**, პასუხი **404**-ია, `Gate::forUser()` ცხადად რექვესთის მომხმარებელს იღებს. 17 policy ახლა `OwnedRecordPolicy`-ის ცარიელი მემკვიდრეა (იყო 17 იდენტური ასლი), ხოლო policy-ის გარეშე დარჩენილ 40+ `BelongsToUser` მოდელსაც იგივე შემოწმება ეხება | 🟠 → ✅ |
| ✅ `backend/app/Http/Controllers/Api/AuthController.php:66,99,123-124` | **[გასწორებულია — აუდიტის შემდეგ ნაპოვნი]** `register()`/`login()`/`logout()` `$request->session()`-ს **დაცვის გარეშე** იძახებდნენ. `/api/*`-ს სესია მხოლოდ **stateful** დომენიდან მოსულ რექვესთს აქვს | ნებისმიერი სხვა კლიენტი (curl, მობილური, არასწორად კონფიგურირებული `SANCTUM_STATEFUL_DOMAINS`) შესვლაზე **500-ს** იღებდა სუფთა პასუხის ნაცვლად — ე.ი. კონფიგურაციის შეცდომა ყველაზე ძნელად ამოსაცნობ ხარვეზად იქცეოდა. ⚠️ **სწორედ ეს იყო მიზეზი, რის გამოც `AuthTest` არ არსებობდა**: `postJson()` stateful არ არის, ე.ი. `/auth/register` ტესტიდან ყოველთვის 500 იყო და ტესტი ფიზიკურად ვერ დაიწერებოდა | **გაკეთდა:** `AuthController::regenerateSession()` + `hasSession()`-ის შემოწმება სამივე ადგილას | 🟠 → ✅ |
| `backend/.env:4` (`APP_DEBUG=true`) | დებაგი ჩართულია და გამონაკლისები JSON-ად ირენდერება (`bootstrap/app.php:35-37`) | ლოკალურად სწორია. თუ აპლიკაცია ოდესმე localhost-ს დატოვებს, ნებისმიერი API-კლიენტი სრულ stack trace-ს, ბილიკებსა და SQL-ს დაინახავს | deploy-ის ჩეკლისტში: `APP_DEBUG=false`, `APP_ENV=production` | 🟡 საშუალო (deploy-ზე კრიტიკული) |

### B. ლოგიკური ბაგები

| ფაილი:ხაზი | პრობლემა | რატომ არის საშიში | სავარაუდო გამოსწორება | სიმძიმე |
|---|---|---|---|---|
| ✅ `backend/app/Models/Video.php:126-130`<br>`backend/app/Services/Video/VideoDownloader.php:49-54` | **[გასწორებულია]** **ჩამოწერა სამუდამოდ „running"-ში იჭედებოდა.** `deleteDownload()` პირველივე ხაზზე ბრუნდება, თუ `download_path` ცარიელია — ხოლო გაჭედილ მდგომარეობაში ის ზუსტად ცარიელია. `start()` კი `running`-ზე 409-ს აბრუნებს | **რეპროდუქცია:** ჩამოწერა დაიწყე → დახურე `artisan serve`-ის ტერმინალი (ან გადატვირთე მანქანა). ფონური პროცესი კვდება, `download_status` რჩება `running`. ამის შემდეგ: `POST` → 409 სამუდამოდ, `DELETE` → არაფერს აკეთებს. **ამ ვიდეოს ჩამოწერა UI-დან აღარასდროს გაეშვება** — მხოლოდ ხელით SQL-ით. ჩვეულებრივი dev-რუტინა (ტერმინალის დახურვა) ამას იწვევს **გაკეთდა:** ახალი სვეტი `videos.download_started_at` (⚠️ **`updated_at` არ გამოდგებოდა** — ის ჩანაწერის ნებისმიერ ცვლილებაზე განახლდება), ერთი ფორმულა `Video::downloadStale()`, რომელსაც `start()`-იც ეკითხება და `VideoResource`-იც (`download_stale`). `deleteDownload()` სვეტებს ბილიკის გარეშეც ასუფთავებს. ⚠️ **UI-ც განბლოკილია**: `running`-ზე ღილაკი გამორთული იყო და წაშლა არ ჩანდა, ე.ი. backend-ის გასწორება მარტო არ იკმარებდა — ახლა გაჭედილზე „ხელახლა სცადე" ჩანს და polling ჩერდება | 🟠 → ✅ |
| ✅ `backend/app/Services/Chat/ChatService.php:158-163`<br>`backend/app/Models/Message.php:47-55` | **[გასწორებულია]** **„მხოლოდ ჩემთან წაშლა" ერთ, გაზიარებულ სამეულ სვეტში იწერებოდა** (`removed_at`/`removed_by`/`removed_scope`), მაშინ როცა ეს ფაქტი **თითო მხარისაა** | **რეპროდუქცია:** A წერს „გამარჯობა". B შლის **მხოლოდ თავისთან** → `removed_by = B`, A ხედავს, B ვერა. ✔ შემდეგ A შლის **მხოლოდ თავისთან** → `removed_by = A` გადაეწერა → `scopeVisibleTo(B)` ახლა `true`-ს აბრუნებს და **B-სთან წერილი ისევ გამოჩნდა**. ე.ი. ერთი მხარის წაშლა მეორისას აუქმებს — ჩუმად, შეტყობინების გარეშე **გაკეთდა:** ცხრილი `message_hides` (⚠️ **და არა ორი სვეტი**: `hidden_by_a`/`hidden_by_b` „მონაწილე ზუსტად ორია"-ს ჩააკერებდა სქემაში — ზუსტად ის დაშვება, რომლის გამოც მონაწილეები pivot-ია). `removed_at`/`removed_by` ახლა **მხოლოდ** „ორივესთან"-ს ნიშნავს, `removed_scope` კი **წაიშალა** (გადატანის შემდეგ მხოლოდ `both`-ს შეიცავდა, ე.ი. `where('removed_scope','self')` ჩუმად ცარიელს დააბრუნებდა) | 🟠 → ✅ |
| ✅ `backend/app/Models/Status.php:165-177` + 8 ტყუპი<br>(`VideoType:68` · `NoteCategory:73` · `BookmarkCategory:74` · `BookGenre:76` · `GameGenre:84` · `SongGenre:77` · `BoardGameGenre:75` · `Role:104`) | **[გასწორებულია]** **`makeKey()` უნიკალურობის ციკლის შემდეგ ჭრიდა სტრიქონს.** ციკლი ამოწმებს სრულ `$key`-ს, `return mb_substr($key, 0, 60)` კი უკვე შემოწმებულს აჭრის. `-2` სუფიქსიც ჭრაშივე იკარგება | ცხრილებს `unique(user_id, module, key)` აქვთ (`2026_09_12_000004:53`) და სხვებს `unique(user_id, key)`. **გაზომილი:** 80-სიმბოლოიანი ქართული სახელი (ვალიდაციის მაქსიმუმი, `StatusController.php:334`) → `Str::slug` → **91 სიმბოლო**. ორი ასეთი სახელი, რომელთა slug-ის პირველი 60 სიმბოლო ემთხვევა, მეორეზე **`QueryException` → 500**-ს იძლევა. ჩვეულებრივი შენახვა, არანაირი გაფრთხილება **გაკეთდა:** `App\Support\DictionaryKey::make()` — ჯერ ჭრა (სუფიქსის ადგილის დატოვებით), მერე შემოწმება, ე.ი. `$taken()`-ს **ზუსტად ის** სტრიქონი გადაეცემა, რაც ჩაიწერება. შვიდი per-user ლექსიკონი ახალ trait-ზეა (`HasDictionaryKey`), `Status`/`Role`/`CustomFields` პირდაპირ იძახებენ. ⚠️ **სწორი ასლი პროექტშივე იდო** — `CustomFields::makeKey()` — ე.ი. ალგორითმი ცნობილი იყო და უბრალოდ არ იყო გაზიარებული. ციკლს ჭერიც დაემატა | 🟡 → ✅ |
| ✅ `backend/app/Http/Controllers/Api/PublicProfileController.php:64`<br>`backend/app/Http/Controllers/Api/ChatController.php:122` | **[გასწორებულია]** **`per_page`-ს ქვედა ზღვარი არ ჰქონდა** — `min((int) …, 100)` და არა `min(max(…, 1), 100)`. `Builder::limit()` უარყოფით მნიშვნელობას **ჩუმად უგულებელყოფს** (`vendor/…/Query/Builder.php:3177`), ე.ი. LIMIT საერთოდ არ ეწერება | `GET /api/public/profiles/{username}/movie?per_page=-1` — **ავტორიზაციის გარეშე** — მთელ საჯარო ბიბლიოთეკას ერთ პასუხში აბრუნებს. გვერდებად დაყოფის მთელი აზრი იკარგება; 353-ფილმიანი ბაზა ეს უკვე ასეული კილობაიტია, სამი ათასი — მეგაბაიტები. `Controller::paginated()` (ხაზი 66) სწორად აკეთებს — ეს ორი მას არ იყენებს **გაკეთდა:** `min(max(…, 1), 100)` ორივეგან; რეგრესია დაფარულია `PublicProfileTest`-ით (`-1`, `0` და `100000`) | 🟡 → ✅ |
| ✅ `backend/app/Services/Storage/StorageMeter.php:1061-1075, 1121-1133` | **[გასწორებულია]** **კვოტის TOCTOU.** `guard()` (კითხვა) და `add()` (ჩაწერა) ატომური არ არის | ორი პარალელური ატვირთვა ორივე გაივლის შემოწმებას და კვოტას გადააჭარბებს. იგივე `VideoDownloader::start()`-ის 409-ზე (`VideoDownloader.php:50`), რომლის კომენტარიც ამტკიცებს, რომ „ორი პარალელური yt-dlp კვოტას ორჯერ **ვერ** დახარჯავს" | dev-ზე `artisan serve` ერთნაკადიანია, ე.ი. დღეს არ ხდება. სწორი გამოსწორება — `SELECT … FOR UPDATE` ტრანზაქციაში, ან `UPDATE users SET storage_used_bytes = … WHERE storage_used_bytes + ? <= storage_quota_bytes` და affected rows-ის შემოწმება (იგივე compare-and-swap, რაც `ReminderDispatcher`-ს უკვე აქვს) | 🟡 საშუალო |
| ✅ `backend/app/Http/Controllers/Api/GalleryVideoController.php:140` | **[გასწორებულია]** `sort_order`-ის მაქსიმუმი **ყველა მომხმარებლის** რიგებიდან იკითხება (`withoutGlobalScope('owner')` `user_id`-ის ფილტრის გარეშე), მაშინ როცა ორი ხაზით ზემოთ (127) იგივე query სწორად იფილტრება | ფუნქციური ზიანი მცირეა (რიგითობა ხტება), მაგრამ `gallery_videos.sort_order` `unsignedSmallInteger`-ია — საზიარო მშობელზე (მაგ. ერთი მსახიობი) რიცხვი 65535-ს რომ მიაღწიოს, MySQL strict რეჟიმში ჩაწერა გატყდება. მთავარი კი ის არის, რომ **წესი ერთ ფუნქციაში ორნაირად წერია** | `->where('user_id', $user->getKey())` დაემატოს | 🟢 დაბალი |
| ✅ `backend/app/Services/Storage/StorageMeter.php:598-602` | **[გასწორებულია]** `tempnam()` ფაილს ქმნის, `$zip->open()`-ის ჩავარდნაზე კი `return null` ხდება — დროებითი ფაილი რჩება | `%TEMP%`-ში ნაგავი გროვდება. იშვიათი გზაა, მაგრამ `deleteFileAfterSend()` მხოლოდ წარმატებულ გზას ფარავს | `if (…!== true) { @unlink($zipPath); return null; }` | 🟢 დაბალი |
| ✅ `backend/app/Http/Controllers/Api/AuthController.php:186-198` | **[გასწორებულია]** პაროლის შეცვლა **სხვა სესიებს არ აუქმებს** | პაროლი იმიტომ იცვლება, რომ ძველი კომპრომეტირებულია; ძველი სესია კი აქტიური რჩება `SESSION_LIFETIME`-ის ბოლომდე | `Auth::logoutOtherDevices($newPassword)` ან `$request->session()->regenerate()` + სხვა სესიების წაშლა `sessions` ცხრილიდან | 🟢 დაბალი |
| ✅ `backend/app/Http/Controllers/Api/AuthController.php:200-208` | **[გასწორებულია]** `settings` ნებისმიერი ზომის მასივია (`['required','array']`), ზომის ჭერის გარეშე; შემდეგ `forceFill`-ით JSON სვეტში | ერთი მოთხოვნით მეგაბაიტების ჩაწერა შეიძლება, და `users.settings` ყოველ `/auth/me`-ზე იკითხება. გარდა ამისა, ეს JSON `AuditObserver`-ის გავლით **მთლიანად** ჩაეწერება `audit_logs.new_values`-შიც | `'settings' => ['required','array','max:200']` + გასაღებების allowlist (`UserSettings`-ში უკვე არსებობს ცნობილი გასაღებების სია) | 🟢 დაბალი |

### B-დამატებითი. ტესტის წერისას ნაპოვნი

| ფაილი:ხაზი | პრობლემა | რატომ არის საშიში | გამოსწორება | სიმძიმე |
|---|---|---|---|---|
| ✅ `backend/app/Http/Requests/StoreMovieRequest.php:24`<br>`UpdateMovieRequest.php:26`<br>`StoreSeriesRequest.php:24` · `UpdateSeriesRequest.php:26` | **[გასწორებულია]** `imdb_id`-ის ვალიდაცია **გლობალურად** უნიკალურობას ითხოვდა (`unique:movies,imdb_id`), მაშინ როცა ბაზის ინდექსი **2026-08-28-იდან** `unique(user_id, imdb_id)`-ია და `CLAUDE.md` ცხადად წერს: „ორმა ანგარიშმა ერთი და იგივე ფილმი უნდა შეძლოს დაამატოს“ | მეორე მომხმარებელი **ვერ ამატებდა** ფილმს, რომელიც სხვას უკვე ჰქონდა — 422 „ეს ფილმი უკვე დამატებულია“. ⚠️ და ეს **ინფორმაციის გაჟონვაცაა**: პასუხი ამბობდა, რომ ვიღაცას ეს ფილმი აქვს. ⚠️ `anime` მოდული (2026-09-11) იმავე წესს **სწორად** იცავდა და მიზეზსაც აღწერდა — ე.ი. ცოდნა არსებობდა და ორი უძველესი დომენი უბრალოდ გამორჩა | **გაკეთდა:** `Rule::unique(...)->where('user_id', …)` ოთხივე request-ში, `anime`-ის ზუსტი ფორმით; დაფარულია `MovieModuleTest`-ის ორი ტესტით (ორი ანგარიში — ნება; ერთ ბიბლიოთეკაში დუბლი — უარი) | 🟠 → ✅ |

### C. რესურსები და წარმადობა

| ფაილი:ხაზი | პრობლემა | რატომ არის საშიში | სავარაუდო გამოსწორება | სიმძიმე |
|---|---|---|---|---|
| ✅ `backend/app/Services/Profile/MatchService.php:189-236` | **[გასწორებულია]** `ranking()` 50 პროფილზე იმეორებს `summary()`-ს, რომელიც **თითოეულის სრულ საჯარო ბიბლიოთეკას** 9 დომენზე მეხსიერებაში ტვირთავს (`records()`, ხაზი 251-268). `memo` მხოლოდ გამეორებას იჭერს, არა მოცულობას | `GET /api/matches` = 50 × 9 = **450 სრული `get()`** ერთ მოთხოვნაში, ქეშის გარეშე. `/people` გვერდის ყოველი გახსნა. დღეს ბაზა პატარაა და ეს არ ჩანს; 10 აქტიურ ანგარიშზე, თითო 1000 საჯარო ჩანაწერით, ეს ათასობით ჩანაწერის მეხსიერებაში აწყობაა | `keys()` მხოლოდ იდენტობის სვეტებს საჭიროებს — სრული მოდელების ნაცვლად `pluck()`. მერე, საჭიროებისას, `CLAUDE.md`-ში უკვე აღწერილი content-stamp ქეში (`count` + `max(updated_at)`) და არა TTL | 🟡 საშუალო |
| ✅ `backend/app/Services/Notes/ReminderDispatcher.php:56-79` | **[გასწორებულია]** `run()` **GET** მოთხოვნის შიგნით ასრულებს 200-მდე შეხსენებას და თითოეულზე სინქრონულ HTTPS-ს ტელეგრამისკენ (`sendTelegram`, ხაზი 145) | `GET /api/note-reminders/due` SPA-ს პერიოდული polling-ია. დაგროვილი რიგი + ნელი ტელეგრამი = ერთნაკადიანი `artisan serve` ბლოკირდება ათეული წამით, ე.ი. მთელი აპლიკაცია იყინება | გაგზავნა რიგში (იხ. D1) ან polling-ის მხარეს BATCH-ის დაწევა (მაგ. 20) — cron-ს სრული 200 დარჩეს | 🟡 საშუალო |
| `backend/app/Observers/AuditObserver.php:30-33` + `AuditRegistry.php:139-141` | `GalleryImage`/`GalleryVideo` აუდიტში არიან, ე.ი. 500-ფოტოიანი გალერეის ჩამოტვირთვა **500 დამატებით INSERT-ს** წერს, თითოეული სრული `attributesToArray()`-ით | ეს ცნობიერი გადაწყვეტილებაა („აბსოლუტურად ყველა მოდული"), მაგრამ `audit_logs` მასობრივ ოპერაციებზე ყველაზე სწრაფად იზრდება და ის სტრიქონები ყველაზე ნაკლებად საკითხავია. ცხრილს გასუფთავება ხელით აქვს, ავტომატური მოცილება — არა | ან მასობრივი იმპორტისთვის `AuditLogger::suppress()` (უკვე არსებობს, `AuditLogger.php:135`) + ერთი შემაჯამებელი ჩანაწერი („N ფოტო ჩამოიტვირთა"), ან `audit_logs`-ის ავტომატური როტაცია (N დღეზე ძველი, `PROTECTED_ACTIONS`-ის გარდა) | 🟢 დაბალი |
| `backend/app/Http/Controllers/Controller.php:63-65` | `?all=1` **ყველა** სიის endpoint-ზე მუშაობს და ჭერი არ აქვს | ოთხი ლეგიტიმური გამომძახებელი აქვს, მაგრამ ის მარშრუტზე არ არის შეზღუდული: ნებისმიერ ავტორიზებულ მომხმარებელს შეუძლია ყოველ სიაზე მთელი ბიბლიოთეკის მოთხოვნა. rate limit-ის არარსებობასთან ერთად (A1) ეს იაფი DoS-ია | ჭერი `all=1`-ზეც (მაგ. 5000 ჩანაწერი), ან — უკეთ — A1-ის rate limiting | 🟢 დაბალი |

### D. დაკვირვებადობა, დუბლირება, ტესტები

| ფაილი:ხაზი | პრობლემა | რატომ არის საშიში | სავარაუდო გამოსწორება | სიმძიმე |
|---|---|---|---|---|
| `app/Services/{Tmdb,Games,Books,BoardGames,Serp,Web,Cast,Enrichment}/*` — 20+ ადგილი<br>მაგ. `RawgClient.php:144`, `BggClient.php:109`, `OpenLibraryClient.php:155`, `IgdbClient.php:159`, `GeorgianShops.php:137,279`, `CastEnricher.php:66`, `MovieEnricher.php:139`, `TvEnricher.php:157` | **გარე წყაროს ჩავარდნა არსად არ ილოგება.** ყველა `catch (Throwable) { return null; }` ჩუმია. მთელ `app/`-ში ნამდვილი `Log::` გამოძახება **6-ია** | ეს „წყარო არ პასუხობს ≠ ვერაფერი ვიპოვე" ნიმუში სწორია — მაგრამ **მიზეზი არსად რჩება**. „RAWG-მა რატომ არაფერი დააბრუნა" პასუხგაუცემელი კითხვაა: გასაღები გაქრა? timeout? 429? `storage/logs`-ში ამაზე ერთი ხაზიც არ წერია | ყოველ ასეთ `catch`-ში `Log::warning('<source> failed', ['url' => …, 'error' => $e->getMessage()])`. ერთი გაზიარებული `SourceClient` ბაზისური კლასი ამას ერთხელ დაწერს — ცხრავე კლიენტს ერთი და იგივე სტრუქტურა აქვს (`get()` + `blocked()` + timeout + cacert) | 🟡 საშუალო |
| ✅ `backend/tests/` — **`AuthTest.php` არ არსებობდა** | რეგისტრაცია, login, logout, პროფილის განახლება, პაროლის შეცვლა — **არცერთს არ აქვს გამიზნული ტესტი**. `grep -rln "auth/login" tests/` ერთადერთ `AuditLogTest.php`-ს პოულობს, სადაც ისინი გვერდითაა შემოწმებული | ეს პროექტის ყველაზე უსაფრთხოებრივად კრიტიკული კონტროლერია: „პირველი მომხმარებელი super_admin-ია" (`AuthController.php:48`), „გათიშული ანგარიში ვერ შედის" (ხაზი 89), „`profile_visibility` ცარიელზე არ იცვლება" (ხაზი 163) — არცერთი წესი დაფარული არ არის. რეგრესია ჩუმად გავა **გაკეთდა:** `AuthTest` — 22 ტესტი (bootstrap-ის super_admin, ცარიელი მეორე ანგარიში, გათიშული ვერ შედის, უნიკალურობა, პაროლი, `profile_visibility`-ის „ცარიელი = არ შეცვალო“, პარამეტრები). ⚠️ **ტესტის წერამ თვითონ ორი ხაფანგი გამოაჩინა**: `User`-ს Laravel 13-ის `#[Fillable]` ატრიბუტი აქვს, ე.ი. `is_active`/`profile_visibility` `create()`-ით **ჩუმად იკარგება**; და `UserResource`-ში `role` **სტრიქონია** და არა ობიექტი | 🟡 → ✅ |
| ✅ `backend/tests/` — `MovieModuleTest` არ არსებობდა | ცხრავე ახალ მოდულს თავისი `*ModuleTest.php` აქვს; **უძველეს ორს — არა**. ფილმები მხოლოდ სხვისი ტესტების გვერდითაა (`GalleryTest`, `TranslationTest`, `OwnershipTest`) | `MovieController` (320 ხაზი) — CRUD, ფილტრები, ფრანჩაიზის კლასტერი, `from-tmdb`, `bulk-status`, კოლექცია — გამიზნული დაფარვის გარეშე. ეს ის დომენია, საიდანაც ყველაფერი დაიწყო და რომელსაც ყველაზე მეტი ჯვარედინი ფენა ეხება **გაკეთდა:** `MovieModuleTest` — 20 ტესტი (CRUD, ვალიდაცია, ფილტრები, დალაგება, ფრანჩაიზის კლასტერი, სტატუსი/რჩეული, მფლობელობა, მოდულის კარიბჭე). ⚠️ **და მაშინვე იპოვა ნამდვილი ბაგი** — იხ. §B-დამატებითი | 🟡 → ✅ |
| 9 ფაილი × `makeKey()` (იხ. B3) | ერთი და იგივე 12-ხაზიანი ფუნქცია ცხრაჯერ; `Role::makeKey` მეათე ვარიანტია | დუბლირება თავისთავადაც პრობლემაა, მაგრამ აქ **ბაგიც ცხრაჯერ დუბლირდა** | ერთი `App\Support\DictionaryKey` | 🟡 საშუალო |
| `BookController.php:374`, `BoardGameController.php:346`, `GameController.php:470`, `MediaDownloader.php:62`, `ItemSyncer.php:351,377,383` | **`Storage::disk('public')` ჩაწერილია ხელით**, მაშინ როცა `StorageMeter.php:51-54`-ის დოკბლოკი ამტკიცებს: „რომელი დისკია, ამას **მხოლოდ** `StorageFolder::diskFor()` წყვეტს … ერთი დავიწყებული `self::DISK` პრივატულ ფაილს public-ზე დაწერდა" | დღეს სწორია (ეს ბილიკები მართლაც საჯაროა), მაგრამ წესი უკვე დარღვეულია შვიდ ადგილას. მომავალი პრივატული მოდულის დამატებისას სწორედ ეს შვიდი გამოირჩევა | `Storage::disk(StorageFolder::diskFor($path))`-ზე გადაყვანა | 🟢 დაბალი |
| `SerpApiClient.php:449-452` vs `Translator` | **ორი პროვაიდერი ხარჯს სხვადასხვანაირად ითვლის**: SerpApi ჩავარდნილ გამოძახებას **არ** აღრიცხავს, Gemini — აღრიცხავს (და `CLAUDE.md` ამას ცხადად ასაბუთებს: „უარყოფილი მოთხოვნაც ითვლება") | ორივე ერთსა და იმავე კითხვას პასუხობს („რამდენი დამრჩა"), ორნაირად. SerpApi-ს მრიცხველი რეალურზე ნაკლებს აჩვენებს | გადაწყდეს ერთი წესი და ორივეგან ერთნაირად ჩაიწეროს (ჩემი რეკომენდაცია: ჩაწერა ყოველ გამოძახებაზე, რომელიც ქსელში გავიდა) | 🟢 დაბალი |

### E. დასაზუსტებელი

ეს ის ადგილებია, სადაც **ვერ დავრწმუნდი** და გამოსწორებამდე გადამოწმება
სჭირდება — შენი გადასაწყვეტია, ბაგია თუ ცნობიერი გადაწყვეტილება:

1. **`ChatService::between()` (`ChatService.php:44-62`)** — ორი პარალელური
   `POST /chat/with/{username}` ორ საუბარს შექმნიდა (`conversation_user`-ზე
   უნიკალური ინდექსი ვერ დავინახე). ერთნაკადიან dev-სერვერზე ეს არ ხდება;
   საჭიროა თუ არა დაცვა — იმაზეა დამოკიდებული, სად გაეშვება.
2. **`PurgeService::runOne()` (`PurgeService.php:247`)** — `$id` პირდაპირ
   მუშავდება, `plan()`-ის scope-თან შედარების გარეშე. `super_admin`-ისთვისაა
   და `confirm: "DELETE"` აქვს, ე.ი. სავარაუდოდ ცნობიერია — მაგრამ ეს ნიშნავს,
   რომ კლიენტს შეუძლია გეგმის გარეთ დარჩენილი ჩანაწერიც წაშალოს.
3. **`gallery_images.sort_order` / `gallery_videos.sort_order` —
   `unsignedSmallInteger`** (`2026_09_03_000003:50`). `WebImageImporter.php:69`
   `max()+1`-ით ზრდის. 65535 არარეალურია, მაგრამ ჭერი არსად არ მოწმდება.
4. **`frontend/src/App.css`** — არაფერს არ იმპორტირებს (გადამოწმებულია),
   Vite-ის შაბლონის ნარჩენია. `CLAUDE.md` ამბობს „განზრახ დატოვებული" — თუ
   ასეა, კარგია; თუ არა, წასაშლელია.
5. **`frontend/src/lib/node_modules/.vite/`** — Vite-ის ქეშის ნარჩენი
   საქაღალდე `src`-ის შიგნით. git-ში არ არის, ე.ი. უვნებელია, მაგრამ იქ არ
   უნდა იდგეს.

---

## 4. რეკომენდებული ახალი მოდულები / ფუნქციონალი

### D1. სერვერული სამუშაო რიგი (Queue) — **ყველაზე დიდი არქიტექტურული ხარვეზი**

**რატომ.** `backend/.env:32` ამბობს `QUEUE_CONNECTION=database`, მაგრამ
`app/Jobs/` **საერთოდ არ არსებობს** და `dispatch()` არსად არ იძახება. ოთხი
გრძელი ოპერაცია — სინქრონი, გალერეის ჩამოტვირთვა, თარგმანი და მასობრივი
წაშლა — **ბრაუზერის ტაბში** ტრიალებს (`frontend/src/components/ui/queue.tsx`).
ე.ი. 300-ჩანაწერიანი სინქრონი ჩერდება ტაბის დახურვაზე, ქსელის გაწყვეტაზე,
ლეპტოპის დაძინებაზე. `BackgroundProcess`-იც (`app/Support/BackgroundProcess.php`)
სწორედ ამის შემოვლითი გზაა — `popen`-ით გაშვებული detached პროცესი, რადგან
ნამდვილი worker არ არსებობს.

**სად ჩაჯდება.** `app/Jobs/{SyncItem,FetchGallery,TranslateItem,PurgeItem}Job` +
`jobs`/`failed_jobs` ცხრილები (Laravel-ს მიგრაციები მზად აქვს). SPA-ს რიგი
რჩება, მაგრამ „გაშვება"-ს ნაცვლად **მდგომარეობას კითხულობს**
(`GET /api/jobs/{batch}`), ე.ი. `ui/queue.tsx` მარტივდება და არა რთულდება.
`Bus::batch()` პროგრესსაც და გაუქმებასაც უფასოდ იძლევა.

**სირთულე:** მაღალი (2-3 დღე). **მოგება:** ოთხივე გრძელი ოპერაცია ტაბისგან
დამოუკიდებელი ხდება, `ReminderDispatcher`-ის ბლოკირება (C2) თავისით ქრება,
`BackgroundProcess` იშლება. ⚠️ ფასი: Windows-ზე `php artisan queue:work`
მუდმივად უნდა იყოს გაშვებული — იგივე პრობლემა, რაც `schedule:work`-ს აქვს,
და იგივე პასუხი: Windows-ის სერვისი.

### D2. Rate limiting და ბოროტად გამოყენების კონტროლი

**რატომ.** იხ. A1. გარდა ავტორიზაციისა, პროექტს **გაზიარებული, ფასიანი
რესურსები** აქვს: SerpApi-ს 250 ძებნა თვეში, Gemini-ის 1500 გამოძახება
დღეში, TMDB-ის ლიმიტები. ეს კვოტები **ინსტალაციისაა და არა მომხმარებლისა**
(`TranslationUsage`-ის დოკბლოკი ამას პირდაპირ ამბობს), ე.ი. ერთი ანგარიში
ყველას ტოვებს უკვოტოდ.

**სად ჩაჯდება.** `bootstrap/app.php`-ში `throttle` + `RateLimiter::for()`
განსაზღვრებები `AppServiceProvider`-ში. ზედა ფენად — **per-user** კვოტა
გარე წყაროებზე (`serp_searches`/`translation_usages`-ს `user_id` უკვე აქვთ,
ე.ი. მონაცემი უკვე გვაქვს, მხოლოდ შემოწმება აკლია).

**სირთულე:** დაბალი (ნახევარი დღე ბაზისურ ნაწილზე).

### D3. ლოგირება და შეცდომების თვალყური

**რატომ.** იხ. D1 ცხრილში. დღეს „რატომ არ მუშაობს" კითხვაზე პასუხი მხოლოდ
კოდის კითხვით მიიღება. `LOG_LEVEL=debug` არის, მაგრამ წერს თითქმის არაფერი.

**სად ჩაჯდება.** (1) ერთი `Services\Support\SourceClient` ბაზისური კლასი
ცხრავე გარე კლიენტისთვის — timeout, cacert, `blocked()`, retry **და**
ლოგირება ერთხელ; (2) `config/logging.php`-ში ცალკე `sources` არხი
(`storage/logs/sources.log`), რომ გარე წყაროების ისტორია აპლიკაციის
ლოგში არ დაიკარგოს.

**სირთულე:** საშუალო (1 დღე) — და გვერდით პროდუქტად **ცხრა კლიენტის
დუბლირებული `get()` მეთოდი ერთდება**.

### D4. ანგარიშის სასიცოცხლო ციკლი

**რატომ.** დღეს არ არსებობს: პაროლის აღდგენა, ელფოსტის დადასტურება,
ანგარიშის წაშლა მომხმარებლის მიერ, 2FA. მრავალმომხმარებლიან აპლიკაციაში
დავიწყებული პაროლი = **ანგარიშის სამუდამო დაკარგვა** (ადმინის ხელით
ჩარევის გარეშე). `MAIL_MAILER=log`, ე.ი. ფოსტის არხი ისედაც არ დგას —
ესეც ამავე თასქის ნაწილია.

**სად ჩაჯდება.** `AuthController` + Laravel-ის მზა `password_reset_tokens`
(მიგრაცია უკვე არსებობს). ⚠️ შეხსენებების ელფოსტის არხი **ცნობიერად
წაიშალა** (`CLAUDE.md`, §8.2) სწორედ იმიტომ, რომ SMTP არ დგას — ე.ი. ეს
თასქი ჯერ SMTP-ის გადაწყვეტას ითხოვს და მერე კოდს.

**სირთულე:** საშუალო. ⚠️ **შენი გადასაწყვეტია, გჭირდება თუ არა** — თუ
აპლიკაცია სამუდამოდ localhost-ზე რჩება და ანგარიშები მხოლოდ შენია,
ეს ზედმეტია და ამ სიაში არ უნდა იდგეს.

### D5. CI (`.github/workflows`)

**რატომ.** `.github` საერთოდ არ არსებობს. ოთხი შემოწმება უკვე მზა და
სწრაფია (`php artisan test` — 59 წმ, `npm test`, `npm run build`,
`python frontend/src/i18n/audit.py`, `pint --test`, `oxlint`), მაგრამ ყოველ
ჯერზე ხელით უნდა გაეშვას. სწორედ ამიტომ არსებობს `Tasks.md`-ში „ჯანმრთელობის
სურათი" — ის CI-ის ხელით შესრულებული ჩანაცვლებაა.

**სირთულე:** დაბალი (1-2 საათი). ⚠️ თუ რეპო არასდროს დაიპუშება, უაზროა —
მაშინ ალტერნატივა ერთი `check.ps1`-ია, რომელიც ექვსივეს ერთად უშვებს.

### D6. ჯვარედინი ძებნა

**რატომ.** `VideoSearch` (333 ხაზი, mb-aware Levenshtein, relevance) **მხოლოდ
ვიდეოს მოდულს ემსახურება**. დანარჩენ ათ მოდულში ძებნა მარტივი `like`-ია, და
გლობალური ძებნა („იპოვე ეს სიტყვა ყველგან") საერთოდ არ არსებობს — 11-მოდულიან
კატალოგში ეს შესამჩნევი ხარვეზია.

**სად ჩაჯდება.** `VideoSearch`-ის განზოგადება `Services/Search/`-ად +
`GET /api/search?q=` ყველა ჩართულ მოდულზე. ⚠️ FULLTEXT ცნობიერად არ
გამოიყენება (sqlite-ს არ აქვს, ქართულს ცუდად ამუშავებს) — ეს შეზღუდვა
რჩება.

**სირთულე:** საშუალო-მაღალი.

### ნახევრად დასრულებული მოდულები

| მოდული | რა აკლია |
|---|---|
| `gallery` | ყველაზე დიდი კონტროლერი (1424 ხაზი) — 6 გვერდი, 12 endpoint ერთ ფაილში. ფუნქციურად სრულია, მაგრამ დაშლას ითხოვს (cut-ების მიხედვით) |
| `video` | ჩამოწერა გაჭედვის რისკით (B1); `videos.visibility` არსებობს, UI არ აქვს |
| `song`, `playlist` | `visibility` სვეტი არსებობს, გადამრთველი — არა (მხოლოდ ბეჯად ჩანს) |
| `note` | ერთადერთი, რომელიც **ვერასდროს გახდება საჯარო** (ცნობიერი); `note_notifications`-ს ავტომატური მოცილება არ აქვს |
| `chat` | polling-ზეა (5/15/30 წმ). WebSocket-ი არაა საჭირო, მაგრამ წაკითხვის ქვითარი და აკრეფის ინდიკატორი აკლია |
| Policy-ების ფენა | 17 ფაილი დაწერილია და 0-ჯერ გამოიძახება (A5) |

---

## 5. TOP 5 პრიორიტეტი

| # | რა | რატომ პირველი | სირთულე |
|---|---|---|---|
| **1** | **Rate limiting** (A1) + `/media/sync` უფლების გასწორება (A4) | ორივე **პატარაა** (ერთად ნახევარი დღე) და ორივე უსაფრთხოებისაა. A4 სამსტრიქონიანი ცვლილებაა და უფლებების მოდელის ნამდვილ ხვრელს ხურავს. A1-ის გარეშე ღია რეგისტრაცია + გაზიარებული API-კვოტები ერთმანეთს ვერ ეგუება | დაბალი |
| **2** | **SSRF + პასუხის ზომის ჭერი** (A2, A3) | ერთი `SafeHttp` helper ორივეს ხურავს **და** D3-ის საფუძველიც ხდება. A3 დამატებით იმას ასწორებს, რომ დოკბლოკი დღეს ტყუის | საშუალო |
| **3** | **ჩამოწერის გაჭედვა** (B1) + **ჩატის წაშლა** (B2) | ორივე მომხმარებლისთვის **ხილული** ბაგია, რომელიც ჩუმად ტყდება. B1-ს ჩვეულებრივი ტერმინალის დახურვა იწვევს. ორივე პატარა და ორივე ტესტით იფარება | დაბალი-საშუალო |
| **4** | **`makeKey()` ერთ ადგილას** (B3) + `per_page` (B4) | 500-ის შეცდომა ჩვეულებრივ შენახვაზე, ცხრა ასლში. გამოსწორება **დუბლირებასაც** ხსნის — ცხრა ფუნქცია ერთდება | დაბალი |
| **5** | **`AuthTest` + `MovieModuleTest`** (D-ის ბოლო ორი) | 456 ტესტი აქვს პროექტს და **ავტორიზაცია დაფარული არ არის**. სანამ 1-4 იცვლება, ტესტები იმისთვისაა საჭირო, რომ ცვლილებამ ჩუმად არაფერი გატეხოს | საშუალო |

**რატომ არა policy-ები (A5) TOP 5-ში:** ის უფრო დიდი გადაწყვეტილებაა
(„ავამუშავოთ თუ წავშალოთ") და პრაქტიკული ხვრელი **დღეს არ არის** — global
scope ყველა 94 ადგილას სწორად მუშაობს. მაგრამ ის მეექვსეა და პასუხს ითხოვს.

**რატომ არა Queue (D1) TOP 5-ში:** ის ყველაზე დიდი მოგებაა, მაგრამ ყველაზე
დიდი სამუშაოც — და მას სჭირდება წინაპირობა, რომელიც `Tasks.md`-შივე გიწერია:
**MariaDB (და მერე `queue:work`) Windows-ის სერვისად**. ჯერ ის.
