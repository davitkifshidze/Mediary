{
 "__unparsedToolInput": {
  "raw": "{\"area\": \"replacing explanatory paragraph text with icons + tooltips across the app\", \"current_state\": \"There is NO shared \\\"info icon + tooltip\\\" primitive at page level. Explanatory prose is hand-written as muted `<p>` / `<li>` / boxed `<div>` on almost every page.\\n\\nWhat exists today:\\n\\n1) `frontend/src/components/ui/tooltip.tsx` (28 lines) is a thin Radix re-export: `TooltipProvider`/`Tooltip`/`TooltipTrigger` are raw primitives, only `TooltipContent` is wrapped (`LAYER_TOOLTIP` = `z-[110]`, `max-w-xs rounded-md border bg-popover px-2.5 py-1.5 text-xs`). Mounted once in `main.tsx:25` as `<TooltipProvider delayDuration={0}>`, inside `QueueProvider`, outside `<App/>`. 14 files use `TooltipTrigger` today, all of them wrapping an *existing* control (a card, a badge, a button) — none is a standalone \\\"explain this section\\\" affordance.\\n\\n2) `frontend/src/components/ui/field-label.tsx` ALREADY is 90% of the requested component — `FieldHint({hint, required})` renders a `<button type=\\\"button\\\">` trigger with a lucide `<Info>` and a `<TooltipContent>` that stacks a red \\\"required\\\" line above the hint line, i.e. it already models \\\"one element, two independent texts\\\". But it is form-scoped, not page-scoped: it imports `Label`, its critical text is hard-coded to `t('form.requiredHint')` (the caller cannot supply one), and `tabIndex={-1}` makes it keyboard-unreachable on purpose (fine when the `*` marker duplicates the meaning, fatal for a standalone icon whose tooltip is the ONLY carrier of the text).\\n\\n3) TOUCH IS BROKEN BY CONSTRUCTION — verified in the installed source, `frontend/node_modules/@radix-ui/react-tooltip@1.2.16/dist/index.mjs:190-213`: `onPointerMove: (event) => { if (event.pointerType === \\\"touch\\\") return; ... }`, `onPointerDown: () => { if (context.open) context.onClose(); isPointerDownRef.current = true }`, `onFocus: () => { if (!isPointerDownRef.current) context.onOpen() }`, `onClick: context.onClose`. A tap therefore opens nothing at all: pointermove bails on touch, pointerdown arms the guard",
  "len": 17967
 }
}