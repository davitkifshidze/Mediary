# Tasks — Mediary

> მიმდინარე დავალებების ერთადერთი წყარო. **Claude-მ ეს ფაილი ყოველ სესიაზე უნდა წაიკითხოს.**
> **შესრულებული პუნქტი აქედან იშლება** — ფაილი მხოლოდ ღია სამუშაოს ინახავს.
> მზიდი წესები `CLAUDE.md`-შია.
>
> ⚠️ **`docs/DECISIONS.md` წაშლილია (2026-09-14, შენი მითითებით).** ღია
> კითხვა აღარსად ინახება ცალკე — თუ რაიმე გადასაწყვეტია, იმ თასქშივე წერია,
> რომელსაც ეხება.

---

## 🩺 მდგომარეობა — 2026-09-14 (თარგმანის `review` რეჟიმის შემდეგ)

| შემოწმება | შედეგი |
|---|---|
| `php artisan test` | **452 ტესტი · 2386 assertion · გავიდა** |
| `npm test` (Vitest) | **94 ტესტი · გავიდა** |
| `npm run build` (`tsc -b` + vite) | **გავიდა** |
| `python frontend/src/i18n/audit.py` | **OK** (ka/en სინქრონში) |
| `npm run lint` (oxlint) | **0 შეცდომა**; მხოლოდ ძველი კოსმეტიკური გაფრთხილებები |
| `./vendor/bin/pint --test` | **გავიდა** |
| მიგრაცია | `translation_usages` **გაშვებულია ცოცხალ ბაზაზე** |

**თარგმანის ბლოკი დახურულია.** წყაროს არჩევანი, ლოგი, ლიმიტები და ბოლოს
**`review` რეჟიმი** — ყველა გაკეთდა; მზიდი წესები `CLAUDE.md`-შია
(„Translation sources, log and quota" + „Translation `review` mode").
შენი პასუხის მიხედვით გადამოწმებული ტექსტის წყარო **`translation`-ია**.

### `review`-ის მოცულობა ცოცხალ ბაზაზე (გაზომილი 2026-09-14)

`movie_translations`: en — 357 ჩანაწერი / 97,782 სიმბოლო, ka — 312 / 67,368.
მთელი ბიბლიოთეკა ≈ **25k input / ~60k output ტოკენი**, ე.ი. ~357 მოთხოვნა —
Gemini-ის უფასო დღიური ლიმიტის (1,500) **მეოთხედი, ანუ 0 ხარჯი**.
გაზომილი ტემპით (1.6 წმ ერთი გადამოწმება) + 4000 ms პაუზა ≈ **35 წუთი**.
⚠️ გაშვება შენი გადასაწყვეტია — ეს ერთადერთი რეჟიმია, რომელიც არსებულ
ტექსტს გადაწერს.

---

## 🔧 ორი მანქანური საქმე (ამაღლებული shell სჭირდება)

- **MariaDB Windows-ის სერვისად** — ყოველი არასუფთა გაჩერება აღდგენის ციკლია.
- **`yt-dlp` / `ffmpeg` დაყენება** — ორივე ხელახლა შემოწმდა 2026-09-14-ს: არაა.
