# Tasks — Mediary

> მიმდინარე დავალებების ერთადერთი წყარო. **Claude-მ ეს ფაილი ყოველ სესიაზე უნდა წაიკითხოს.**
> შესრულებულს ვნიშნავთ `[x]`-ით. სექციები პრიორიტეტის მიხედვით (A → H).
> **⚠️ ნიშნით მონიშნული პუნქტები საჭიროებს ჯერ user-თან შეთანხმებას — არ დაიწყო იმპლემენტაცია.**

---

## A. ბაგები (სწრაფი გასწორებები, პირველ რიგში)

### A1. „აღმოაჩინე“-ს refresh ერორი
- [x] refresh-ზე დაჭერისას ბრუნდება ვალიდაციის ერორი:
  `{refresh: ["The refresh field must be true or false."]}`.
- [x] მიზეზი: axios `true`-ს query-ში `"true"` სტრიქონად აგზავნის, Laravel-ის `boolean`
  წესი კი მხოლოდ `true/false/1/0/"1"/"0"`-ს იღებს. გასწორდა ორივე მხრიდან:
  `api/media.ts` → `discover()` აგზავნის `refresh=1`-ს (0-ის ნაცვლად საერთოდ არ აგზავნის);
  `DiscoverController::index()` → ვალიდაციამდე `$request->merge(['refresh' => $request->boolean('refresh')])`.
- [x] გადამოწმდა discover-ის ორივე type-ზე (`movie` და `series`) — `true/1/false/0/yes`
  და პარამეტრის გარეშე, ყველგან 200.

### A2. კატეგორიების (ჟანრების) რაოდენობა არასწორია
- [x] რეალური მიზეზი: `/api/genres` მხოლოდ `movies_count`-ს აბრუნებდა, ამიტომ
  სერიალების ბიბლიოთეკაშიც **ფილმების** რიცხვი ჩანდა (მაგ. drama: 79 ფილმის ნაცვლად 12 სერიალი).
- [x] backend: `GenreController::index()` → `withCount(['movies','series'])`, `GenreResource`
  აბრუნებს ორივეს (`movies_count` + `series_count`); `?type=movie|series` განსაზღვრავს დალაგებას.
  `store`/`update` → `loadCount(['movies','series'])`.
- [x] frontend: `fetchGenres(type?)`, `GenreChips`-ს დაემატა `type` prop და ირჩევს
  შესაბამის რაოდენობას; `LibraryPage` queryKey = `['genres', type]`; ჟანრების გვერდზე
  ორივე ჩანს (`N ფილმი · M სერიალი`, ახალი i18n გასაღები `genres.seriesCount`).
- [x] გადამოწმდა curl-ით: movie-სორტი → action 124/0, drama 79/12; series-სორტი →
  drama 12, sci-fi-fantasy 8, action-adventure 7.

### A3. მსახიობის გვერდზე სერიალების პაგინაცია გატეხილია
- [x] მსახიობის გვერდზე სერიალების სექციაში გვერდების გადართვა არასწორად მუშაობდა:
  - ზედა ზოლი (header/მთვლელი) არ იცვლება ახალ გვერდზე გადასვლისას;
  - ახალ გვერდზე პირველი ჩანაწერი უცვლელი რჩება;
  - უკან დაბრუნებისას ჩანაწერები მეტდება და **დუბლირდება**.
- [x] რეალური მიზეზი სხვა აღმოჩნდა (pagination state გამიჯნული იყო): `CastController::show()`
  TMDB-ის `personTvCredits`-ს დედუპლიკაციის გარეშე აბრუნებდა, TMDB კი **თითო როლზე**
  ცალკე ჩანაწერს გვაძლევს — Bryan Cranston: 123 ჩანაწერი, მხოლოდ 103 უნიკალური
  (Family Guy ×6). დუბლირებული `key={s.tmdb_id}` React-ს ელემენტების არასწორად
  გადამისამართებას აიძულებდა → „პირველი ჩანაწერი არ იცვლება" და „დუბლირდება".
- [x] backend: ორივე ციკლს (movies + series) დაემატა `$seen` დედუპლიკაცია `tmdb_id`-ით,
  პირველი (ყველაზე პოპულარული) ჩანაწერი რჩება.
- [x] frontend: `SuggestionSection`-ში გვერდი დიაპაზონში იჭერება (`current = min(page, pages)`),
  რომ სიის შემცირებისას ცარიელი ბადე არ გამოჩნდეს.
- [x] გადამოწმდა: 3 მსახიობზე dupes=0 (Cranston 123→103, Aaron Paul 50→46).

### A4. ფილმის წაშლის დადასტურების popup — პოზიცია/ანიმაცია
- [x] მიზეზი: Tailwind v4 `-translate-x-1/2 -translate-y-1/2`-ს ცალკე CSS თვისებად
  (`translate: -50% -50%`) აკომპილირებს, `fb-pop-in` კეიფრეიმი კი `transform`-ში ისევ
  `translate(-50%,-50%)`-ს აწესებდა — ორი თვისება ერთმანეთს ეშტაბელებოდა, ამიტომ
  ანიმაციის 160ms-ის განმავლობაში დიალოგი ორმაგად წაწეული (მარცხნივ-ზემოთ) იყო,
  ბოლოს კი ხტებოდა ცენტრში.
- [x] `index.css` → `fb-pop-in` მხოლოდ `scale`-ს ცვლის. ეხება ორივე დიალოგს:
  `ui/feedback.tsx` (useConfirm — ფილმის წაშლა) და `GenresPage` (ჟანრის წაშლა).

### A5. წაშლის შემდეგ არასწორ გვერდზე გადასვლა
- [x] `MovieCard`-ის ბმული დეტალურ გვერდზე ახლა `state.from`-ს გადასცემს (საიდან შემოვედი).
- [x] `MoviePage`: „უკან" ბმულიც და წაშლის შემდგომი გადასვლაც იყენებს `from`-ს
  (ბიბლიოთეკა / მსახიობის გვერდი), ხოლო თუ `from` სხვა **ჩანაწერის** გვერდია
  (ფრანჩაიზის ბმულით მოსვლა) — ჩართულია `isDetailPath()` დაცვა და ვბრუნდებით
  ბიბლიოთეკაზე. ე.ი. სხვა ფილმზე ვეღარასდროს გადავა.
- [x] წაშლის გადასვლა გახდა `replace: true` — წაშლილი ჩანაწერის URL ისტორიაში აღარ რჩება
  (Back-ზე მკვდარი გვერდი იხსნებოდა).
- [ ] ⚠️ ზუსტი რეპრო („სხვა ფილმზე გადადის") კოდში ვერ დავადასტურე — წაშლა უკვე
  `libraryPath`-ზე გადადიოდა. თუ კვლავ განმეორდება, მიწერე რომელი გვერდიდან.

### A6. Cursor pointer clickable ელემენტებზე
- [x] გავიარე მთელი `src` სკრიპტით (ფრჩხილებზე ზუსტი JSX-სკანერი): 28 clickable
  ნატიფი ელემენტი + Radix-პრიმიტივები + hover-სტილიანი ელემენტები — **დარღვევა 0**,
  cursor `buttonVariants`/`base`/კომპონენტების ბაზისურ კლასებშია.
- [x] რეალური ხარვეზი **react-select**-ში იყო (ჟანრების ასარჩევი `GenreSelect`,
  `GenreSingleSelect`, `MovieMultiSelect` — არა Tailwind-ის კომპონენტი): მისი შიდა
  ელემენტები default-ად `cursor: default`-ს იყენებს. `lib/selectStyles.ts`-ში
  `cursor: 'pointer'` დაემატა `control`-ს, **`multiValueRemove`-ს (ჟანრის მოხსნის „x")**,
  `dropdownIndicator`-სა და `clearIndicator`-ს (`option`-ს უკვე ჰქონდა).
- [x] ერთი ფაილის ცვლილება ავტომატურად ეხება სამივე select-ს ფორმებზეც და ჟანრების გვერდზეც.

### A7. სერიალის დამატების ღილაკი და სათაურის ლეიბლი
- [x] ღილაკი საიდბარშია (`w-64` — ამიტომ იშლებოდა ორ ხაზზე). ორივე სექციაში
  (ფილმები/სერიალები) ახლა მოკლე `actions.addShort` = „დამატება" / „Add" —
  სექციის სათაური ისედაც ამბობს რომელ დომენზეა.
- [x] ზუსტი სტრიქონი „სერიალია" კოდში არ არსებობდა; რეალური ხარვეზი ისაა, რომ
  **სერიალის ფორმაზე lookup-ის ტექსტები „ფილმზე" საუბრობდა**. დაემატა ka/en სერიის
  ვარიანტები (`lookupPlaceholderSeries`, `lookupPickSeries`, `lookupNotFoundSeries`,
  `alreadyAddedSeries`) და `MovieFormPage`-ში `tm()` ჰელპერი ირჩევს type-ის მიხედვით.
- [ ] თუ სახელის ველზე კონკრეტულად სხვა მცდარი ტექსტი გინახავს — მიწერე, რომელ
  გვერდზე/ენაზე, და გავასწორებ.

---

## B. UI / UX გაპრიალება

### B1. ჟანრის მთვლელის border-radius
- [x] `GenresPage`-ის მთვლელი badge (`rounded-full`) → `rounded-[5px]`, იგივე რადიუსი
  რაც ჟანრების ჩიპებს (`GenreChips`) აქვს.

### B2. დამატებების queue (მარჯვნივ ქვემოთ)
- [x] `enqueue()` ახალ პარტიაზე `setExpanded(false)` + თუ რიგი უსაქმოა, წინა პარტიის
  `done`/`error` ჩანაწერებს ასუფთავებს (მთვლელი `N/M` თავიდან იწყება).
- [x] ჩაკეცვა/ამოკეცვა: მთელი header ზოლი გახდა ტოგლი (`aria-expanded`), ჩევრონი
  ინდიკატორად დარჩა; „დახურვა" (X) ცალკე ღილაკია.
- [x] ზომა: `max-w-sm` → `max-w-md`, სია `max-h-56` → `max-h-80`, ჩანაწერის ტექსტი
  `text-xs` → `text-sm`, padding ოდნავ გაიზარდა.

### B3. დამატებული ჩანაწერი არ „ამოვარდეს“
- [x] მიზეზი: `CastController::show()` შემოთავაზებებიდან **ფილტრავდა** უკვე დამატებულებს
  (`in_array($c['id'], $ownTmdbIds)`), ამიტომ დამატებისთანავე ბარათი ქრებოდა.
  ახლა ჩანაწერი რჩება და ბრუნდება `owned` + `movie_id` (როგორც discover-ში).
- [x] `ActorPage`: `owned` ბარათს „დამატებულია" ნიშანი აქვს და ბმულია დეტალურ გვერდზე
  (add-overlay/queue მხოლოდ არადამატებულებზე).
- [x] „აღმოაჩინე"-ს ბადეს ეს ქცევა ისედაც ჰქონდა (`owned` badge) — შეცვლა არ დასჭირვებია.
- [ ] შენიშვნა: owned შემოთავაზება ახლა ორჯერ ჩანს — „ჩემს კოლექციაში" სექციაშიც და
  შემოთავაზებებშიც. თუ დუბლი აწუხებს, მიწერე და შემოთავაზებებში დავმალავ.

### B4. ფილტრის გასუფთავების ღილაკი
- [x] `LibraryPage`-ის „გასუფთავება" link-იდან გახდა `Button variant="outline" size="sm"`
  მოწითალო ტონით (`border-destructive/40 text-destructive hover:bg-destructive/10`) + X აიქონი.
- [x] underline მოიხსნა; ღილაკი ყოველთვის ჩანს (ცარიელ ფილტრზე `disabled` — layout არ ხტება).

### B5. საიდბარის საწყისი მდგომარეობა
- [x] `Sidebar`-ის საწყისი state → `{ movie: false, series: false }`.

### B6. სერიალების გვერდზე შესვლისას სექციების მდგომარეობა
- [x] დომენის შეცვლაზე (`useRef`-ით დაცული — mount-ზე არ ირთვება, რომ B5 არ დაირღვეს)
  აქტიური სექცია იშლება, დანარჩენი იკეცება.
- [x] დომენი ახლა მხოლოდ დომენის მარშრუტებზე ითვლება (`domainFromRoute`): `/genres`,
  `/status`, `/actors/*` → `null`, ე.ი. ამ გვერდებზე გადასვლა სექციებს არ ანძრევს.

### B7. ქართული სახელები შემოთავაზებებში
- [x] **მიზეზი აღმოჩნდა სხვა:** `title_ka` უკვე ბრუნდებოდა, მაგრამ ტოლი იყო ინგლისურის —
  `Translator` (EN→KA) არ მუშაობს: `ANTHROPIC_API_KEY` ცარიელია, Google-ის უფასო endpoint
  კი **429**-ს აბრუნებს, ჩავარდნაზე კლასი ორიგინალებს აბრუნებს.
- [x] გადაწყდა TMDB-ით: მას **რეალურად აქვს** ქართული სახელები (`language=ka`).
  `TmdbClient`-ს `$language` პარამეტრი დაემატა (`searchMoviesPaged`, `searchTvPaged`,
  `personCredits`, `personTvCredits`; `discover`/`discoverTv` ისედაც იღებდა params-ში).
- [x] `DiscoverController` + `CastController` აკეთებს მეორე (ka) გამოძახებას და სახელებს
  `tmdb_id`-ით ამწყვდევს. ka გამოძახება არასავალდებულოა (`try/catch` → ინგლისური რჩება).
- [x] `App\Support\Lang::georgian()` — TMDB ka-თარგმანის უქონლობაზე ჩუმად ორიგინალ ენას
  აბრუნებს (მაგ. „Facing El Chapo" → „La captura"), ამიტომ სახელს ვიღებთ მხოლოდ თუ
  მასში მხედრულია (`\p{Georgian}`).
- [x] frontend: `lib/display.ts` → `tmdbTitle()` / `tmdbSubtitle()`; `ActorPage`-ისა და
  `DiscoverModal`-ის ბარათებზე მთავარი სახელი მიმდინარე ენაზეა, ქვემოთ — მეორე ენა
  (თუ განსხვავდება).
- [x] გადამოწმდა: discover movie/series → „სპაიდერმენი: ახალი დღე", „ოდისეა", „რიჩერი";
  Keanu Reeves → „მატრიცა", „ჯონ უიკი: თავი 4"; owned 13/135, dupes 0.
- [ ] `Translator` აღარ გამოიძახება discover/cast-ში (ზედმეტი latency უშედეგოდ) —
  რჩება F1-ისთვის (აღწერების თარგმანი), სადაც `ANTHROPIC_API_KEY` დაგვჭირდება.

---

## C. ჟანრების მართვა (ფიჩერი)

### C1. ჟანრის რედაქტირება — ფილმების გადანაწილება
- [x] backend: ახალი `GenreItemController` + მარშრუტები
  `GET /api/genres/{genre}/items` (ორივე დომენი ერთ პასუხში: `movies`, `series`,
  `movies_count`, `series_count`) და `POST /api/genres/{genre}/items`
  (`type`, `action`, `ids`|`all`, `target_genre_id` → `{affected, genre}`).
- [x] `action`-ები: `attach` / `detach` / `move` / `replace`.
  - **move** — ეს ჟანრი მოეხსნება, სამიზნე დაემატება; ჩანაწერის დანარჩენი ჟანრები რჩება.
  - **replace** — ჩანაწერს **ყველა** ჟანრი ჩაენაცვლება სამიზნით (`sync([target])`),
    ამიტომ UI-ში დადასტურებით არის დაცული.
- [x] `all=1`: `attach`-ზე ბიბლიოთეკის ყველა ჩანაწერი, დანარჩენზე — ჟანრზე მიბმული ყველა.
  id-ები ფილტრდება არსებულებზე, ცვლილება `DB::transaction`-შია.
- [x] frontend: `components/GenreItemsManager.tsx` — ჟანრის რედაქტირების დიალოგში
  დომენის ტაბები რაოდენობით, მიბმულთა სია checkbox-ებით + „ყველას მონიშვნა",
  სამიზნე ჟანრის select და ქმედებები (ჩახსნა / გადატანა / ჩანაცვლება).
  ჩახსნა და ჩანაცვლება `useConfirm`-ით.
- [x] იმავე დიალოგში ჟანრში **დამატებაც** (`MovieMultiSelect` — მხოლოდ ჟანრზე
  არმიბმული ჩანაწერები).
- [x] მთელ ჟანრზე: სრული წაშლა და „სხვა ჟანრზე გადატანა + წაშლა" უკვე იყო
  (`DELETE /genres/{id}` + `reassign_to`/`force`) — **მაგრამ მხოლოდ ფილმებს ითვლიდა**.
  გასწორდა: `destroy()` ორივე დომენს ითვლის და გადაბმისას სერიალებსაც გადაიტანს;
  409-ის პასუხს `series_count` დაემატა. frontend-ის წაშლის დიალოგიც ჯამურ რიცხვს იყენებს
  (მანამდე სერიალებზე მიბმული ჟანრი გაფრთხილების გარეშე იშლებოდა).
- [x] `ModalShell`-ს დაემატა `wide` + `max-h-[90vh] overflow-y-auto`.
- [x] **I7-ის შემდეგ:** ჟანრები გლობალურია — ჩვეულებრივი მომხმარებლის წაშლა 202-ს აბრუნებს
  და ადმინთან მიდის დასადასტურებლად; მიბმული ჩანაწერების მთვლელი წაშლისას **ყველა
  მომხმარებელზე** ითვლება (`Services/Genres/GenreRemover`), სიაში კი — per-user.
- [x] `useConfirm`-ის z-index 60/61 → **90/91** — მოდალის შიგნიდან გამოძახებული
  დადასტურება სხვა დიალოგებზე და queue-ს ტოსტზე (z-70) მაღლა უნდა იყოს.

### C2. ჟანრის დამატებისას ფილმების მონიშვნა
- [x] `GenreItemsPicker` — შექმნის დიალოგში ორივე დომენის multi-select + „ყველა (N)"
  და „გასუფთავება" ღილაკები (ერთი / რამდენიმე / ყველა).
- [x] ჟანრი ჯერ იქმნება, მერე ერთ mutation-ში ეწერება მიბმები
  (`createGenre` → `updateGenreItems(attach)`).

**გადამოწმდა curl-ით** (დროებით ჟანრებზე/ფილმზე, მონაცემები დასუფთავებულია):
attach 1 → move A→B → replace (ჩანაწერს მხოლოდ სამიზნე დარჩა) → detach `all`;
არასწორი სამიზნე → 422; `attach all` სერიალებზე → 22; წაშლა flag-ების გარეშე → 409
`{movies_count:0, series_count:22}`; `GET drama/items` → 78 ფილმი / 17 სერიალი.

---

## D. სორტირება და დაჯგუფება

### D1. ჯგუფის გათვალისწინება სორტირებისას
- [x] backend: `GET /api/movies?group=0|1` — `group=0`-ზე `clusterByFranchise()` არ
  გამოიძახება და სია სუფთად დალაგებული ბრუნდება. default (პარამეტრის გარეშე) — ჩართული.
- [x] frontend: `LibraryPage`-ზე დაჯგუფების ტოგლი (Layers აიქონი, `aria-pressed`),
  დალაგების ისრის გვერდით; ჩართულზე primary-ჩარჩო. `MediaFilters.group` queryKey-შია,
  ამიტომ გადართვა ავტომატურად ახლებს სიას.
- [x] გამორთულზე ჩანს განმარტება (`sort.groupOffHint`): „ფრანჩაიზის ნაწილები ერთად
  აღარ იდგება: დალაგება სუფთად არჩეული ველითაა, ამიტომ მათ შორის სხვა ფილმები მოხვდება."
- [x] ტოგლი მხოლოდ ფილმებზეა — TV-ს `belongs_to_collection` არ აქვს, `SeriesController`
  კლასტერიზაციას არ აკეთებს.
- [x] „ჯგუფი თავში წამოვიდეს, თუ ახალი ნაწილი აქვს" — ეს არსებული ლოგიკიდან გამომდინარეობს:
  კლასტერი დგება მისი **პირველი (sort-ით)** ნაწილის პოზიციაზე, ე.ი. `year_desc`-ზე
  ყველაზე ახლის ადგილას. გადამოწმდა: Black Panther 3 (2028) → მთელი ჯგუფი თავშია.

### D2. დაჯგუფებული ფილმების თანრიგი
- [x] ჯგუფის შიგნით მიმართულება იყო ფიქსირებული `year asc` (ძველი მარცხნივ) —
  გახდა `year desc` (**ახალი მარცხნივ, კლებადობით მარჯვნივ**).
- [x] „როცა ფილტრი არ დევს" გავიგე როგორც „როცა ზრდადი დალაგება არ არის არჩეული":
  `*_asc` sort-ზე ჯგუფის შიგნითაც ზრდადი ხდება, თორემ ჯგუფი მთელი სიის მიმართულებას
  ეწინააღმდეგებოდა. სტატუსი/ჟანრი/დიაპაზონის ფილტრები მიმართულებაზე არ მოქმედებს.
- [x] გადამოწმდა curl-ით: `year_desc&group=1` → Avengers 2027→2012 (ახალი მარცხნივ);
  `year_desc&group=0` → 2028, 2027, 2027, 2026… (ჯგუფები დაშლილია);
  `year_asc&group=1` → Godfather 1972→1990, Jumanji 1995→2026.

---

## E. პარამეტრიზაცია / პარამეტრების გვერდი

### E1. ერთი პარამეტრების გვერდი
- [x] ~~persist = localStorage~~ → **I7-ის შემდეგ persist = backend, per-user**
  (`users.settings` json, `PUT /api/auth/settings`, debounce 400ms). localStorage დარჩა
  ლოკალურ ქეშად და ძველი მდგომარეობის ერთჯერადი მიგრაციისთვის — როგორც დაგეგმილი იყო,
  ცვლილება მართლაც ერთ ფაილში მოთავსდა (`lib/settings.tsx`).
- [x] `lib/settings.tsx` — `SettingsProvider` + `useSettings()`, გასაღები
  `mediary.settings.v1`; უცნობი/მოძველებული გასაღებები იგნორირდება და default-ით ისხამს.
- [x] `pages/SettingsPage.tsx` + მარშრუტი `/settings` + საიდბარის ბმული;
  სექციები: „სიები და გვერდები", „აღმოაჩინე", „მონაცემების სინქრონიზაცია" (J1),
  პლუს „საწყის მნიშვნელობებზე" ღილაკი.

### E2. ჩანაწერების რაოდენობა გვერდზე
- [x] მსახიობის გვერდი: `actorMoviesPerPage` / `actorSeriesPerPage` (5/10/15/20/30/50) —
  `SuggestionSection`-ს `perPage` prop-ად გადაეცემა (ფიქსირებული `PER_PAGE = 10` მოიხსნა).
- [x] ბიბლიოთეკა: `libraryPageSize` (0 = ყველა · 24/48/60/96/120). `LibraryPage`-ს
  დაემატა კლიენტის მხარეს პაგინაცია (API ისედაც სრულ სიას აბრუნებს) — ფრანჩაიზის
  კლასტერიზაცია backend-ზე ხდება, ამიტომ ჭრა თანრიგს არ არღვევს.
- [x] ფილტრის/დალაგების/გვერდის-ზომის ცვლილებაზე პირველ გვერდზე ბრუნდება.

### E3. „აღმოაჩინე“-ს fetch პარამეტრები
- [x] `GET /api/discover` იღებს `max_pages` (1–500) და `per_page` (20–100).
- [x] `discoverMaxPages` — რამდენ გვერდამდე მივყვეთ; `discoverPerPage` — ჩანაწერი გვერდზე.
- [x] backend: ერთი „ჩვენების" გვერდი = `chunk = per_page / 20` ცალი TMDB გვერდის
  შერწყმა; `total_pages = ceil(min(tmdb_total, max_pages) / chunk)`; ქეშის გასაღებში
  `chunk`-იც და `max`-იც შედის.
- [ ] ⚠️ **10000 გვერდი ტექნიკურად შეუძლებელია:** TMDB `page > 500`-ზე შეცდომას აბრუნებს
  (მყარი API ლიმიტი, არა ჩვენი). ამიტომ ზედა ზღვარი 500-ია — `per_page=100`-ზე ეს
  50 000 ჩანაწერამდეა. თუ მეტი გჭირდება, სხვა გზა სჭირდება (მაგ. ჟანრით/წლით დაჭრილი
  რამდენიმე მოთხოვნა) — მითხარი და დავგეგმავ.
- [x] გადამოწმდა curl-ით: `per_page=20` → 20 ჩანაწერი / 100 გვერდი; `per_page=60&max_pages=500`
  → 60 ჩანაწერი / 167 გვერდი, page 1 vs page 2 გადაფარვა **0**; `max_pages=3` → 3 გვერდი;
  `max_pages=9999` → 422.

### E4. ⚠️ დამატებითი პარამეტრიზაცია — შესათანხმებელი
- [ ] Claude-მ შემოგვთავაზოს კიდევ რისი პარამეტრიზაცია შეიძლება (მაგ. ჩვევადი
  ენა, default სორტირება, poster ზომა, ქეშის TTL და ა.შ.), **მაგრამ ჯერ user-თან
  შეთანხმდეს, არ დაიწყოს იმპლემენტაცია.**

---

## F. თარგმანის ფიჩერი (დიდი) + ჰედერი + ავტორიზაცია

### F1. ავტომატური თარგმანი დამატებისას
- [ ] რასაც ვამატებ, ყველას ჰქონდეს სახელისა და აღწერის თარგმანი — ავტომატურად
  ითარგმნოს ყველა (KA↔EN, `ANTHROPIC_API_KEY` უკვე გათვალისწინებულია არქიტექტურაში).
- [ ] ⚠️ **წინაპირობა (გაზომილი B7-ზე):** `ANTHROPIC_API_KEY` **სავალდებულოა** —
  `Translator`-ის უფასო fallback (Google `translate_a/single`) ახლა **429**-ს აბრუნებს,
  ე.ი. კლავიშის გარეშე თარგმანი უბრალოდ ინგლისურს აბრუნებს უცვლელად.
- [ ] **სახელებზე** თარგმანი ხშირად საჭირო არაა: TMDB-ს `language=ka` რეალურ ქართულ
  სახელს იძლევა (იხ. B7 / `App\Support\Lang::georgian()`). Claude ღირს **აღწერებზე**.

### F2. თარგმანის ნოტიფიკაციები ჰედერში
- [ ] თუ თარგმანი დამატებას ამძიმებს (async-ად უნდა გაკეთდეს), ჰედერში იყოს
  **შეტყობინებების ღილაკი**, სადაც ჩანს „გაქვს N სათარგმნი“.
- [ ] იყოს **„თარგმნა“** ღილაკი, სადაც ირჩევ: კონკრეტულ ფილმს / რამდენიმეს / ჟანრს /
  კატეგორიას / ყველას — და უშვებ თარგმანს.

### F3. ჰედერი
- [ ] დაემატოს სრულფასოვანი ჰედერი (ნავიგაცია + შეტყობინებები + პროფილი).
- [ ] **დეტალები და შეთანხმებული სახე — იხ. K12** (ზოლი, აიქონი+სახელი, ჩამოსაშლელი
  მენიუ: პროფილი / პარამეტრები / გასვლა).

### F4. ავტორიზაცია / რეგისტრაცია / პროფილი
- [x] **Laravel Sanctum, SPA cookie რეჟიმი** (შეთანხმებული): სესია httpOnly ქუქშია,
  ტოკენი ფრონტზე არ ინახება. `bootstrap/app.php` → `statefulApi()`;
  `.env` → `SANCTUM_STATEFUL_DOMAINS`; `config/cors.php` → `supports_credentials=true`
  + `sanctum/csrf-cookie`; axios → `withCredentials` + `withXSRFToken`.
- [x] `/login` და `/register` გვერდები; **ღია რეგისტრაცია** (`ALLOW_REGISTRATION`),
  ახალი ანგარიში ცარიელია — მოდულებს ითხოვს ადმინისგან (I3).
- [x] `/profile`: სახელი, გვარი, username, მეილი, ავატარის ატვირთვა/წაშლა,
  პაროლის ცვლილება (`current_password`-ის შემოწმებით).
- [x] საიდბარში მომხმარებლის ბლოკი (ავატარი, სახელი, როლი) + გასვლის ღილაკი.
- [x] ყველა `/api` route `auth:sanctum`-ის უკან, გარდა `/health`-ისა და
  `/auth/{register,login}`-ისა; 401-ზე ფრონტი ავტომატურად ბრუნდება login-ზე
  (`mediary:unauthenticated` event) და ქეშს ასუფთავებს.

---

## G. ქართული საიტების ლინკები (ჯერ დაგეგმვის ფაზა)

> user მოგცემს საწყის საიტებს (მაგ. `ge.movie`, `maikadri.ge` …). **ჯერ მხოლოდ
> საყურებელი ლინკები** — არა auto-import. პირველი ნაბიჯი: დაგეგმვა/კვლევა.

### G1. კვლევა/დაგეგმვა (deliverable — ცალკე დოკუმენტი)
- [ ] მოამზადე დოკუმენტი: რა თასქები დაგვჭირდება ქართული საიტების გასაპარსად.
- [ ] თითო წყაროსთვის: URL სტრუქტურა, ძებნის endpoint, სათაურის/სეზონის ამოცნობა,
  robots/ToS, rate-limit, ქართული↔ინგლისური სახელის შეჯერების სტრატეგია.
- [ ] მაგ. ამოცანა: „მოძებნე 10 ლინკი, სადაც ქართულად ყურებაა შესაძლებელი“.

### G2. პრიორიტეტული ლინკების პარამეტრი
- [ ] პარამეტრებში/პროფილში მიეთითოს რომელ საიტებს ჰქონდეს **პრიორიტეტი**.
- [ ] ძებნისას ჯერ user-ის ჩაწერილი პრიორიტეტული ლინკები წამოვიდეს, დანარჩენი — ავტომატურად.

### G3. ლინკების რაოდენობის პარამეტრიზაცია + შეტყობინება
- [ ] იმართებოდეს, თითო ფილმზე/სერიალზე რამდენი ლინკი დაიწეროს.
- [ ] მაგ.: 3 ლინკის ლიმიტი, 5 პრიორიტეტული — დაიწეროს მხოლოდ 3.
- [ ] შეტყობინება: „მეტი ლინკის წამოღებაა შესაძლებელი — წამოვიღოთ?“ — მეტი არ
  წამოიღოს, სანამ user არ დაუდასტურებს ძებნის გაშვებამდე.
- [ ] ეს ფუნქციონალი **ორივესთვის** — ფილმებზეც და სერიალებზეც.

### G4. ფილმის vs სერიალის ლინკის გარჩევა
- [ ] სისტემამ გაარჩიოს ფილმისა და სერიალის ლინკი მაშინაც, თუ სახელი იდენტურია.

### G5. სერიალის სეზონების ფუნქციონალი
- [ ] ვიცოდე რამდენი სეზონია გამოსული და რამდენი სეზონის ქართული ლინკია ნაპოვნი.
- [ ] შეტყობინება/ჩვენება სერიალის შიგნით: მაგ. „5 სეზონია, ამ საიტზე მხოლოდ 3 სეზონის
  ლინკია; სხვა საიტზე — 2 სეზონის“ (per-site სეზონების დაფარვა ჩანდეს).

---

## H. უცხოური საიტების ლინკები (მხოლოდ უფასო ყურება)

### H1. უცხოური ლინკების გაწერა
- [ ] ფილმებზეც და სერიალებზეც შესაძლებელი იყოს **უცხოური** საიტის ლინკების გაწერა,
  **მხოლოდ ისეთი, სადაც ყურება უფასოა**.

### H2. იგივე პარამეტრიზაცია, რაც G-ში
- [ ] ფავორიტი/პრიორიტეტული ლინკები, რაოდენობის ლიმიტი, „რამდენი წამოიღოს“.
- [ ] თუ ფავორიტი არ არის — წამოიღოს იმდენი, რამდენიც გაწერილია; თუ ვერ იპოვა —
  წამოიღოს რამდენსაც იპოვის.

---

## I. მრავალმომხმარებლიანობა + მოდულური სისტema + უფლებები (დიდი ეპიკი) ⚠️

> **ხედვა:** Mediary უნდა გახდეს მრავალმომხმარებლიანი, **მოდულური** პლატფორმა. თითოეულ
> მომხმარებელს ჰქონდეს **საკუთარი** მონაცემები და **მხოლოდ ის მოდულები**, რაც სუპერ-ადმინმა
> ჩაურთო. ამჟამად არსებული ორი მოდული (ფილმები, სერიალები) გახდეს პირველი ორი „plug-in".
>
> **✅ შესრულებულია (2026-08-28).** დიზაინი: `docs/I7-multi-tenancy-and-modules.md`
> (§12 — დამტკიცებული პასუხები). შესრულდა ხუთივე ეტაპი: auth → მფლობელობა → მოდულები →
> ადმინი → per-user პარამეტრები, **პლუს I5 (ვიდეოს მოდული)**, რომელმაც recipe რეალურად
> გამოსცადა. გადამოწმება: `php artisan test` **18/18**, `npm run build` ok,
> API ხელით ორ ანგარიშზე. ღიაა მხოლოდ I6 (მომავალი მოდულების ჩამონათვალი).

### I1. მონაცემთა per-user მფლობელობა (multi-tenancy)
- [x] `movies`/`series`-ს დაემატა `user_id` (FK, cascade) + `BelongsToUser` ტრეიტი:
  global scope `owner` და `user_id`-ის ავტო-შევსება ჩაწერისას. ე.ი. სია, ფილტრები,
  `withCount`, route-model-binding — **ყველა ავტომატურად სკოუპდება**, controller-ები უცვლელია.
- [x] სხვისი ჩანაწერი → **404** (და არა 403 — id-ების ჩამოთვლაც იკეტება). მეორე ფენა:
  `MoviePolicy`/`SeriesPolicy`.
- [x] „ერთი TMDB ფილმი რამდენიმე user-ს, თითოს თავისი სტატუსი/ლინკები" — სრულდება
  **per-user რიგებით** (ვარიანტი A); pivot-ები (`genreables`/`castables`) და
  `GenreItemController` უცვლელი დარჩა, რადგან მფლობელობა მშობელი ჩანაწერიდან მოდის.
- [x] `genres` და `cast_members` — **გლობალური** (შეთანხმებით: ჟანრებს `user_id` არ დაემატა).
  მედია-ფაილები ისედაც კონტენტზეა მიბმული (`posters/<slug>.jpg`), ე.ი. დისკზე დუბლი არ ჩნდება.
- [x] `movies.imdb_id`/`series.imdb_id` unique → **`unique(user_id, imdb_id)`** (თორემ ორი
  მომხმარებელი ერთსა და იმავე ფილმს ვერ დაამატებდა) + `(user_id,status)`/`(user_id,year)` ინდექსები.
- [x] data-migration: არსებული **258 ფილმი + 22 სერიალი** მიება პირველ super_admin-ს (id=1);
  მიგრაცია ჩერდება გასაგები შეტყობინებით, თუ ჩანაწერებია და მომხმარებელი არა. rollback — `down()`.
- [x] `php artisan media:redownload --user=` — CLI-ს `Auth::id()` არ აქვს, ე.ი. scope უქმია;
  `--user`-ის გარეშე გაფრთხილებაა, რომ ყველა მომხმარებელზე გაივლის.

### I2. მოდულური არქიტექტურა (pluggable modules)
- [x] `modules` ცხრილი: `key`, `name_ka/en`, `description_ka/en`, `icon`, `route_base`,
  `api_base`, `morph_alias`, `is_sensitive`, `enabled_by_default`, `is_active`, `sort_order`.
  ფილმები/სერიალები — პირველი ორი ჩანაწერი (`ModulesSeeder`).
- [x] frontend: `lib/modules.tsx` (`ModulesProvider`/`useModules`) — ნავიგაციაც და
  **მარშრუტებიც** `GET /api/modules`-იდან იგება; `lib/media.ts` დარჩა generic გვერდების
  დესკრიპტორად. ჩაურთველ მოდულს არც მენიუში აქვს ჩანაწერი და არც მისამართი.
- [x] recipe „როგორ ემატება ახალი მოდული" — `docs/I7-multi-tenancy-and-modules.md` §7.3 (8 ნაბიჯი).

### I3. მოდულების per-user ჩართვა/გამორთვა
- [x] `module_user` pivot (`settings` json + `enabled_at`).
- [x] backend: `module:movie` / `module:series` middleware; გაზიარებულ endpoint-ებზე
  (`/lookup`, `/discover`, `/media/sync/{type}/{id}`) — `module:@type`, რომელიც დომენს
  `type` პარამეტრიდან იღებს. ჩაურთველზე → **403 `module_not_enabled`**.
- [x] frontend: `/modules` გვერდი — რა მაქვს ჩართული, რას ვთხოვ ადმინს, მოთხოვნების ისტორია.

### I4. როლები, უფლებები და სუპერ-ადმინი
- [x] როლი `users.role` (`super_admin|user`) + `is_active`; `Gate::before` — ადმინი ყველა
  policy-ს გაივლის. ⚠️ `owner` scope მასზეც მოქმედებს: სხვისი ბიბლიოთეკა ავტომატურად
  **არ** უჩანს, ადმინისეული წვდომა მხოლოდ `/api/admin/*`-ზეა (`withoutGlobalScope`).
- [x] `/admin` პანელი სამი ტაბით: **მოთხოვნები** (badge-ით საიდბარშიც), **მომხმარებლები**
  (როლი, აქტიურობა, per-user მოდულების checkbox-ები, წაშლა) და **მოდულები**
  (`is_active`, `enabled_by_default`).
- [x] დაცვები: ბოლო super_admin-ის ჩამოქვეითება/გათიშვა → 422; საკუთარი თავის წაშლა/გათიშვა → 422.
- [x] `php artisan mediary:bootstrap-admin` + პირველი რეგისტრირებული ავტომატურად super_admin-ია.

### I5. ვიდეო მოდული (ნებისმიერი წყარო)
- [x] `videos` ცხრილი + `Video` მოდელი (`BelongsToUser`) + `VideoController` (CRUD,
  რჩეული, „ვნახე" მთვლელი) + `VideoPolicy`. მარშრუტები `module:video`-ს უკან.
- [x] ბმულის ამოცნობა — `App\Support\VideoUrl`: YouTube (`watch`/`youtu.be`/`shorts`/`live`/`embed`),
  Vimeo, Dailymotion, პირდაპირი ფაილი (`.mp4/.webm/…`), დანარჩენი — `other`.
  YouTube-ს thumbnail ავტომატურად მოაქვს (`i.ytimg.com`), embed — `youtube-nocookie`.
- [x] მეტამონაცემი: სათაური, აღწერა, პლატფორმა, thumbnail (ატვირთული **ან** პლატფორმის),
  ხანგრძლივობა, `tags` (json), რჩეული, ნახვების მთვლელი. **ჟანრებს არ ებმის** — გლობალური
  ჟანრების ლექსიკონი ვიდეოს კატეგორიებით არ უნდა დაბინძურდეს.
- [x] **18+ ცალკე მოდულია** (`video_adult`, `is_sensitive`): მისი გარეშე ასეთი ჩანაწერი
  არც სიაშია, არც პირდაპირ იხსნება (**404**) და არც იქმნება (403). ⚠️ sensitive მოდულს
  **super_admin-იც ვერ იღებს ავტომატურად** — მასაც აშკარად ერთვება (`User::hasModule`).
- [x] age/consent gate: `module_user.settings.consent_at` (`PUT /api/modules/{key}/settings`);
  UI-ში 18+ ჩანაწერები ცალკე ღილაკით ირთვება და პირველად დადასტურებას ითხოვს.
- [x] private thumbnail: 18+ ჩანაწერის ატვირთული სურათი **private დისკზეა**
  (`storage/app/private`) და მხოლოდ `GET /api/videos/{id}/thumb`-იდან გადის —
  `/storage/*` ავტორიზაციას არ ამოწმებს.
- [x] embed-ის უსაფრთხოება: **თვითნებური HTML არასდროს ინახება**; iframe-ის src მხოლოდ
  allowlist-იდან იგება (backend) და ხელახლა მოწმდება ფრონტზეც (`VideoEmbed`), `sandbox`-ით.
  უცნობი წყარო — უბრალოდ გარე ბმული.
- [x] გადამოწმდა: `VideoModuleTest` (6 ტესტი, 35 assertion) + ხელით API-ზე.

### I6. სამომავლო მოდულების ჩამონათვალი (backlog)
- [ ] ანიმეები (TMDB/AniList enrichment) — მედია-დომენი, ე.ი. `MEDIA`-ში ერთი ჩანაწერი
  + morph alias + კონტროლერი; generic გვერდები (Library/Movie/Form) ისედაც მუშაობს.
- [ ] თამაშები (მაგ. IGDB/RAWG enrichment).
- [ ] საიტების/რესურსების ლინკები (bookmarks-ტიპის მოდული).
- [x] ~~ვიდეოები~~ — გაკეთდა (I5).
- [ ] თითო ახალ მოდულზე — ცალკე ქვე-ეპიკი, I2-ის recipe-ს მიხედვით
  (`docs/I7-multi-tenancy-and-modules.md` §7.3). არა-მედია მოდულს ფრონტზე ორი ადგილი სჭირდება:
  `PAGE_MODULE_KEYS` (`lib/modules.tsx`) და `MODULE_PAGES` (`App.tsx`).

### I7. დაგეგმვა (deliverable) — ჯერ ეს
- [x] **დიზაინ-დოკუმენტი მზადაა: `docs/I7-multi-tenancy-and-modules.md`** — ER მოდელი
  (users, modules, module_user, owner-scoping, გლობალური vs per-user), auth მიდგომა
  (Sanctum SPA cookie), 5-ეტაპიანი მიგრაციის გეგმა rollback-ით და backend/frontend
  ცვლილებების სია. კოდი არ შეხებია.
- [x] **მთავარი წინადადება:** მფლობელობა **per-user რიგებით** (`movies.user_id`), არა
  გლობალური კატალოგი + per-user pivot — ასე `genreables`/`castables`, `GenreItemController`
  და ჟანრების მთვლელები **უცვლელი რჩება** (მფლობელობა მშობელი ჩანაწერიდან მოდის),
  და Tasks-ის მოთხოვნა („ერთი TMDB ფილმი რამდენიმე user-ს, თითოს თავისი სტატუსი/ლინკები")
  მაინც სრულდება. სამივე ვარიანტის შედარება დოკუმენტის §3-შია.
- [x] გაზომილი ფაქტები დოკუმენტში: Laravel **13.17** (CLAUDE.md-ში „11" წერია — გასასწორებელი),
  Sanctum **არ არის დაინსტალირებული**, `users` **0 რიგი**, გადასატანი მონაცემი —
  258 ფილმი / 22 სერიალი / 36 ჟანრი / 2359 მსახიობი / 3170 castable.
- [x] დაფიქსირებული ხაფანგები: `movies.imdb_id`/`series.imdb_id` **unique** → per-user
  რიგებზე ორი user ერთსა და იმავე ფილმს ვერ დაამატებს (საჭიროა `unique(user_id, imdb_id)`);
  `/storage/*` ავტორიზაციის გარეშე იხსნება (I5-ის sensitive მოდულს private disk სჭირდება);
  `media:redownload` CLI-ს `Auth::id()` არ აქვს → global scope გამორთულია (`--user=`).
- [x] **7 კითხვაზე პასუხი მიღებულია** (დოკუმენტის §12): (1) ვარიანტი A, (2) ქუქები,
  (3) ღია რეგისტრაცია + მოდულებზე მოთხოვნა ადმინთან, (4) ჟანრები გლობალური,
  წაშლა ადმინის დადასტურებით, (5) არსებული ჩანაწერები super_admin-ს, (6) login მისაღებია,
  (7) ეტაპები მიყოლებით.
- [x] პასუხების გამო გეგმას დაემატა ერთი ცხრილი — **`approval_requests`** (ერთი მექანიზმი
  ორივე ნაკადზე: მოდულის ჩართვა + ჟანრის წაშლა). დეტალები დოკუმენტის §12.1-ში.
- [x] ეპიკი შესრულდა ეტაპობრივად (იხ. I1–I4).
- [ ] ⚠️ **იმპლემენტაცია არ დაიწყო** სანამ დიზაინ-დოკუმენტი და მიგრაციის გეგმა
  user-თან არ შეთანხმდება.

---

## J. მასობრივი სინქრონიზაცია — მედია + აღწერები (მართვადი, გაჩერებადი)

> **მიზეზი:** ამჟამად „მედიის ჩამოტვირთვა" ერთი დიდი ღილაკია ბიბლიოთეკის თავში (`LibraryPage.tsx:155`)
> და ბევრ ადგილს იკავებს — ჰედერის დამატებისას (F3) სივრცე აღარ დარჩება. თვითონ ოპერაციაც
> „ყველაფერი ან არაფერია": `POST /api/media/redownload` **ერთ სინქრონულ რექვესთში** ამუშავებს
> ყველა ჩანაწერს (248 ფილმი + 16 სერიალი ≈ 26 წუთი), პროგრესი არ ჩანს და გაჩერება შეუძლებელია.
>
> **⚠️ კრიტიკული ტექნიკური ფაქტი:** `php artisan serve` PHP-ის ჩაშენებული dev-სერვერია და
> **ერთდროულად ერთ რექვესთს ემსახურება**. სანამ ეს 26-წუთიანი ციკლი გადის, მთელი აპლიკაცია
> იბლოკება — `/api/movies`-იც და `/storage/*.jpg`-იც რიგში დგება (რეალურად დაფიქსირდა 2026-08-27).
> ამას ემატება `set_time_limit(0)` + `ignore_user_abort(true)` `MediaController`-ში — ტაბის
> დახურვაც კი არ აჩერებს პროცესს. ამიტომ ქვემოთ აღწერილი per-item მიდგომა **მხოლოდ UX არ არის —
> ის ამ ბლოკირების პრობლემასაც ხსნის.**
>
> **✅ გადაწყდა (J1–J5 შესრულებულია).** `MediaController` წაშლილია; UI-დან სათითაო
> სინქრონი მუშაობს (`/api/media/sync/plan` + `/api/media/sync/{type}/{id}`), ციკლს
> queue ატარებს პროგრესითა და გაჩერებით. CLI-ვარიანტი: `php artisan media:redownload`.

### J1. ღილაკების გატანა ბიბლიოთეკის თავიდან
- [x] **შეთანხმებული: ვარიანტი (a)** — პარამეტრების გვერდი (E1), სექცია
  „მონაცემების სინქრონიზაცია".
- [x] „მედიის ჩამოტვირთვა" მოეხსნა `LibraryPage`-ის ზედა პანელს (mutation/confirm-იანად)
  და გადავიდა `SettingsPage`-ზე; იქვე წერია გაფრთხილება, რომ ერთ გრძელ რექვესთად მიდის
  და API-ს ბლოკავს.
- [x] ღილაკი აღარ არის ერთჯერადი „გაუშვი ყველაფერზე" — ხსნის J2-ის დიალოგს (`SyncDialog`).

### J2. სკოუპის არჩევა გაშვებამდე
- [x] `POST /api/media/sync/plan` (`MediaSyncController::plan`) — ფილტრები → რიგი:
  `types`, `status`, `favorite`, `genres[]`, `ids{movie,series}`, `missing_media_only`.
  აბრუნებს `items`, `count`, `eta_seconds` და `skipped_without_tmdb`-ს.
- [x] `components/SyncDialog.tsx`: დომენების checkbox-ები + სკოუპის radio
  (ყველაფერი / სტატუსი / რჩეულები / ჟანრები / კონკრეტული ჩანაწერები).
- [x] გაშვებამდე ჩანს ჩანაწერების რაოდენობა და ETA (10.5 ჩანაწერი/წთ).
  სანიტი: სრულ ბიბლიოთეკაზე `count=274`, `eta≈26.1` წთ — ზუსტად ემთხვევა 2026-08-27-ის გაზომვას.
- [x] tmdb_id-ის გარეშე ჩანაწერები ჩუმად არ იკარგება — დიალოგში ეწერება რამდენი გამოტოვდა (J5).

### J3. სათითაო შესრულება, ხილული პროგრესი და გაჩერება
- [x] `POST /api/media/sync/{type}/{id}` — ერთი მოკლე რექვესთი თითო ჩანაწერზე;
  per-item შეცდომა 200-ით ბრუნდება (`ok:false` + `error`), რომ ციკლი არ გაწყდეს.
- [x] ციკლს **queue** ატარებს: `components/ui/queue.tsx` გაფართოვდა ორი სახის
  სამუშაოზე (`add` | `sync`) — ახალი UI არ შეიქმნა, იმავე რიგში ჩაჯდა.
- [x] პროგრესი: მიმდინარე ჩანაწერის სახელი, `N/M`, ჩავარდნილების რიცხვი და
  **დარჩენილი დრო** (დასრულებულების საშუალო × დარჩენილი + პაუზა).
- [x] გაჩერება: (1) რიგში მდგომის გამოტოვება, (2) „დანარჩენის გაჩერება",
  (3) მიმდინარე რექვესთი `AbortController`-ით (გაუქმებული „გაუქმდა"-დ მოინიშნება).
- [x] შეჯამება: `done/total` + ჩავარდნილების სია მიზეზებით და **„ხელახლა ცდა (N)"**
  ღილაკი, რომელიც მხოლოდ ჩავარდნილებს აბრუნებს რიგში.
- [x] გამოტოვებული ჩანაწერი ცალკე აიქონით (`SkipForward`) აღინიშნება.

### J4. იგივე ფუნქციონალი აღწერებზე/მეტამონაცემებზე
- [x] ახალი სერვისი `App\Services\Sync\ItemSyncer` — Enricher-ებისგან განსხვავებით
  **ირჩევა**, რომელი ველები განახლდეს და რა რეჟიმში. Enricher-ები („ყველაფერი,
  მხოლოდ ცარიელი") დამატებისა და ერთეულოვანი `resync`-ისთვის დარჩა უცვლელი.
- [x] ველები checkbox-ებით: `title`, `description`, `year`, `rating`, `genres`, `cast`,
  `details` (ხანგრძლივობა / IMDb / კოლექცია / სეზონები-ეპიზოდები).
- [x] რეჟიმი: **მხოლოდ ცარიელი ველები** (default) და **გადაწერა** (წითელი გაფრთხილებით).
- [x] ბონუსი: `title_ka`/`description_ka` ახლა TMDB-ის `language=ka` პასუხიდან მოდის
  (`Lang::georgian()` ფილტრით) — B7-ის იმავე მიდგომით. გადამოწმდა: The Matrix → „მატრიცა".
- [ ] `POST /movies/{id}/resync`-ს პარამეტრები **არ** დაემატა — ცალკე endpoint უფრო
  სუფთა გამოდგა (resync ერთი ჩანაწერის ღილაკია, sync — მასობრივი ციკლის ერთი ნაბიჯი).

### J5. ეფექტურობა და უსაფრთხოება
- [x] **„მხოლოდ დაკარგული ფაილები"** — თუ პოსტერი და ყველა მსახიობის ფოტო ადგილზეა,
  ჩანაწერი TMDB-ს შეხების გარეშე გამოტოვდება (**66ms** vs ~5.7s).
  გაზომილი ეფექტი: 274 ჩანაწერიდან რეალურად 62-ს (**23%**) აკლია მედია.
  ხელით ატვირთული პოსტერი ამ რეჟიმში არ გადაიწერება.
- [x] TMDB rate-limit: პაუზა ჩანაწერებს შორის — პარამეტრი `syncDelayMs`
  (0/200/500/1000/2000, default 200) პარამეტრების გვერდზე.
- [x] ჩავარდნის მიზეზი ორივე მხარეს: `Log::warning('sync failed', …)` + პასუხის
  `error`, რომელიც queue-ს სიაში ჩანაწერის ქვეშ ჩანს.
- [x] ძველი blocking `POST /api/media/redownload` **წაიშალა** (`MediaController`-იანად)
  და გახდა artisan ბრძანება: `php artisan media:redownload [--type=] [--missing]`
  (პროგრესბარი + per-item შეცდომები). ბრაუზერიდან აღარ გამოიძახება.

### J6. სასურველია (თუ per-item საკმარისი არ აღმოჩნდა)
- [ ] queue job + worker (`php artisan queue:work`) — **ჯერ არ სჭირდება.** per-item
  მიდგომამ ბლოკირება მოხსნა: ყველაზე გრძელი რექვესთი ახლა ~1.3–6 წმ-ია (და
  „მხოლოდ დაკარგულზე" 66ms), ე.ი. `/api/movies` მაქსიმუმ ერთ ჩანაწერს ელოდება
  26 წუთის ნაცვლად. თუ მაინც დაგჭირდა — **მხოლოდ შეთანხმებით.**

**გადამოწმდა curl-ით:** plan-ის ხუთი სკოუპი (ყველა 274 / სერიალები+watched 0 /
drama 81 / დაკარგული მედია 62 / კონკრეტული id-ები 2); per-item — skip 66ms,
`title_ka` შევსება, overwrite-ით `runtime`; 3-ჩანაწერიანი სრული ციკლი
(ok=3, საშუალო 1.3წმ); ვალიდაცია — არასწორი type 422, უცნობი id 404, უცნობი ველი 422.

---

## K. ახალი მოთხოვნები (2026-08-28)

> ეს სექცია user-ის ბოლო შეკვეთაა. **ყოველი პუნქტის დაწყებამდე Claude ეკითხება user-ს,
> რომელი გააკეთოს შემდეგ.** მიმდევრობა თავისთავად პრიორიტეტი არ არის.

### K1. ჟანრის რედაქტირების მოდალი — ტაბები + განმარტებები
- [x] ახალი `components/ui/tabs.tsx`: `Tabs` (badge-ებით, `role=tablist`/`aria-selected`) და
  `TabInfo` (განმარტების ზოლი). Radix-ის tabs პაკეტი პროექტში არ არის და არც დაგვჭირდა.
- [x] რედაქტირების მოდალი დაიყო ოთხ ტაბად: **სახელები · ჩანაწერები · დამატება · ქმედებები**.
- [x] თითო ტაბს ზემოთ აქვს ინფო-ტექსტი (ka/en), მ.შ. სამივე ქმედების ზუსტი განმარტება
  („ჩახსნა მოხსნის მხოლოდ ამ ჟანრს · გადატანა ჩაანაცვლებს სამიზნით · ჩანაცვლება წაშლის
  **ყველა** ჟანრს").
- [x] `GenreItemsManager` ყოველთვის დამონტაჟებულია (`section` prop-ით) — **მონიშვნა და
  არჩეული დომენი ტაბებს შორის გადართვისას არ იკარგება**.
- [x] ქმედებების ტაბზე თითო ღილაკს თავისი ერთ-სტრიქონიანი განმარტება უწერია გვერდით;
  ჩანაცვლება/ჩახსნა `destructiveOutline`-ია (K11).
- [x] ახალი ჟანრის შექმნა უცვლელი დარჩა (ტაბები არ სჭირდება — სახელი + წინასწარი მონიშვნა).

### K2. ვიდეო — ავტომატური მეტამონაცემი ბმულიდან
- [x] ახალი სერვისი `App\Services\Video\VideoMetadata` + `POST /api/videos/metadata`
  (ჩანაწერს არ ქმნის — ფორმა ამით ივსება ჩასმისთანავე).
- [x] `POST /api/videos`-ზე `title` **აღარ არის სავალდებულო**: ცარიელი ველები ბმულიდან ივსება
  (`autofill`, გამორთვადია `autofill=0`-ით). **ხელით შევსებულს არასდროს გადააწერს.**
- [x] ფრონტი: ბმულის ჩასმა/blur → მეტამონაცემი, thumbnail-ის გადახედვა, პლატფორმა/ავტორი/
  ხანგრძლივობა; ცარიელი ველები თავისით ივსება, სათაურის placeholder — „ბმულიდან შეივსება".
- [x] **გადამოწმებული რეალურ ბმულებზე:**
  - YouTube oEmbed → სათაური „Rick Astley — Never Gonna Give You Up…", არხი, thumbnail. ✔
  - Vimeo oEmbed → სათაური „Big Buck Bunny", **ხანგრძლივობა 597 წმ**, აღწერა, thumbnail. ✔
  - მხოლოდ ბმულით შექმნა → სათაური/ხანგრძლივობა/thumbnail თავისით; ხელით მითითებული
    სათაური უცვლელი დარჩა. ✔
- [x] `YOUTUBE_API_KEY` (`config/services.php` → `services.youtube.key`, `.env.example`-ში):
  დაყენებისთანავე YouTube-ზეც ჩაირთვება **ხანგრძლივობა + ტეგები** (Data API v3), კოდი მზადაა.
  UI-ში წერია, რომ ამ ორისთვის გასაღებია საჭირო.
- [x] აღწერაც ავტომატურად მოდის (Vimeo/YouTube-API), მაგრამ რედაქტირებადია.
- [ ] პირდაპირი ფაილის (`.mp4`) ხანგრძლივობა — ბრაუზერიდან (`<video>.duration`); ჯერ არ გაკეთებულა.

### K3. ვიდეო — მიმაგრებული ფაილები, ჩანიშვნები, დოკუმენტები
- [x] ორი **polymorphic** ცხრილი: `attachments` (`kind=image|doc`, `disk`, `path`,
  `original_name`, `mime`, `size`, `sort_order`) და `notes` (`body`) — ორივეს `user_id`-იც აქვს,
  ე.ი. `BelongsToUser`-ის scope პირდაპირ მუშაობს ფაილის route-ზეც.
- [x] ტრეიტი `HasAttachments` (`attachments`/`images`/`documents`/`notes`) — **მომავალ
  მოდულებზე ერთი `use`-ია** (მაგ. შემსრულებლის გალერეა, K5).
- [x] ვიდეოს დეტალური ხედი დაიყო ტაბებად: **ვიდეო · ფოტოები · ჩანიშვნები · დოკუმენტები**
  (იგივე `Tabs`/`TabInfo`, რაც K1-ში).
- [x] დოკუმენტების ტიპები allowlist-ითაა (`pdf,doc,docx,txt,rtf,odt,xls,xlsx,csv,ppt,pptx`,
  ≤20MB), ფოტოები — `image` ≤8MB.
- [x] **18+ ჩანაწერის ფაილები private დისკზეა** და `GET /api/attachments/{id}/file`-იდან გადის.
- [x] ⚠️ `morphs()` cascade-ს **არ ქმნის** — ვიდეოს წაშლაზე ფაილები და ჩანიშვნები ხელით იშლება
  (`Video::booted()` → `deleteAttachmentsAndNotes()`), ფაილიც დისკიდან.
  გადამოწმდა ტესტით და ხელით: წაშლის შემდეგ `attachments=0, notes=0, files_left=0`.
- [x] ბარათზე/სიაში ჩანს მთვლელები (`images_count`, `documents_count`, `notes_count`).

### K4. ვიდეო — ძებნა და შემოთავაზებები
- [ ] ძებნა **ყველაფერში**: სათაური, აღწერა, ტეგები, ჩანიშვნები, წყარო/პლატფორმა.
- [ ] „ახლო სიტყვებზე" ამოგდება (ბეჭდვის შეცდომა/ნაწილობრივი დამთხვევა) — MySQL fulltext
  natural-language + ხელით relevance, ან trigram/Levenshtein PHP-ში. საზომი: „კორირება" →
  „კორეა"-ს პოვნა.
- [ ] **„მსგავსი ვიდეოები"** ბლოკი ვიდეოს გვერდზე: სათაური, thumbnail, ხანგრძლივობა, წყარო,
  ბმული — და ბმულით გადასვლა ჩანაწერზე.
- [ ] ⚠️ **ფაქტი:** YouTube-ის „related videos" API **გაუქმებულია** (2023-ში `relatedToVideoId`
  ამოიღეს), ე.ი. გარე რეკომენდაცია აღარ არსებობს. შემოთავაზება აეწყობა **ჩემი ბიბლიოთეკიდან**
  (საერთო ტეგები + სათაურის მსგავსება + იგივე პლატფორმა/არხი).

### K5. 18+ — ცალკე ტაბი და პორნო-მსახიობები
- [ ] 18+ გახდეს **ცალკე ტაბი/სექცია** ვიდეოებში (და არა მხოლოდ ფილტრის ღილაკი), თავისი
  ფუნქციონალით.
- [ ] **შემსრულებლების (performers) დომენი** — ფილმების მსახიობების ანალოგიით:
  სია, ცალკე გვერდი, „ამ შემსრულებლის ვიდეოები" (`ActorPage`-ის მსგავსი).
- [ ] **რეიტინგის სისტემა** შემსრულებელზე (ჩემი შეფასება 1–10 + შენიშვნა).
- [ ] **ფოტო-გალერეა**: ხელით ატვირთვა **და** ბმულის ჩაგდებით ფოტოს წამოღება.
- [ ] „100+ ფოტოს ავტომატური წამოღება PornHub-ის ტიპის საიტიდან" — **ჯერ კვლევა (იხ. K6)**,
  არა პირდაპირ იმპლემენტაცია. მიზეზი ქვემოთ.

### K6. ⚠️ 18+ საიტების პარსინგი — ჯერ კვლევა/გეგმა
- [ ] გასაკეთებელი პირველი ნაბიჯი — **მოკლე დოკუმენტი** (G1-ის ანალოგიით): რომელი წყაროა
  რეალურად პარსებადი, რა სტრუქტურით, რა ლიმიტებით.
- [ ] რაც უკვე ვიცი და გასათვალისწინებელია:
  - მსხვილი tube-საიტების **ToS პირდაპირ კრძალავს ავტომატურ ჩამოტვირთვას/სკრეიპინგს**;
  - წინ დგას Cloudflare/ბოტ-დაცვა — მარტივი `Http::get()` ხშირად 403-ს დააბრუნებს;
  - ზოგს აქვს **ოფიციალური API/oEmbed** — თუ არსებობს, ჯერ ის უნდა ვცადოთ.
- [ ] ამიტომ **სამუშაო გეგმა ასეთია**: (1) ხელით ატვირთვა + (2) **ბმულიდან ერთი ფოტოს**
  წამოღება (რასაც user თვითონ მიუთითებს) — ეს უპრობლემოდ კეთდება და მაშინვე გამოსადეგია;
  (3) მასობრივი გალერეა — მხოლოდ იმ წყაროდან, სადაც ეს დაშვებულია/ტექნიკურად შესაძლებელია,
  და **user-თან შეთანხმებით**.
- [ ] ფაილები private დისკზე (როგორც 18+ thumbnail-ები), `/storage/*`-ზე არ გავიდეს.

### K7. ვიდეოების საიდბარის სექციები
- [x] ახალი ველი `videos.kind` (`media` | `info`, default `media`) + ინდექსი `(user_id, kind)`;
  ფილტრი `GET /api/videos?kind=`; ფორმაში ტიპის გადამრთველი.
- [x] საიდბარში ვიდეოები ჩამოიშლება ფილმების/სერიალების მსგავსად:
  **ყველა · მედია · ინფორმაციული · 18+ · რჩეული · დამატება**.
  სექცია მისამართში აისახება (`/videos?view=info`), „დამატება" → `?new=1` ხსნის ფორმას.
- [x] **18+ სექცია მხოლოდ `video_adult` მოდულით ჩანს** და ასაკის დადასტურებამდე
  ბადეს საერთოდ არ აჩვენებს (consent-ის ეკრანი) — ე.ი. პირდაპირი მისამართითაც დაცულია.
- [x] გადამოწმდა API-ზე: `kind=media` → YouTube-ის ჩანაწერი, `kind=info` → Vimeo-ს ჩანაწერი.

### K8. ბაგი — საიდბარის კატეგორია იკარგება ჩანაწერზე შესვლისას
- [x] მიზეზი დადასტურდა: `Sidebar` აქტიურ ხედს `?view=`-იდან კითხულობდა, დეტალურ გვერდზე
  კი ეს პარამეტრი აღარ არსებობს → მონიშვნა „ყველაზე" ხტებოდა.
- [x] გასწორდა: საიდბარი იმახსოვრებს **ბოლო ხედს თითო დომენზე** (`lastView`) და ჩანაწერის
  გვერდზე იმას ინარჩუნებს.
- [x] იმავე ცვლილებამ მეორე ხარვეზიც მოხსნა: აქტიური დომენი ახლა `domainFromRoute`-ით
  განისაზღვრება (და არა `mediaFromPath`-ით), ამიტომ `/genres`, `/settings`, `/admin`-ზე
  ფილმების სექცია აღარ ჩანს „აქტიურად".

### K9. ჟანრზე დაჭერა ჩანაწერის გვერდზე
- [x] `MoviePage`-ის ჟანრები გახდა ბმულები → `{libraryPath}?genre=<slug>`; `LibraryPage`
  ჟანრს `?genre=`-იდან იღებს (საწყისი მნიშვნელობაც და შემდგომი ცვლილებაც), ჩიპი მონიშნულია.

### K10. ჟანრების გვერდი — მთვლელის განლაგება
- [x] ორი მთვლელი ერთმანეთის ქვეშ, თითო თავის ჩარჩოში (`border`), ერთი სიგანით (`w-28`),
  1px-იანი დაშორებით. სერიალების მთვლელი ახლა ყოველთვის ჩანს (0-ზეც) — სვეტები რომ
  ერთმანეთს დაემთხვეს.

### K11. ღილაკების სტილი
- [x] `Button`-ს დაემატა ვარიანტი **`destructiveOutline`** (მოწითალო კონტური) — ერთ ადგილას
  აღწერილი სტილი გამოიყენება: ბიბლიოთეკის „გასუფთავება", ვიდეოების „გასუფთავება",
  ჟანრის „ჩახსნა", ჩანაწერის „წაშლა".
- [x] `size="sm"`-ს padding გაიზარდა (`px-3` → `px-3.5`).

### K12. ჰედერი და პროფილის მენიუ (F3-ის კონკრეტიზაცია)
- [x] ახალი `components/Header.tsx` — sticky ზოლი (h-14): მარცხნივ ლოგო (მობილურზე +
  ჰამბურგერი), მარჯვნივ ენა, თემა და **მომხმარებლის მენიუ** (ავატარი + სახელი + ჩევრონი).
- [x] ჩამოსაშლელი მენიუ (Radix Popover): **პროფილი · პარამეტრები · გასვლა** თავიანთი
  აიქონებით; ზემოთ სახელი/ელფოსტა. მობილურზე ენის არჩევანიც აქვეა (ზოლში არ ეტევა).
- [x] საიდბარიდან მოიხსნა: მობილურის ზედა ზოლი, ლოგო და პროფილის/ენის/თემის ბლოკი —
  **დარჩა მხოლოდ ნავიგაცია**. უჯრის მდგომარეობა `AppShell`-შია (ჰედერიც მართავს).
- [x] განლაგება: `Header` ზემოთ, ქვემოთ `Sidebar + main`; საიდბარი `sticky top-14`
  სიმაღლით `calc(100vh-3.5rem)`.

### K13. მოდულების გვერდი — თვითონ გამორთვა
- [x] `module_user.is_hidden` — გამორთვა **უფლებას არ ხსნის** (detach-ს არ ვაკეთებთ),
  ამიტომ ხელახლა ჩართვისას ადმინის დადასტურება აღარ სჭირდება.
- [x] `PATCH /api/modules/{key}` `{enabled}` — მხოლოდ უკვე მინიჭებულ მოდულზე (თორემ 403);
  `/api/modules` აბრუნებს `enabled` + **`granted`**-ს, რომ UI-მ გაარჩიოს
  „ადმინს ვთხოვე" და „მე თვითონ გამოვრთე".
- [x] `/modules`-ზე ჩართულ მოდულს Switch აქვს; გამორთვისას ტოსტი წერს, რომ მონაცემები რჩება.
- [x] გამორთული მოდულის API-ც იკეტება (403) და ნავიგაციიდანაც ქრება.
- [x] ტესტები: `test_user_can_disable_and_reenable_own_module`,
  `test_user_cannot_enable_module_without_grant`.
- [x] ⚠️ **გზაში დაჭერილი ბაგი:** სვეტს თავიდან `hidden` ერქვა და `$model->pivot->hidden`
  მოდელის **შიგნიდან** Eloquent-ის protected `$hidden` თვისებას (სერიალიზაციის სიას)
  კითხულობდა, არა DB-ის სვეტს — გამორთვა ჩუმად არ მუშაობდა. სვეტი გადაერქვა `is_hidden`-ად.

### K14. ადმინის პანელი — გაფართოება
- [x] სათაური/აღწერა: „ადმინი" → **„მართვის პანელი"** + ახალი აღწერა.
- [x] **მომხმარებლის შიდა გვერდი** `/admin/users/:id` (`AdminUserPage` + `GET /api/admin/users/{user}`):
  ვინაობა/ავატარი, უფლებები (როლი, აქტიურობა), მოდულების ჩართვა-გამორთვა განმარტებებით,
  მოთხოვნების ისტორია და ცალკე **„საშიში ზონა"** წაშლისთვის (სიიდან წაშლა მოიხსნა).
- [x] შიგთავსის სტატისტიკა: ფილმები · სერიალები · ვიდეოები (მ.შ. რამდენი 18+) · რჩეულები,
  **ბოლო აქტივობა** (`sessions.last_activity`-დან) და **დაკავებული ადგილი**.
- [x] ⚠️ „ადგილი" მხოლოდ **ამ user-ის ატვირთულ** ფაილებს ითვლის (ავატარი, ხელით ატვირთული
  პოსტერები, ვიდეოს thumbnail-ები). TMDB-დან ჩამოტვირთული პოსტერები `posters/<slug>.jpg`-ია,
  ე.ი. ანგარიშებს შორის გაზიარებული — კონკრეტულ user-ს ვერ მიეწერება.
- [x] მოდულების ტაბში მთვლელის ნაცვლად **ვის აქვს ჩართული** — სახელების ჩიპები ბმულით
  შიდა გვერდზე; **სუპერ-ადმინიც ითვლება** (მისი ავტომატური წვდომა ცალკე აღინიშნება).
- [x] მოიხსნა UI-დან დეველოპერული შენიშვნა „ახალი მოდული კოდსაც მოითხოვს…" (ისევ რჩება
  `CLAUDE.md`-სა და `docs/I7-*.md` §7.3-ში).
- [x] მომხმარებლის წაშლისას ვიდეოს thumbnail-ებიც იშლება დისკიდან (რიგებს cascade ხსნიდა,
  ფაილები რჩებოდა).

---

## ⚠️ შესათანხმებელი (იმპლემენტაცია არ დაიწყო შეთანხმებამდე)

- [ ] **კატეგორიების სტილი** — ბევრი კატეგორიაა და 5-6 ხაზზეა გაშლილი. Claude-მ
  მოიფიქროს ვარიანტ(ებ)ი (მაგ. scrollable chips, collapsible, „მეტის ჩვენება“,
  dropdown-ფილტრი), **მაგრამ ჯერ user-თან შეათანხმოს — არ გაწეროს კოდი.**
- [ ] **დამატებითი პარამეტრიზაცია** (იხ. E4). ბაზა უკვე მზადაა (`lib/settings.tsx`) —
  ახალი პარამეტრი = ერთი ველი `Settings`-ში + ერთი რიგი `SettingsPage`-ზე.
- [x] ~~**ავტორიზაციის მოცულობა/მიდგომა** (იხ. F4)~~ — **შეთანხმდა: Sanctum SPA cookie.** შესრულებულია.
- [x] ~~**მრავალმომხმარებლიანობა + მოდულური სისტema + სუპერ-ადმინი** (სექცია I)~~ —
  დიზაინი დამტკიცდა და **შესრულდა** (`docs/I7-multi-tenancy-and-modules.md`).
- [x] ~~სად გავიტანოთ სინქრონიზაციის ღილაკები (J1)~~ — **შეთანხმდა: პარამეტრების გვერდი.**

---

## შენიშვნები
- მონაცემები: **XAMPP MySQL** (`C:\xampp\mysql\data\mediary`). WAMP არ ჩართო. MySQL გამორთე Control Panel-ის „Stop“-ით (force-kill → სისტემური ცხრილების კორუფცია).
- SQL ბექაპი: `backend/mediary_backup.sql`. არქიტექტურა/ბრძანებები: `CLAUDE.md`.
- დასრულებული: სერიალების დომენი (ცალკე ფილმებისგან, polymorphic genre/cast, TMDB TV enrichment, `type`-parameterized API + frontend). დეტალები — git history / `CLAUDE.md`.
