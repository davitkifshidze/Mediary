# Tasks

ამ ფაილში მხოლოდ **ღია** სამუშაოა. დასრულებული პუნქტი იშლება, დასამახსოვრებელი
წესი კი `CLAUDE.md`-ში გადადის.

> პარტია ჩაიწერა **2026-09-16**-ს. ყოველი სექცია რეკონზე დგას — ფაილები, ხაზები
> და რიცხვები კოდშია გადამოწმებული (ნაწილი ცოცხალ ბაზაზეც), და არა `CLAUDE.md`-დან
> ნავარაუდევი. დეტალური ანგარიშები დროებით `.recon/*.md`-შია (git-ს გარეთ);
> დავალებების დამტკიცების შემდეგ წაიშლება.
>
> ⚠️ `CLAUDE.md`-ში ციტირებული ნომრები (`§16.3`, `§25.5`…) **დასრულებული**
> სამუშაოს მისამართებია და ამ ფაილის ნუმერაციასთან კავშირი არ აქვს.

---

## 0. სანამ დაიწყება

- [ ] **0.1** სამუშაო ხე დაუკომიტებელია — **71 ფაილი** (ალბომები, პაროლით ჩაკეტვა,
      „სტატუსი და ტიპი სავალდებულოა"). ახალი პარტიის დაწყებამდე დაიკომიტოს, თორემ
      ორი პარტიის დიფი ერთმანეთში აირევა და ვერცერთს ვერ გადახედავ ცალკე.

---

## რიგითობა — რატომ ასე

შენ თვითონ თქვი: „ბევრი რამე არის გლობალური ცვლილება ვიზუალური". სწორედ ეს
განსაზღვრავს რიგს — **ჯერ ის სამი, რაც ერთხელ კეთდება და მერე ყველა სხვა თასქი
პატარავდება**, შემდეგ ცალკეული გვერდები, ბოლოს ორი დიდი ფუნქცია.

| # | სექცია | რატომ აქ | მოცულობა |
|---|---|---|---|
| §1 | აიქონები და ფერები | ყველაზე პატარა და ყველაზე თვალსაჩინო; **ცოცხალი ხარვეზია** — ქვე-სექციების ნახევარი ერთსა და იმავე ნაცრისფერ ბადეს ხატავს. აქვე დგება აიქონების ლექსიკონი, რომელსაც §2 ეყრდნობა | S–M |
| §2 | ქარდ-სტილის ტაბები ყველგან | §1-ის ლექსიკონზე დგას; ერთი კომპონენტი + ერთი რეესტრი და მერე 9 ჭრილი პატარა ცვლილებაა | M–L |
| §3 | ინფო-აიქონები ტექსტების ნაცვლად | დამოუკიდებელია, მაგრამ ისევ გლობალური ვიზუალური გავლაა — სამივე ერთად რომ გაკეთდეს, ინტერფეისი ერთ ენაზე დაიწყებს ლაპარაკს | M–L |
| §4 | ველების კონსტრუქტორი — ჩაკეტვის მოხსნა | **ორი ცოცხალი ხარვეზი აქვს**, რომელთაგან ერთი სწორედ იმას აკეთებს, რისგანაც ჩაკეტვა გვიცავს: შენახვა ჩუმად არაფერს აკეთებს | M |
| §5 | მოთხოვნები — სორტირება, ძებნა, გვერდის ზომა | დამოუკიდებელი, პატარა, `DataTable` უკვე მზადაა | M |
| §6 | წიგნი — ენა და ჩანიშვნების მოდალი | დამოუკიდებელი; მოდალის ნაწილი ხუთივე მოდულზე გავრცელდება | M–L |
| §7 | პროფილი, საჯარო გალერეა და ჩაკეტილი ალბომები | §2-ის ბარათებს იყენებს; **სერვერზე ასვლის წინაპირობაა** და პირად დისკზე გადატანას მოითხოვს | L |
| §8 | სერვერის დრო → საქართველო | გადაწყვეტილია; მონაცემებს ეხება, ამიტომ სრული ასლის შემდეგ | M |
| §9 | მასობრივი ცვლილება — ტეგების სკოუპები | დამოუკიდებელი; ლექსიკა უკვე არსებობს `/purge`-ში, ე.ი. გამოსაგონებელი არაფერია | M–L |
| §10 | ჩატი — Messenger-ის დონე | 12 მოთხოვნიდან 7 საერთოდ არ არსებობს; ეტაპებად | XL |
| §11 | ბაზის ასლი — ვიუერი და ნაწილობრივი აღდგენა | ყველაზე სახიფათო (FK-ები, კასკადები); ეტაპებად | XL |

⚡ = ამ სექციაში ცოცხალი ხარვეზია და მისი გასწორება ფუნქციაზე ადრე უნდა მოხდეს.

---

## §1. აიქონები და ფერები — საიდმენიუ და მისი ქვე-სექციები ⚡

> შენი სიტყვები: „ასევე ფილმები, სერიალები და საიდ მენიუს ყველა ქვე მენიუს და
> თავად საიდმენიუსაც გადაუარე და თუ სადმე შეუსაბამო აიქონი ან ფერია გაასწორე;
> ქვე მენიუს ბევრს შეუსაბამო აიქონები აქვს და შესაბამისი აიქონები და ფერები მიეცი".

**მიზეზი ნაპოვნია და ის ერთია.** `frontend/src/components/ModuleIcon.tsx`-ის `ICONS`
რუკაში **21 აიქონია**, უცნობი სახელი კი ჩუმად `LayoutGrid`-ად იხატება
(`ModuleIcon.tsx:56` — `(name && ICONS[name]) || LayoutGrid`). საიდმენიუს **ყველა**
ქვე-სექცია ამ ფუნქციას გადის (`Sidebar.tsx:394, 460, 533, 593…`).

ცოცხალ ბაზაზე გაზომილი შედეგი:

| სად | აიქონი ბაზაში | რა იხატება |
|---|---|---|
| მოდული `note` | `NotebookPen` | **`LayoutGrid`** — ჩანაწერების მოდულს საკუთარი აიქონი არ აქვს |
| სტატუსი `undecided` (movie · series · anime · video) | `HelpCircle` | **`LayoutGrid`** |
| სტატუსი `watching` (ოთხივე) | `Eye` | **`LayoutGrid`** |
| სტატუსი `watched` (ოთხივე) | `CheckCircle2` | **`LayoutGrid`** |

ანუ ფილმებში, სერიალებში, ანიმეში და ვიდეოში **ოთხი სტატუსიდან სამი ერთი და იმავე
ნაცრისფერ ბადეს ხატავს** — ზუსტად ის, რასაც „შეუსაბამო აიქონები" ნიშნავს. სულ **15
სტატუსის რიგი + 1 მოდული**.

დამატებით, ის აიქონებიც შეუსაბამოა, რომლებიც **წარმატებით** იხსნება:

- `note.archived` და `bookmark.archived` → **`Wrench`** (ქანჩის გასაღები „არქივზე")
- `note.done` → `ListChecks`, `bookmark.read` → `ListChecks` — და `ListChecks`
  ამავე დროს **მასობრივი სტატუსის სექციის** აიქონია საიდმენიუში
- `to_watch` · `open` · `to_read` → **`Bookmark`** — და `Bookmark` ამავე დროს
  **ბუკმარკის მოდულის** აიქონია, ე.ი. ორი სხვადასხვა რამ ერთნაირად გამოიყურება
- `/people` და `/users` — **ორივე `Users`** (`lib/toolSections.ts:68` და `:72`)

- [ ] **1.1** ⚡ `ModuleIcon`-ის `ICONS` რუკა შეივსოს — მინიმუმ ის სახელები, რაც
      სიდერსა და `StatusDomain`-ში უკვე წერია (`NotebookPen`, `HelpCircle`, `Eye`,
      `CheckCircle2`/`CircleCheckBig`, `Circle`, `Star`, `Archive`, `CheckCheck`).
      ⚠️ სრული დინამიური იმპორტი განზრახ არ გვინდა (ფაილის თავზე ეს წერია — bundle),
      ამიტომ სია ხელით ფართოვდება.
- [ ] **1.2** ტესტი, რომელიც ამ ხარვეზს აღარ გაუშვებს: **ყველა სახელი, რასაც
      `ModulesSeeder` და `StatusDomain::defaults()` წერს, `ICONS`-ში უნდა იყოს.**
      დღეს ეს ჩუმად ტყდება — არც `tsc`, არც lint, არც ტესტი ვერ ხედავს.
- [ ] **1.3** სტატუსების ნაგულისხმევი აიქონები გასწორდეს `StatusDomain`-ში:
      `archived` → `Archive`, `done`/`read` → `CheckCheck` ან `BookOpenCheck`,
      `to_read`/`open` → `Inbox`/`Clock` (რომ `Bookmark` ბუკმარკის მოდულს დარჩეს).
      ⚠️ **მიგრაცია მხოლოდ იმ რიგებს შეეხოს, რომლებსაც ჯერ კიდევ ძველი
      ნაგულისხმევი აქვთ** — ხელით არჩეული აიქონი არ გადაიწეროს (`COLORS`-ის წესი
      სიდერში: მხოლოდ `null`-ზე ივსება).
- [ ] **1.4** `lib/toolSections.ts` — გამეორებული აიქონები გაიმიჯნოს
      (`/people` ↔ `/users`: `ContactRound` vs `Users`, ან `UserCog` ადმინზე).
- [ ] **1.5** სრული გავლა რიგ-რიგობით: მოდულის 11 რიგი, თითოეულის ქვე-სექციები
      („ყველა" · სტატუსები · „რჩეული" · ვიდეოს „ჩამოტვირთული" · „დამატება"),
      გალერეის 7 ჭრილი, ინსტრუმენტების 15 რიგი. ⚠️ **ყოველი შემოთავაზებული lucide
      სახელი გადამოწმდეს `node_modules/lucide-react`-ში** — React-ის ექსპორტის
      სახელი და გამოსახატი კლასის id სხვადასხვაა და შეცდომა ჩუმია.
- [ ] **1.6** `statuses.icon`-ის ამრჩევი დიალოგი (`StatusDialog`) იმავე `ICON_NAMES`
      სიას ხატავს — გაფართოებული სია ავტომატურად აქაც გამოჩნდება; შემოწმდეს, რომ
      ამრჩევში დამატებული აიქონები აზრიან ჯგუფებად დგას და არა ნაყარად.

❓ **გადასაწყვეტია:** აიქონების ამრჩევს გვინდა **მთელი lucide** (≈1500, დინამიური
იმპორტით, bundle-ის ფასად) თუ გაფართოებული ხელით შედგენილი სია (≈60–80)?
დღეს 21-ია და ეს აშკარად ცოტაა.

---

## §2. ქარდ-სტილის ტაბები — ყველგან, სადაც ჭრილია

> შენი სიტყვები: „ჩანაწერების გვერდზე ეს ყველა, აქტიური, შეჩერებული, დასრულებული
> ქარდის მსგავს ტაბებად გადააკეთე შესაბამისი ფერებით და აიქონებით"; „ყველგან სადაც
> არის ტაბების მსგავსი რამ ან გადამრთველი, იყოს ქარდის სტილის ტაბი შესაბამისი
> ფერით და აიქონებით".

ბარათი უკვე არსებობს — `components/ui/scope-card.tsx` (`ScopeCard` + `ScopeGroup`),
ხუთი მომხმარებლით: `/audit`, `/requests`, `/status`, `/purge` და გალერეა. ე.ი.
საკითხი ახალი კომპონენტი კი არაა, არამედ **ორი პატარა დამატება + ერთი რეესტრი +
9 გადაყვანა**.

მთელი ინტერფეისის ინვენტარიზაცია გაკეთდა: **9 ნამდვილი ჭრილია** (ერთი სიის
ურთიერთგამომრიცხავი ჭრილები), **2 სასაზღვრო** და **17 ჯგუფი, რომელიც არ უნდა
შეიცვალოს**.

- [ ] **2.1** `ScopeCard`-ს დაემატოს `size?: 'sm' | 'md'` და `ScopeGroup`-ს
      `layout?: 'grid' | 'inline'`. ორივე საჭიროა: ჭრილების ნაწილი **ბარათის
      შიგნით ან ხელსაწყოების ზოლში** ზის (`VisibilityManager`, `StorageLibrary`),
      სადაც დღევანდელი გვერდის დონის ბარათი ზედმეტად მძიმეა; ხოლო ფიქსირებული
      `grid-cols-2 sm:3 lg:4 xl:6` სამვარიანტიან ჭრილს ექვს სვეტზე ჭიმავს.
- [ ] **2.2** **`frontend/src/lib/cutStyle.ts`** — ერთი რეესტრი „ჭრილი →
      {აიქონი, ფერი}", ზუსტად `lib/actionStyle.ts`-ის ფორმით. სამ ცნებას დღეს
      **საერთოდ არ აქვს** ფერი და აიქონი (შეხსენების მდგომარეობები, ხილვადობა,
      არა-მოდულური დომენები `cast`/`playlist`/`gallery`/`account`/`chat`/`backup`),
      ორს კი არასწორ ადგილას აქვს: `RequestsPage`-ის ლოკალური `STATUSES` და
      `GALLERY_SCOPE_STYLE` (`components/`-ში ზის). ორივე აქ გადმოვიდეს.
      ⚠️ ფერები **თემის ტოკენებია (`var(--…)`), არასდროს hex** — `modAccent()` მათ
      `color-mix()`-ში დებს, ამიტომ მუქ თემას მეორე პალიტრა არ სჭირდება.
      ⚠️ `statusTone()` **არ გამოდგება** — `lib/statusStyles.ts` Tailwind-ის კლასებს
      აბრუნებს და არა CSS-ცვლადებს.
- [ ] **2.3** `/notes/reminders` — შენი დასახელებული შემთხვევა: ყველა · აქტიური ·
      შეჩერებული · დასრულებული ბარათებად. რიცხვები უკვე ითვლება
      (`NoteRemindersPage.tsx:83-87`), i18n გასაღებები ორივე ლოკალში დევს.
      ⚠️ აიქონები/ტონები `ReminderCard`-ს **უკვე აქვს** (`BellRing`/`BellOff`/
      `CheckCheck`, `STATE_TONE`) — რეესტრმა ის უნდა გადმოიტანოს და არა მეორე
      კომპლექტი გამოიგონოს, თორემ ერთსა და იმავე შეხსენებას ბარათზე ერთი ფერი
      ექნება და ტაბზე მეორე.
- [ ] **2.4** გალერეის ფოტოს კატეგორიის ჭრილი (`GalleryPanel.tsx:102-114`).
      ყველაზე იოლი — `GALLERY_SCOPE_STYLE`-ს **ზუსტად ეს გასაღებები უკვე აქვს**.
- [ ] **2.5** `/search` — დომენების ჭრილი. ⚠️ სამი დომენი მოდული არ არის
      (`cast`, `playlist`, `gallery`), ე.ი. ფერი რეესტრიდან უნდა მოვიდეს.
- [ ] **2.6** `/u/{username}` — საჯარო პროფილის დომენების ჭრილი.
- [ ] **2.7** `VisibilityManager` — დომენები **და** „ყველა/საჯარო/პირადი".
      §7-თან ერთად კეთდება.
- [ ] **2.8** `StorageLibrary` — მოდულის ჭრილი **და** ფაილის სახეობა (ორივე
      კომპაქტური ვარიანტით).
- [ ] **2.9** `GalleryDownloadDialog` — „ჩანაწერი ↔ მსახიობები" (დღეს `ui/tabs`).
      ორი სკოუპია და არა ერთის ორი ხედი; `RecordsCut:120` უკვე ხატავს
      ორვარიანტიან ჭრილს ორ ბარათად, ე.ი. ფორმა დამკვიდრებულია.
- [ ] **2.10** **„არ გადავიტანოთ" სია ჩაიწეროს `CLAUDE.md`-ში**, თორემ მომდევნო
      გავლაზე ისევ „გამოსასწორებლად" ჩაითვლება. კერძოდ: `LayoutToggle`
      (დაჯგუფებული ↔ არეული — მის დოკბლოკში **წერია, რომ ეს ჭრილი არ არის**),
      `SortPick`, სია↔ბადე, ფრანშიზის ჩამრთველი, ფილტრის მრავალარჩევანი ჩიპები,
      შეხსენების ფორმის ველები (31 დღის ბარათი აბსურდია), აიქონების ამრჩევები და
      ყველა `RadioGroup`, სადაც არჩევანი **თავის ქვე-ფორმას შლის**.

❓ **გადასაწყვეტია:** სასაზღვრო ორი — გალერეის „აქვს/არ აქვს ფოტო" ფილტრის ზოლში
და `SongDetail`/`VideoDetail`-ის ტაბები მოდალის შიგნით (ბარათები კონტენტს ეკრანს
ქვემოთ ჩააგდებდა). გადავიტანოთ თუ დავტოვოთ?

---

## §3. ინფო-აიქონები და ტულტიპები გრძელი ტექსტების ნაცვლად

> შენი სიტყვები: „ქვევით ამ ინფორმაციებს ჯობს გვერდზე აიქონი და ტულტიპები; თუ
> რამე კრიტიკულია, წითელი აიქონი იყოს, თუ რამე საინფორმაციო — ლურჯი აიქონი, i.
> შეიძლება ორივე იყოს ერთზე და ორივეზე სხვადასხვა დამოუკიდებელი ინფორმაცია
> ისახებოდეს"; და პასუხად: „**სავალდებულო ველებს ჰქონდეს წითელი სამკუთხედი, ხოლო
> დანარჩენებს ჩვეულებრივი `i` აიქონი**".

დასახელებული ოთხი ტექსტი: `roles.permissionsHint`, `roles.adminSectionsHint`,
`audit.subtitle`, `translate.pageHint`.

რეკონის სამი ფაქტი, რომელიც დიზაინს განსაზღვრავს:

1. ⚠️ **Radix-ის `Tooltip` შეხებაზე საერთოდ არ იხსნება** — გადამოწმებულია დაყენებულ
   წყაროში (`@radix-ui/react-tooltip`): `onPointerMove` `touch`-ზე ბრუნდება,
   `onPointerDown` ხურავს და დარაჯს აყენებს, `onClick` ხურავს. ტელეფონზე დაჭერა
   **არაფერს ხსნის**. ე.ი. ტექსტის ტულტიპში გადატანა ტელეფონზე ტექსტის დაკარგვაა.
   → კომპონენტი **`Popover`-ზე უნდა აშენდეს** (`@radix-ui/react-popover` უკვე
   დამოკიდებულებაა და ორ ადგილას გამოიყენება), hover + click ორივე ტრიგერით.
2. **ლურჯი უფასოა**: `index.css`-ში `.lucide-info { color: var(--icon-info) }`
   უკვე გლობალურია. **წითელს კი ცხადი `text-destructive` სჭირდება** —
   `.lucide-triangle-alert` ბაზურად **ქარვისფერია**, არა წითელი.
3. ⚠️ **`PageHeader`-ის subtitle `truncate`-ია**, ე.ი. `audit.subtitle` (81 სიმბოლო),
   `chat.subtitle` (67) და `purge.subtitle` (64) **დღესვე სამწერტილით იჭრება**.
   აიქონში გადატანა ინფორმაციას **აბრუნებს** და არა მალავს — ეს ამ ცვლილების
   ყველაზე ძლიერი არგუმენტია.

გაზომილი მასშტაბი: ლოკალში **270 „hint"-ტიპის გასაღებია**, აქედან **16-ს აქვს ⚠️**.

- [ ] **3.1** `components/ui/info-hint.tsx` — `InfoHint({ info, critical })`.
      ორივე მითითებისას **ორ მეზობელ ტრიგერს** ხატავს (ჯერ წითელი სამკუთხედი,
      მერე ლურჯი `i`), თითოს თავისი popover — ზუსტად „ორივეზე სხვადასხვა
      დამოუკიდებელი ინფორმაცია". ცარიელზე **არაფერს ხატავს**.
      ⚠️ სხვადასხვა **ფიგურა**, არა მარტო სხვა ფერი (ფერი დალტონიკს არაფერს ეუბნება
      — პროექტის საკუთარი წესი როლების სამუშაოდან).
      ⚠️ `onOpenAutoFocus` აუცილებლად უნდა შეჩერდეს — `GlobalSearch`-ში უკვე წერია,
      რომ `Popover` ფოკუსს იტაცებს და აკრეფას წყვეტს.
      ⚠️ პროპად **უკვე ნათარგმნი ტექსტი** მიდის და არასდროს გასაღები — თორემ i18n
      აუდიტის „გასაღებისმაგვარი ლიტერალი `t()`-ს გარეთ" შემოწმება ჩავარდება.
- [ ] **3.2** **სავალდებულო ველი = წითელი სამკუთხედი** (გადაწყვეტილია).
      `FieldHint` (`ui/field-label.tsx`) გახდეს ახლის თხელი გარსი და `required`-ზე
      `AlertTriangle`-ს ხატავდეს, დანარჩენზე — ლურჯ `Info`-ს.
      **179 გამოძახების ადგილი 10 ფაილში** მაშინვე იღებს შეხებასა და კლავიატურას
      (დღეს `tabIndex={-1}` აქვს, ე.ი. კლავიატურით მიუწვდომელია).
      ⚠️ lucide-ის კლასი იკონის id-ია და არა React-ის სახელი: `AlertTriangle` →
      `lucide-triangle-alert` (ის უკვე გასტილულია `index.css`-ში).
- [ ] **3.3** `PageHeader`-ს დაემატოს `hint?: ReactNode`, **სათაურის რიგში** და
      არა subtitle-ის სლოტში (`truncate`-ის შიგნით ღილაკი ჩაიჭრებოდა). 28
      subtitle-იდან **17 გადადის აიქონში, 11 რჩება** (ისინი ცოცხალი ფაქტია —
      რაოდენობა, ნაპოვნის რიცხვი, ბაზის სახელი).
- [ ] **3.4** **შერწყმული გასაღებები გაიყოს** — ეს აღმოჩენა ყველაზე მნიშვნელოვანია:
      9 ტექსტში ერთ სტრიქონში უკვე ზის საინფორმაციო წინადადება **და** კრიტიკული
      დანართი. გაყოფა `X` (ლურჯი) + `XWarn` (წითელი); `translate.reviewWarn`
      სუფიქსს უკვე ამკვიდრებს. ⚠️ `web.pagesHint`-ის `{{max}}` იმ ნახევარს უნდა
      დარჩეს, რომელსაც `{ max }` გადაეცემა, თორემ ეკრანზე ლიტერალი `{{max}}` დაიწერება.
      ⚠️ გაყოფის შემდეგ **`⚠️` სიმბოლო ტექსტიდან ამოიშალოს** — წითელი სამკუთხედი
      *არის* ნიშანი, ორივე ერთსა და იმავეს ორჯერ იტყოდა.
- [ ] **3.5** შენი დასახელებული ოთხი: `RolePage.tsx:253` (ლურჯი),
      `RolePage.tsx:359` (ლურჯი + წითელი — „მასობრივი წაშლა და გლობალური
      გადამრთველები მხოლოდ სუპერ-ადმინს" კრიტიკულია), `AuditPage.tsx:152`
      (ლურჯი + წითელი), `TranslationsPage.tsx:62` (ლურჯი).
- [ ] **3.6** გვერდშიდა პროზის გავლა — ვიწრო შაბლონი **79 ადგილას** ემთხვევა
      (`StorageCard`-ის ხუთი, `UploadLimitsCard`, `VisibilityManager`,
      `PublicProfileCard`, გალერეის ექვსი, `UserPage`-ის ექვსი, `SettingsPage`-ის
      სამი და ა.შ.). წითელი მხოლოდ იქ, სადაც უგულებელყოფა მონაცემს, ფულს ან
      კვოტას ხარჯავს (მაგ. `storage.orphansHint` — წმენდა ფაილებს შლის;
      `gallery.albumLockedMoveHint` — ფოტო მაშინვე ქრება).
- [ ] **3.7** ტესტი, რომ **დაჭერა ხსნის** — სწორედ ეს არის ის დეფექტი, რაც Radix-ის
      `Tooltip`-ს აქვს, და მხოლოდ ასეთი ტესტი შეაჩერებს უკან დაბრუნებას.
      ფაილი `.ts`-ად (`createElement`), როგორც სამი არსებული კომპონენტის ტესტი.

**წესი, რომლითაც კლასიფიკაცია ხდება** (რომ მომავალში სია აღარ დაგვჭირდეს):

> აიქონს შეუძლია დამალოს ის ტექსტი, რომლის გარეშეც მოქმედება მაინც სრულდება.
> **პროზად რჩება**, თუ: (1) პირობითია და მდგომარეობის გამო გამოჩნდა; (2) ნაბიჯის
> ინსტრუქციაა (`StepSection.hint`); (3) ცოცხალი ფაქტია (რიცხვი, სახელი, კვოტა);
> (4) ცარიელი მდგომარეობის ახსნაა.
> დანარჩენი აიქონში გადადის. **წითელი სამკუთხედი** — თუ უგულებელყოფა მონაცემს
> კარგავს, ფულს/კვოტას ხარჯავს, რაღაცას ხსნის, შეუქცევად შედეგს იძლევა, **ან ველი
> სავალდებულოა**. **ლურჯი `i`** — დანარჩენი.

❓ **გადასაწყვეტია:** `TabInfo`-ს 9 წყვეტილჩარჩოიანი ყუთი (მოდალების შიგნით)
გადავიტანოთ თუ დავტოვოთ? და ველების ქვეშ მდგომი ~30 „როგორ შეავსო" მინიშნება?

---

## §4. ველების კონსტრუქტორი — ჩაკეტვის მოხსნა სუპერ-ადმინისთვის ⚡

> შენი სიტყვები: „(სახელი* ჩაკეტილია, ველის გარეშე ჩანაწერი ვერ ჩაიწერება, ამიტომ
> ჩართულობა და სავალდებულოობა არ იმართება; ლეიბლი კი შეგიძლია შეცვალო) — ანუ
> სუპერ ადმინით რომ ვარ შესული, ასეთი რამის უფლება მე არ უნდა შემიზღუდო; უბრალოდ
> თუ მსგავსი ველია, გამაფრთხილე, თუ სავალდებულო ველს ჩავხსნი, რა შეიძლება მოხდეს
> და ის, რომ არ შეიძლება".

**ორი ცოცხალი ხარვეზი ამ არეალში — გასწორდეს ფუნქციაზე ადრე.**

- [ ] **4.1** ⚡ **„შენახვა" ჩუმად არაფერს აკეთებს.** ეს დღესვე შესაძლებელია,
      ჩაკეტვის მოხსნის გარეშე: `/modules/video`-ზე გამორთე `status` (ან `type_id`) →
      ფორმაზე ბლოკი `hidden`-ით იმალება, `form.status` ცარიელი რჩება (2026-09-16-დან
      არაფერი წინასწარ არ ირჩევა), `submit()` კი უპირობოდ ამოწმებს `pickErrors`-ს და
      ჩუმად ბრუნდება. **არც თოსტი, არც შეცდომა — შეცდომის ტექსტი `hidden` ელემენტზეა
      დახატული.** ერთნაირი ფორმა რვა ადგილას: `VideosPage`, `MovieFormPage`,
      `NoteForm`, `BookForm`, `GameForm`, `BoardGameForm`, `BookmarksPage`, `SongsPage`.
      მინიმალური გასწორება: შემოწმება მხოლოდ **ნაჩვენებ** ველებზე, ან თოსტი, რომელიც
      **ველს ასახელებს**. უკეთესი: ტესტი, რომელიც `RequiredFieldsTest`-ის სიას
      `FieldCatalog`-ს ადარებს და არ უშვებს, რომ სავალდებულო ველი დამალვადი იყოს.
- [ ] **4.2** ⚡ **`AdminUserController::syncModules()` იყენებს `->sync($ids)`-ს**,
      რაც პივოტს ხსნის და თავიდან აბამს მხოლოდ `enabled_at`-ით — ე.ი. ადმინი,
      რომელიც მომხმარებელს მოდულს უცვლის, **შლის მის `module_user.settings`-ს**:
      ველების კონფიგურაციას, გალერეის ნაგულისხმევებს, ჩანაწერების არხებს და
      სტატუსების განლაგებას. ნამდვილი მონაცემის დაკარგვაა და ამ ამოცანასთან
      უშუალოდ ახლოსაა.

შემდეგ თვითონ ფუნქცია. **ჩაკეტილი ველი ცხრაა** (თითო მოდულზე ერთი: `title`, ან
`url` ვიდეო/სიმღერა/ბუკმარკზე), ჩაკეტვა კი სამ ფენაშია: `FieldCatalog::for()`
აიძულებს `true`-ს, `FieldSettings::save()` საერთოდ არ ინახავს, კონტროლერი კი ჩუმად
უგულებელყოფს.

⚠️ **მთავარი აღმოჩენა: დღეს ჩაკეტვა ფორმაში ისედაც უაზროა** — **არცერთი ფორმა არ
ეკითხება `fields.shows()`-ს ჩაკეტილ ველზე** (ცხრავე ადგილას უპირობოდ იხატება, ორ
ფაილში კომენტარიც წერია, რომ „აზრი არ აქვს"). ე.ი. მარტო backend-ის ჩაკეტვის მოხსნა
გამორთვის ღილაკს დახატავდა, რომელიც **არაფერს შეცვლიდა** — ზუსტად ის, რასაც
პროექტი „ღილაკი, რომელიც არაფერს აკეთებს, უარესია ვიდრე ღილაკის არარსებობა" ჰქვია.

- [ ] **4.3** `module_user.settings.fields.<key>.unlocked` — მესამე მდგომარეობა.
      `locked` კატალოგის ფაქტად რჩება (ის სიმართლეს ამბობს სქემაზე), პასუხში კი
      ორივე მიდის, რომ ინტერფეისმა თქვას „ჩაკეტილია, მაგრამ შენ მოხსენი".
      ⚠️ `ModuleController::updateFields()`-ის ვალიდაციაში `fields.*.unlocked`
      **აუცილებლად** დაემატოს — `validate()` მხოლოდ ვალიდირებულ გასაღებებს აბრუნებს,
      ე.ი. დაუსახელებელი ატრიბუტი მთელ `fields` მასივს აგდებს და PUT 500-ით ვარდება
      (ეს უკვე მოხდა `public`-ზე).
- [ ] **4.4** გაფრთხილება `useConfirm`-ით (`variant: 'destructive'`), **აკრეფილი
      სიტყვის გარეშე** — აკრეფა `/purge`-სა და აღდგენას რჩება, სადაც შეუქცევადი
      დანაკარგია; აქ კი კონფიგურაციაა და უკან ბრუნდება. ტექსტმა სამი რამ უნდა თქვას:
      *რა იშლება* (ფორმაზე ველი აღარ იქნება), *როგორ გამოჩნდება* (შენახვა 422-ით
      ჩავარდება ეკრანზე აღარმყოფ ველზე, ე.ი. ღილაკი ვიზუალურად არაფერს გააკეთებს,
      და ახალი ჩანაწერის დამატება ამ მოდულში შეუძლებელი გახდება), *როგორ დაბრუნდე*
      (აქვე, ამავე გვერდზე უკან ჩართვით). ბოლოში — „არ გირჩევთ".
      ⚠️ **ფილმზე/სერიალზე/ანიმეზე ტექსტი ოდნავ სხვაა**: discover → `from-tmdb`
      ბარე `Request`-ს იღებს და სტატუსს `HasStatus`-ის hook-ი ავსებს, ე.ი. დამატება
      მაინც მუშაობს. ერთი ტექსტი, რომელიც 11-დან 3 მოდულზე მცდარია, არ გამოდგება.
- [ ] **4.5** ცხრავე ფორმამ **დაიწყოს `fields.shows()`-ის დაცვა ჩაკეტილ ველზეც** —
      თორემ ჩამრთველი ტყუილია. ეს სამუშაოს დიდი ნაწილია და სწორედ ის, რაც ყველაზე
      იოლად გამოტოვდება.
- [ ] **4.6** `VideoModuleTest:609` (`test_locked_field_is_listed_but_cannot_be_disabled`)
      **გადაიწეროს და არ წაიშალოს**: ჩაკეტილი → მოხსნილი → გამორთული → ისევ ჩაკეტილი,
      და ჩაკეტვისას დროშები `true`-ზე უნდა დაბრუნდეს, თორემ დამალული და თან ისევ
      ჩაკეტილი ველი დარჩება.

❓ **გადასაწყვეტია:** ჩაკეტვის მოხსნა მხოლოდ `super_admin`-ს მივცეთ თუ ყველას, ვისაც
ეს კონფიგურაცია ეკუთვნის? ტექნიკურად `/modules/{key}/fields` **საკუთარ** კონფიგს
წერს (სხვისას ვერავინ შეცვლის), ე.ი. ზიანი ერთი ადამიანის ფორმებია. მე მაინც
`super_admin`-ზე დავტოვებდი — ჩაკეტვის ღირებულება შემთხვევითი დაჭერის შეჩერებაა.

❓ **გადასაწყვეტია:** გვინდა თუ არა „ამ მოდულის ველები ნაგულისხმევზე დააბრუნე"
ღილაკი, როგორც საგარანტიო გასასვლელი? ერთი ჩაწერაა (`settings['fields']`-ის წაშლა).

---

## §5. მოთხოვნები — სორტირება, ძებნა და გვერდის ზომა

> შენი სიტყვები: „მოთხოვნები — მოდულში დამტკიცებული და სხვადასხვა ტაბები რომ არის,
> მას [ცხრილს] დეითეიბლის მსგავსი სორტირება ჰქონდეს, ასევე სერჩი და გვერდზე
> რამდენი გამოჩნდეს და ასე შემდეგ".

`DataTable` (`components/ui/data-table.tsx`) უკვე არსებობს — ძებნა, სორტირება,
გვერდის ზომა (10/25/50/100), გვერდები — და მისი ერთადერთი მომხმარებელი
`/modules/{key}`-ის მოდალია. `/requests` კი ხელით აწყობილი ბარათების დასტაა
(`RequestsPage.tsx:156-258`), სორტირების, ძებნისა და გვერდების გარეშე.

მასშტაბი: ბაზაში სულ **13 მოთხოვნაა** და ისინი ხელით იქმნება, ე.ი. `DataTable`-ის
კლიენტის მხარეს ფილტრაცია აქ **სწორია** (მისივე დოკბლოკი ამბობს, რომ სერვერული
ვარიანტი ცალკე კომპონენტი უნდა იყოს და არა დამალული განშტოება).

- [ ] **5.1** სია `DataTable`-ზე: ტიპი · მოთხოვნა · მომხმარებელი · თარიღი ·
      სტატუსი · მოქმედებები. `searchOf` მოიცავს მომხმარებლის სახელს, ელფოსტას,
      მოდულის/ჟანრის სახელს ორივე ენაზე, შეტყობინებასა და განხილვის შენიშვნას.
      ⚠️ თარიღი **`useDateFormat()`-ით** იხატება და ნედლ ISO-ზე სორტირდება —
      `RequestsPage.tsx:164` დღეს პირდაპირ `toLocaleString()`-ს ეძახის, რაც
      პროექტის წესს არღვევს; ეს არ უნდა გადაყვეს ახალ სვეტს.
      ⚠️ `requestLabel()` (`lib/display.ts`) რჩება ერთადერთ ტექსტის წყაროდ — უჯრამ
      ის უნდა გამოიძახოს და არა თავისი სტრიქონი ააწყოს.
- [ ] **5.2** **„რიგში" მყოფი მოთხოვნა სტრიქონი არ არის — ფორმაა**: მას აქვს
      შენიშვნის ტექსტარეა და (კვოტაზე) მეგაბაიტების ველი. ორივე `ModalShell`-ის
      „განხილვის" ფანჯარაში გადადის, სადაც ტიპის მიხედვით კონტექსტიც ჩანს.
      ⚠️ `grantOf()`-ის წესი უნდა გადარჩეს: **ცარიელი ველი ნიშნავს „ზუსტად რაც
      მოითხოვა"** — თუ მოდალი `0`-ს გაგზავნის, backend 422-ს აბრუნებს და მუშა
      დამტკიცება აუხსნელ შეცდომად იქცევა. ასევე შენიშვნა **უარყოფასაც** ეხება.
- [ ] **5.3** ⚠️ ჟანრის წაშლის წითელი გაფრთხილება („ეს ჟანრს ყველა ანგარიშიდან
      შლის") **არ უნდა გადავიდეს ისეთ უჯრაში, რომელიც გვერდით გადაიწევა** — ეს
      ერთადერთი ადგილია, სადაც ადმინი დამტკიცებამდე ხედავს, რას შლის.
- [ ] **5.4** ოთხი სტატუსის ბარათი (`ScopeCard`) იქვე რჩება, ცხრილის თავზე,
      იმავე პასუხიდან მომავალი რიცხვებით. ⚠️ აქ ფასეტი **განზრახ არ გამორიცხავს
      საკუთარ ჭრილს** (აუდიტისგან განსხვავებით) — ოთხი სტატუსი ურთიერთგამომრიცხავია,
      ე.ი. „გამორიცხე საკუთარი თავი" ყველა ბარათზე ჯამს დაწერდა. ეს არ „გასწორდეს".
- [ ] **5.5** `DataTable`-ს ორი დამატებითი, არასავალდებულო პროპი: `empty` გახდეს
      `ReactNode` (რომ `EmptyState` ჩაჯდეს — დღეს `/requests`-ზე ბარე `<p>`-ია) და
      `toolbar?: ReactNode` გვერდის საკუთარი კონტროლისთვის.
- [ ] **5.6** ახალი i18n გასაღებები **ორივე ლოკალში**, მერე `audit.py`.

❓ **გადასაწყვეტია:** „რიგში" ჭრილი ბარათებად დავტოვოთ (იქ მოქმედება ხდება) და
ცხრილი მხოლოდ დამტკიცებულ/უარყოფილ/ყველაზე გავუშვათ? შენი ფორმულირება
(„დამტკიცებული და სხვადასხვა ტაბები") სწორედ ასე იკითხება.

❓ **გადასაწყვეტია:** გვინდა ტიპის ჭრილიც (მოდული/კვოტა/ჟანრი) მეორე რიგ ბარათებად?
დღეს API ტიპებზე რიცხვებს საერთოდ არ აბრუნებს.

❓ **გადასაწყვეტია:** `/users`-საც აქვს თავისი ხელით აწყობილი ცხრილი (სორტირებით,
გვერდების გარეშე). ამავე გავლაზე გადავიტანოთ `DataTable`-ზე?

---

## §6. წიგნი — ენის სელექტი და ჩანიშვნების/ციტატების მოდალი

> შენი სიტყვები: „წიგნის დამატებაში ენა რომ არის, მანდ ნუ არის — ka en ru იყოს
> ჩვეულებრივი სელექტი"; და ფაილების/ჩანიშვნების ბლოკზე: „აქ ვიზუალიც ცოტა უფრო
> განაახლე; ციტატა და კომენტარი ტექსტები რამე ღილაკის მსგავსად იყოს და შიგნით
> მოდალი იხსნებოდეს, სადაც იქნება ვინ დაამატა, რა დაამატა, როდის, წაშლა ჰქონდეს
> და ძებნა; თუ მოდალს დახურავ ან უკან გახვალ, გადიოდეს იმ გვერდზე, საიდანაც
> დააჭირე მოდალის გახსნას".

**ენა.** დღეს ეს თავისუფალი ტექსტის ველია. ⚠️ **მკაცრი სამვარიანტიანი სელექტი
მონაცემს დაკარგავს**: ჩვენივე `OpenLibraryClient` აბრუნებს `de`-სა და `fr`-საც,
დანარჩენზე კი ნედლ MARC კოდს (`spa`, `ita`, `jpn`…). გერმანული წიგნის იმპორტის
შემდეგ სელექტი ცარიელი ჩანდება და პირველივე შენახვა `language`-ს `null`-ად
გადააწერდა. → **სამი პრესეტი + ჩანაწერის საკუთარი მნიშვნელობა, თუ ის სიაში არაა.**
Backend-ზე `Rule::in` **არ დაემატოს** — 422-ს დააბრუნებდა იმ მნიშვნელობაზე,
რომელიც ჩვენივე იმპორტიორმა ჩაწერა.

**ჩანიშვნები.** ხუთივე მოდულის ჩანიშვნას ერთი ფორმა აქვს
(`{id, body, created_at, updated_at}`), წიგნს კი ორი დამატებითი ველი (`is_quote`,
`page`). ე.ი. **ერთი გაზიარებული კომპონენტი შესაძლებელია.**

- [ ] **6.1** `BookForm`-ის `language` — `Select` სამი პრესეტით + საკუთარი
      მნიშვნელობის შენარჩუნებით. `FieldCatalog`-ში `type` `text` → `select`
      (⚠️ **`key` არ იცვლება** — გასაღების ცვლილება ყველა მომხმარებლის overrides-ს
      ობოლს ტოვებს).
- [ ] **6.2** `components/RecordNotes.tsx` — გაზიარებული, **მხოლოდ სხეული** (სათაური
      და მინიშნება გამომძახებელს რჩება, რადგან `VideoDetail`/`SongDetail` ტაბის
      შიგნით ხატავს). ჩანიშვნა = ღილაკი; ძებნა კლიენტის მხარეს `highlightParts()`-ით
      (უკვე დატესტილი; RegExp-ს არ იყენებს, ე.ი. `(`-ის აკრეფა შაბლონად არ იკითხება).
      ⚠️ წაშლის ღილაკი **ჩანიშვნის ღილაკის გარეთ** — ჩალაგებული `<button>` არასწორი
      HTML-ია და ერთი დაჭერა ორივეს გააკეთებდა.
- [ ] **6.3** `components/RecordNoteDialog.tsx` — ვინ დაამატა, როდის, სრული ტექსტი,
      რედაქტირება, წაშლა `useConfirm`-ით. ⚠️ **„უკან გასვლა იმ გვერდზე, საიდანაც
      გახსენი" კოდს არ საჭიროებს** — `ModalShell`-ის სტეკი ამას თვითონ აკეთებს:
      ჩალაგებულ მოდალს უკან-ისარი ავტომატურად უჩნდება, ქვედა მოდალი CSS-ით იმალება
      და **დამონტაჟებული რჩება**, ე.ი. ნახევრად შევსებული ფორმა ისევ იქ დაგხვდება.
- [ ] **6.4** ⚠️ **ჩანიშვნის წაშლას დღეს დადასტურება არ აქვს** (ფაილის წაშლას აქვს) —
      ეს ასიმეტრიაა და მონაცემის დაკარგვის რეალური გზა. მოდალში გასწორდეს.
- [ ] **6.5** ჩანიშვნის **რედაქტირება UI-ში საერთოდ არ არის**, თუმცა `PATCH`
      ხუთივე მოდულზე მარშრუტიზებულია და ორ API-ფაილში ფუნქციაც წერია. დანარჩენ ორს
      (`game`, `board_game`) `update*` ფუნქცია აკლია — ორი ოთხხაზიანი დამატება.
- [ ] **6.6** ფაილების ბლოკის ვიზუალი: სახეობის ხელით აწყობილი აბრები → `Chip`,
      ორი ბარე `<p>` („ფაილი ჯერ არ არის") → `EmptyState`. `useFileViewer` უცვლელი.
- [ ] **6.7** იგივე მოდალი გავრცელდეს ვიდეოზე, სიმღერაზე, თამაშსა და ბორდგეიმზე.
      ⚠️ `GameDetail` და `BoardGameDetail` დღეს `books.notesEmpty`-ს **ისესხებენ** —
      გაზიარებული კომპონენტი ან ზოგად `notes.*` სივრცეს დააკანონებს, ან ეს გასწორდეს.

❓ **გადასაწყვეტია:** ენა — მკაცრად სამი, თუ სამი პრესეტი + არსებულის შენარჩუნება
(ჩემი რეკომენდაცია მეორეა, იმპორტის გამო)? `de`/`fr` პრესეტებში ჩავამატოთ?

❓ **გადასაწყვეტია:** „ვინ დაამატა" — ამ აპლიკაციაში მეორე ადამიანი ჯერ არ არსებობს,
ჩანიშვნის `user_id` **ყოველთვის შენ ხარ**. გვინდა შენივე სახელი და ავატარი, თუ
სინამდვილეში „როდის დაემატა / როდის შეიცვალა" იყო საჭირო?

❓ **გადასაწყვეტია:** ძებნა ჩანიშვნების **სიაში** გვინდა თუ ერთი ჩანიშვნის ტექსტში?

---

## §7. პროფილი, საჯარო გალერეა და ჩაკეტილი ალბომები

> შენი სიტყვები: „პროფილში პაროლის ცვლილება აიტანე საჯარო პროფილის თავზე";
> „რომელი მოდულები ჩანს — გვერდზე ღილაკი ჰქონდეს მარჯვნივ და მოდალი გამოდიოდეს";
> „ასევე რომელი ჩანაწერი ჩანს და ფუნქციონალი საჯარო/პირადი — ისიც მოდალში გაიტანე,
> და თავზე ფილმები/სერიალები/ანიმეები ქარდის სტილის ტაბები იყოს; ასევე გალერეაც
> იყოს"; „სამომავლოდ სერვერზე რომ ავწევ, სხვა იუზერმა ნახოს ჩემს გალერეაში
> გასაჯაროებული ფოტოები, ხოლო პაროლიანი ალბომები ვერ ნახოს. ჩაკეტილ ფოტოებს
> ბლარიანი ფოტო დაუდგეს — აჯობებს ერთი სტატიკური ბლარის ფოტო; თუ პაროლს შეიყვანს,
> მერე გამოჩნდეს რეალური. ინსპექტიდან ან რამე მახინაციით პაროლის გარეშე რეალური
> ფოტო არ უნდა შეიძლებოდეს"; და დამაზუსტებელი სამი წინადადება:
> „**ალბომის პაროლი ეკუთვნის მომხმარებელს, ვისაც ალბომი აქვს**; დანარჩენი ფოტოები,
> თუ გალერეა საჯაროზეა, აქ ჩანს — უბრალოდ საჯაროშიც პაროლიან ალბომებს პაროლი
> ჭირდება. **პაროლს ადებ შექმნის დროს, მერე რედაქტირება შეგიძლია ექაუნთის პაროლით
> (მედიარიში ავტორიზებული მომხმარებლის პაროლით).** **გალერეის გასაჯაროება შეგიძლია
> გააკეთო ფილმების/მსახიობების გასაჯაროებით, როგორც ლოგიკურია**".

### 7A. განლაგება

- [ ] **7.1** პაროლის ფორმა `ProfilePage.tsx`-ში საჯარო პროფილის ბარათის **ზემოთ**
      გადავიდეს. ⚠️ დღეს ბოლოა და `mb-6` არ აქვს — გადატანისას დაემატოს, თორემ
      ქვემოთ მდგომ ბარათს მიეკვრება. სხვა არაფერი იცვლება.
- [ ] **7.2** `components/PublicModulesDialog.tsx` — მოდულების ჩამრთველები მოდალში,
      ბარათზე კი შემაჯამებელი რიგი + ღილაკი მარჯვნივ. მოდულის აიქონი და ფერი
      იქვე ჩანდეს. ⚠️ წესი, რომელიც არ უნდა დაიკარგოს: **სანამ პროფილი პირადია,
      მოდულების ჩამრთველები მკვდარია** — ან ღილაკი გამოირთოს, ან მოდალმა თქვას რატომ.
- [ ] **7.3** `VisibilityManager` მოდალში (`ModalShell size="wide"`), დომენების
      ჩიპები → `ScopeCard` მოდულის აიქონითა და ფერით (§2.7). ⚠️ რიცხვი მხოლოდ
      **აქტიურ** დომენზე ვიცით — დანარჩენებს `count` `undefined` დარჩეთ და არა `0`
      (`ScopeCard` ამას ითვალისწინებს), თორემ ბარათი ტყუილს იტყვის.

### 7B. საჯარო გალერეა — **ჩანაწერებიდან გამომდინარეობს, ახალი ღერძი არ ემატება**

შენი „ფილმების/მსახიობების გასაჯაროებით, როგორც ლოგიკურია" ზუსტად იმ წესს ემთხვევა,
რაც პროექტში უკვე წერია: **ფოტო მშობლის ხილვადობას იღებს მემკვიდრეობით**. ე.ი.
ფოტოს არც თავისი `visibility` სვეტი სჭირდება და არც ცალკე ჩამრთველი — ეს იქნებოდა
ორი წყარო ერთ ფაქტზე.

**წესი ერთი წინადადებით:** ფოტო საჯაროდ ჩანს, თუ **მისი ჩანაწერი საჯაროა**
(ფილმი · სერიალი · ანიმე · სიმღერა · წიგნი · თამაში), მსახიობის ფოტო კი — თუ ის
მსახიობი ჩემს **საჯარო** ჩანაწერში თამაშობს.

- [ ] **7.4** საჯარო პროფილს დაემატოს გალერეის ჩანართი, რომლის შიგთავსიც
      **გამოთვლადია და არა ცალკე ჩაწერილი**: `PublicProfileService::query()`-ის
      უკვე არსებული სამფენიანი შეკითხვით მოძებნილი საჯარო ჩანაწერების
      `galleryImages()`. ⚠️ მსახიობის ფოტოზე იგივე `whereHas` ლოგიკაა, რასაც
      `GalleryController::sourceQuery()` უკვე იყენებს („რომელი დომენიდან მოვიდა"
      მსახიობზე მისი ფილმებით გამოითვლება) — მეორე განმარტება არ დაიწეროს.
- [ ] **7.5** ⚠️ **გამონაკლისი: ფოტო, რომელსაც მშობელი არ ჰყავს** (ალბომში ან
      უბრალოდ თავისუფალი — §26-ის „უკატეგორიო"). მას მემკვიდრეობით არაფერი
      მოსდის, ე.ი. ერთადერთი, რასაც აქ ჩამრთველი სჭირდება, **ალბომია**:
      `gallery_albums.visibility` (ნაგულისხმევად `private`, როგორც ყველგან) და
      `PublicDomain::DOMAINS`-ში ჩანაწერი. ⚠️ `RegistryConsistencyTest` `visibility`
      სვეტის დამატებისთანავე ჩავარდება, სანამ დომენი არ დარეგისტრირდება.
      ⚠️ `MATCH`-ში **არ** დაემატოს — ალბომს გლობალური იდენტობა არ აქვს
      (`playlist`-ის ზუსტი პრეცედენტი).
- [ ] **7.6** ე.ი. ხილვადობის მმართველში (§7.3) „გალერეის" ტაბი **ალბომებს** აჩვენებს —
      ერთადერთს, რასაც ამ მოდულში გადამრთველი აქვს. დანარჩენი გალერეა უკვე იმ
      ტაბებიდან იმართება, სადაც თვითონ ჩანაწერებია (ფილმები, სერიალები, …).
- [ ] **7.7** `ModuleController`-ის `shareable` გალერეაზე `true` გახდება, ე.ი.
      მოდულს საჯარო ჩამრთველიც გაუჩნდება (`PUT /modules/gallery/public` დღეს 422-ს
      აბრუნებს). ფრონტზე `PUBLIC_DOMAINS` + `DOMAIN_MODULE` სარკეც.

### 7C. ჩაკეტილი ალბომი — ბლარი, პაროლი და „ინსპექტიდანაც არა"

**რაც უკვე მუშაობს** (2026-09-16, დაუკომიტებელი): პაროლი `Hash::make()`-ით ერთ
სვეტში, `password_hash` `$hidden`-ში, გლობალური სკოუპი `GalleryImage`-ზე
(`NULL NOT IN` ხაფანგის ჩათვლით), 423 `album_locked` ორივე შესასვლელზე, პრევიუს
გამოტოვება, სესიაზე დაფუძნებული გახსნა (და არა კლიენტის ტოკენი),
`throttle:album-unlock`, ჩაკეტილი ალბომის წაშლა 423-ია, რიცხვები პატიოსანი რჩება.

**რაც ამ მოთხოვნით იცვლება:**

- [ ] **7.8** ⚠️ **ალბომის პაროლი ანგარიშის პაროლით იმართება** (შენი გადაწყვეტილება).
      პაროლი დაიდება **ალბომის შექმნისას**; შემდეგ მისი შეცვლა ან მოხსნა
      **მედიარიში ავტორიზებული მომხმარებლის ანგარიშის პაროლს** მოითხოვს
      (`Hash::check($request->account_password, $user->password)`), და **აღარ**
      ალბომის მოქმედ პაროლს — დღეს `GalleryAlbumController::update()` სწორედ
      `current_password`-ს ითხოვს.
      ⚠️ **ეს აუქმებს დღევანდელ „აღდგენა არ არსებობს" წესს** და ეს კარგია: „დამავიწყდა
      ალბომის პაროლი" აღარ არის ჩიხი, მფლობელობა კი მაინც მტკიცდება.
      ⚠️ `CLAUDE.md`-ში ჩაწერილი წინადადება („there is no recovery and that is the
      point") ამ ცვლილებასთან ერთად უნდა გადაიწეროს, თორემ ფაილი კოდს დაუპირისპირდება.
      ⚠️ ანგარიშის პაროლის შემოწმებას **თავისი `throttle` სჭირდება** — ეს login-ის
      გარეთ მეორე ადგილი ხდება, სადაც ანგარიშის პაროლი მოწმდება.
- [ ] **7.9** ⚠️ **ფაილი პირად დისკზე უნდა გადავიდეს — ეს აღარ არის არჩევითი.**
      დღეს ჩაკეტილი ფოტო საჯარო დისკზე დევს, ე.ი. **დამახსოვრებული ან გამოცნობილი
      `/storage/...` ბმული მაინც იხსნება** — ზუსტად ის „მახინაცია", რასაც გამორიცხავ.
      საჭიროა: `StorageFolder::PRIVATE_FOLDERS`-ის ჩანაწერი (`videos/downloads`-ის
      პრეცედენტი), ფაილების **ფიზიკური გადატანა** ალბომის ჩაკეტვისას და გახსნისას,
      და ერთადერთი მომსახურე მარშრუტი, რომელიც ამოწმებს „ეს ალბომი ამ სესიაში
      გახსნილია თუ ვარ მისი მფლობელი". ⚠️ გადატანა `path` სვეტს ცვლის —
      `poster_path`-ზე მიმთითებელი ჩანაწერებიც უნდა განახლდეს ერთ ტრანზაქციაში.
- [ ] **7.10** **ბლარის ადგილსამყოფელი — ერთი სტატიკური ფაილი, და არა თითო ფოტოზე
      გენერირებული.** შენ ორივე ვარიანტი დაასახელე; სტატიკური სჯობს სამი მიზეზით:
      გენერირებული ბლარი **რეალური ფოტოდან მიიღება** (ფერი და კომპოზიცია მაინც
      გამოსჭვივის), თითო ფოტოზე ფაილი დისკსა და კვოტას ხარჯავს, და გამოსახულების
      დასამუშავებელი ბიბლიოთეკა პროექტში საერთოდ არ არის. ერთი `public/`-ში მდგომი
      აქტივი **არაფერს ჟონავს და არაფერი ჯდება**.
      ⚠️ ფილას პროპორცია `gallery_images.width/height`-იდან მოვიდეს, რომ ბადემ არ
      ახტეს — ზომა ფოტოს შინაარსზე არაფერს ამბობს.
- [ ] **7.11** **ჩაკეტილი ალბომის ფოტოები პასუხში `path`-ის გარეშე დაბრუნდეს.**
      დღეს ისინი სრულად ქრება (სკოუპი მათ ჭრის); ახალი ქცევა — რიგი ჩანს, მაგრამ
      ატარებს მხოლოდ `id`-ს, ზომებსა და `locked: true`-ს. **`path`, `remote_path`,
      `source_url` — არცერთი არ მიდის.** ეს არის „ინსპექტიდანაც ვერაფერს ნახავ":
      რაც არასდროს გაიგზავნა, იმის პოვნა შეუძლებელია.
      ⚠️ `AlbumLock`-ის სკოუპს ეს ცვლის: „სრულად დამალვიდან" „გაშიშვლებაზე"
      გადადის. ორივე ქცევა საჭიროა — წაშლა/კვოტა/dedupe ისევ `withoutGlobalScope`-ს
      უნდა იყენებდეს (დღეს ასეა და არ უნდა დაირღვეს, თორემ დამალული ფოტო ობოლი
      ფაილივით დარჩება და კვოტას სამუდამოდ დაიკავებს).
- [ ] **7.12** ⚠️ **პაროლის ფანჯარა საჯარო გვერდზეც უნდა მუშაობდეს.** პაროლი
      **მფლობელისაა** — ე.ი. უცხო მას არ იცნობს და ალბომი მისთვის პრაქტიკულად
      ჩაკეტილი რჩება — მაგრამ თვითონ მექანიზმი საჯარო გვერდზეც უნდა არსებობდეს
      („საჯაროშიც პაროლიან ალბომებს პაროლი ჭირდება"). დღეს
      `AlbumLock::hiddenIds()` იწყება `$userId = Auth::id(); if (! $userId) return [];` —
      ე.ი. **არაავტორიზებულზე ჩაკეტვა საერთოდ არ მუშაობს** (დღეს უვნებელია მხოლოდ
      იმიტომ, რომ საჯაროდ გალერეიდან არაფერი იკითხება; §7.4-ის შემდეგ ეს კარი იღება).
      საჭიროა: გახსნილი ალბომების ნაკრები **სესიაზე** იყოს მიბმული და არა
      `Auth::id()`-ზე, ხოლო ჩაკეტილთა სია **პროფილის მფლობელის** id-ით გამოითვალოს.
- [ ] **7.13** ⚠️ **პაროლის გამოცნობის ლიმიტი უცხოზეც.** დღევანდელი
      `throttle:album-unlock` მომხმარებელზეა მიბმული; ანონიმურზე გასაღები
      **IP + ალბომი** უნდა იყოს. ამის გარეშე ოთხსიმბოლოიანი პაროლი წუთებში ცვივა.
- [ ] **7.14** ⚠️ **`poster_path`-ის გვერდის ავლა.** „მთავარად დაყენება" ჩანაწერის
      `poster_path`-ს `gallery_images.path`-ზე მიუთითებს, გლობალური სკოუპი კი
      **მოდელზეა და არა ამ სვეტზე** — ე.ი. ჩაკეტილ ალბომში გადატანილი ფოტო, თუ ის
      ჩანაწერის მთავარი პოსტერია, ისევ იხატება (და საჯარო ბარათზეც). ან ასეთ ფოტოზე
      ჩაკეტილ ალბომში გადატანა აიკრძალოს, ან ჩაკეტვისას პოსტერი გასუფთავდეს.
- [ ] **7.15** მფლობელის საკუთარი ხედიც იმავე ენაზე დაილაპარაკოს: ჩაკეტილი ალბომი
      მასაც ბლარიან ფილებად დაენახოს (და არა ცარიელ სიად), სანამ პაროლს არ შეიყვანს.

---

## §8. სერვერის დრო → საქართველოს დრო

> შენი სიტყვები: „რაც შეეხება თაიმზონას, ყველგან ისე გადაიკეტოს, რომ დრო იყოს იმ
> ქვეყნის მიხედვით, სადაც არის ლოკაცია; ამ ეტაპზე თბილისი, საქართველოს დრო იყოს,
> რადგან მე მანდ ვარ".

**გადაწყვეტილია: `Asia/Tbilisi`.** ეს მონაცემის სემანტიკის ცვლილებაა და არა
ჩვენებისა, ამიტომ თანმიმდევრობა მკაცრია.

გაზომილი მდგომარეობა:

- `backend/config/app.php` → `'timezone' => 'UTC'` (env-ით **არ** იმართება).
- MySQL-ის `time_zone` = `SYSTEM` (+4): `NOW()` = 14:43, `UTC_TIMESTAMP()` = 10:43.
- სქემაში **158 `timestamp`**, **11 `datetime`**, 14 `date` სვეტია.
- Laravel თარიღს ISO-8601-ად UTC-ში ასერიალიზებს (`…Z`), ბრაუზერი კი ლოკალურად
  ხატავს → **SPA უკვე თბილისს აჩვენებს.** UTC დღეს სამ ადგილას ჩანს: ბაზაში,
  ლოგებში და სერვერზე აწყობილ ფაილთა სახელებში (`mediary-2026-09-16-1043.sql`).

⚠️ **არსებული რიგები UTC-ის საათით ჩაიწერა.** გადართვის შემდეგ იგივე სტრიქონი
თბილისად წაიკითხება, ე.ი. **ყოველი ისტორიული დრო 4 საათით უკან გადაიწევს** — თუ
მიგრაცია არ გაუშვა.

- [ ] **8.1** **ჯერ სრული `mysqldump`.** მიგრაცია შეუქცევადია და ეს ერთადერთი
      უკან გზაა.
- [ ] **8.2** `config/app.php` → `env('APP_TIMEZONE', 'Asia/Tbilisi')`, `.env`-ში
      `APP_TIMEZONE=Asia/Tbilisi`. ⚠️ **env-ით იმართებოდეს** — შენ თვითონ თქვი
      „იმ ქვეყნის მიხედვით, სადაც ლოკაციაა", ე.ი. სერვერის გადატანა ერთი ხაზი
      უნდა იყოს და არა კოდის ცვლილება.
- [ ] **8.3** მიგრაცია: **ყველა `timestamp`/`datetime` სვეტს +4 საათი**
      (`UPDATE … SET col = col + INTERVAL 4 HOUR`), რომ ისტორიულმა ჩანაწერმა იგივე
      რეალური მომენტი შეინარჩუნოს. ⚠️ `date` სვეტებს **ხელი არ ეხება** — ისინი
      კალენდარული თარიღებია (გამოსვლის თარიღი, დაბადების დღე), და არა მომენტები.
      ⚠️ ცხრილ-ცხრილობით და ერთი გაშვებით; მიგრაცია იდემპოტენტური არ არის, ორჯერ
      გაშვება 8 საათს დაამატებს — `migrations` ცხრილი ამას იცავს, ხელით გაშვება არა.
- [ ] **8.4** ⚠️ `note_reminders` ცალკე შემოწმდეს: `mode = once`-ის `remind_at` და
      ყველა `next_at` აბსოლუტური მომენტია და +4-ს ექვემდებარება, **ხოლო
      `times_of_day` კედლის საათია და არ უნდა შეიცვალოს** (მას თავისი `timezone`
      სვეტი აქვს). აქ ორმაგი წანაცვლების რეალური რისკია.
- [ ] **8.5** `TranslationUsage::QUOTA_TIMEZONE` (`America/Los_Angeles`) **არ
      შეიცვალოს** — ეს Google-ის ლიმიტის ფანჯარაა და ჩვენს ლოკაციასთან კავშირი არ აქვს.
- [ ] **8.6** ტესტები: (ა) ჩაწერილი და წაკითხული დრო იმავე მომენტს უნდა აღნიშნავდეს;
      (ბ) სერვერზე აწყობილი ფაილის სახელი ქართულ საათს ატარებდეს;
      (გ) შეხსენების `computeNextAt()` იმავე მომენტს აბრუნებდეს, რასაც მიგრაციამდე.
- [ ] **8.7** მიგრაციის შემდეგ `php artisan mediary:storage-recalc` საჭირო **არ არის**,
      მაგრამ `backend/mediary_backup.sql` განახლდეს — შემდეგი აღდგენა უკვე ახალ
      სემანტიკაში უნდა ხდებოდეს.

❓ **სამომავლოდ (ახლა არ კეთდება):** როცა რამდენიმე მომხმარებელი იქნება სხვადასხვა
ქვეყანაში, სწორი გაგრძელებაა `users.timezone` და ჩვენება მომხმარებლის ზონაში —
შენახვა მაშინაც ერთ ზონაში რჩება. `note_reminders.timezone` ამის პრეცედენტია.

---

## §9. მასობრივი ცვლილება — ტეგებისა და სტატუსის სკოუპები

> შენი სიტყვები: „სტატუსის შეცვლა — ვიდეოებში მაგალითად ტიპის შეცვლაზე გამოდის
> ტიპები ქვევით; ტეგების დამატებაზე უნდა ირჩევდე, რომელი ტეგის მქონეს დაუმატო
> ახალი ტეგი, ან რომელი ტიპის მქონეს დაუმატო ახალი ტეგი, ან კონკრეტულს, ან ყველას;
> ტეგების მოხსნაზეც ანალოგიური შესაძლებლობები უნდა იყოს".

დღეს წყაროს არჩევა **ორვარიანტიანია**: `ids` **ან** ერთი `from_type_id`
(`VideoBulkController:43-51`). არც ტეგით, არც სტატუსით, არც „ყველა", არც რამდენიმე
ტიპი.

⚠️ **ლექსიკა უკვე არსებობს და არ უნდა გამოვიგონოთ**: `PurgeService::TARGET_MODES`
ვიდეოზე სწორედ `ids | type | tag | status | all`-ს იცნობს, ფრონტზე კი მისი
ტიპიზებული სარკე `PURGE_TARGET_MODES`. იგივე სიტყვები, იგივე მნიშვნელობა.

- [ ] **9.1** `POST /videos/bulk`-ს დაემატოს `scope`: `ids | type | tag | status | all`.
      ერთი პრივატული `scopeQuery()`, `PurgeService::recordIds()`-ის ფორმით.
      ⚠️ **ტეგის სკოუპი `tags` გასაღებით ვერ დაერქმევა** — `tags` უკვე მოქმედების
      დატვირთვაა, ე.ი. „დაამატე ტეგი X ყველას, ვისაც X აქვს" თავის თავზე
      მიუთითებდა. ერთ-ერთი მხარე გადაერქვას.
      ⚠️ ქართული ტეგი სვეტში **escape-ულად ზის**, ამიტომ შედარება PHP-ში,
      `Video::tagKey()`-ით (როგორც `PurgeService` აკეთებს) — არც ხელით `LIKE`, არც `===`.
- [ ] **9.2** `GET /api/videos/bulk-preview` → `{count, sample[]}`, **იმავე**
      `scopeQuery()`-ზე, რომ „დათვლილი" და „შეცვლილი" ვერ დაშორდეს (ეს `/purge`-ის
      `plan()`/`run()`-ის წესია). ⚠️ **`GET` და არა `POST`** — `preview`/`plan`
      `UPDATE_ENDPOINTS`-ში არ არის, ე.ი. POST-ს შუამავალი `create`-ად წაიკითხავს
      და view+update როლი უსაფუძვლო 403-ს მიიღებს.
- [ ] **9.3** ⚠️ `type_id`-ის `Rule::requiredIf(action === 'type')` უნდა გადარჩეს —
      ეს 2026-09-16-ის დამცავია, რომ მასობრივმა ოპერაციამ ტიპი ვერ მოხსნას.
      `VideoBulkPanel`-ის მკვდარი `NO_TYPE → null` ტოტი წაიშალოს.
- [ ] **9.4** წყაროს ამრჩევი ორვარიანტიანი რადიოდან ბარათებზე გადავიდეს (§2-ის
      ბარათები), ხოლო კლიენტის მხარეს დათვლილი „N შეეხება" სერვერის პრევიუთი
      ჩანაცვლდეს — თორემ ნაჩვენები რიცხვი და შეხებული რიგები ორი სხვადასხვა
      განმარტებით ითვლება.
- [ ] **9.5** მანქანური შეცდომის კოდები — დღეს `VideoBulkController` **ქართულ
      წინადადებებს** აბრუნებს, `RecordStatusController` კი `scope_required`-ს, და
      არცერთი არ არის `lib/errors.ts`-ის `CODES`-ში, ე.ი. 422 ნედლად იხატება.
- [ ] **9.6** `POST /videos/bulk-status` **არსებობს და SPA-ს არცერთი გამომძახებელი
      არ ჰყავს**, თუმცა `/status` გვერდის კომენტარი ამბობს, რომ ვიდეოს მასობრივი
      სტატუსი აქვს. ან ჩაერთოს პანელში (ერთი მუტაცია + სამი გასაღები), ან კომენტარი
      გასწორდეს.
- [ ] **9.7** `bulkStatus.*` ტექსტები სიტყვასიტყვით „ფილმს" ამბობს იმ პანელზე,
      რომელიც სერიალსა და ანიმესაც ემსახურება — `mediaKey()` (`lib/media.ts`)
      სწორედ ამისთვის არსებობს.

⚠️ **მედია დომენებს ტეგი საერთოდ არ აქვთ** (სვეტი არ არსებობს), ე.ი. ტეგების
სკოუპები **მხოლოდ ვიდეოა**. ეს ცხადად უნდა ეწეროს, თორემ იგივე კითხვა თავიდან დადგება.

❓ **გადასაწყვეტია:** პრევიუ სავალდებულო გავხადოთ (როგორც `/purge`-ში) თუ
სურვილისამებრ? და `scope = all` აკრეფილ დადასტურებას მოითხოვდეს თუ ჩვეულებრივს?

❓ **გადასაწყვეტია:** იგივე სკოუპები სიმღერას/წიგნს/ჩანაწერს/ბუკმარკს (დანარჩენი
ტეგიანი მოდულები) მივცეთ, თუ ჯერ მხოლოდ ვიდეო?

---

## §10. ჩატი — Instagram/Messenger-ის დონე

> შენი სიტყვები: „ჩატის ფუნქციონალი მინდა სრულყოფილი, ანალოგი მეტა ინსტაგრამი და
> ფეისბუქი: ჩატის მედია, ჩატის დადუმება/ჩართვა, ასევე ემოჯები, ფაილი/ვიდეო/ფოტოს
> ატვირთვა, ჩატში შესვლისას ბოლო იხსნებოდეს ავტომატურად, ჩატში სერჩის ფუნქციონალი,
> პროფილზე გადასვლა, წერილების დაპინვა, პინების ლისტი, ჩატის სხვადასხვა თემები,
> ემოჯი [რეაქციები], ნიკნეიმები".

12 მოთხოვნიდან: **3 უკვე არსებობს** (მედია, ატვირთვა, პროფილზე გადასვლა),
**2 ნაწილობრივ** (ავტო-სქროლი — ხარვეზით; ემოჯი — ტიპები და თარგმანები არსებობს,
ამრჩევი არა), **7 საერთოდ არ არის**.

### ტიერი 0 — თითო ერთი დღე, სქემის ცვლილების გარეშე

- [ ] **10.1** ⚠️ **ავტო-სქროლი გასწორდეს.** დღეს ეფექტი `thread.data.length`-ზეა
      მიბმული, ე.ი. (ა) სხვა საუბარზე გადასვლისას, თუ გვერდს იგივე სიგრძე აქვს,
      **საერთოდ არ ირთვება** და შუაში აღმოჩნდები; (ბ) 5-წამიანი გამოკითხვა სიგრძეს
      ზრდის, ე.ი. ისტორიის კითხვისას **ახალი წერილი ქვემოთ გადაგაგდებს**. სწორად:
      „ბოლოშია?" მდგომარეობა + მონტაჟზე/საუბრის ცვლილებაზე მყისიერი სქროლი +
      „ახალი წერილები ↓" აბრა.
- [ ] **10.2** ემოჯი კომპოზიტორში. ⚠️ **დამოკიდებულების გარეშე** — `emoji-picker-react`
      ≈100–110 kB gzip (ორი `dnd-kit`, რომელიც პროექტმა უარყო), `emoji-mart`-ს
      დიდი async მონაცემი აქვს, `frimousse` კი emoji-ს **CDN-იდან** იღებს (ოფლაინში
      საერთოდ არაფერი). ამის ნაცვლად: ~150–200 ემოჯის კურირებული სია
      (`lib/emoji.ts`) უკვე არსებულ `@radix-ui/react-popover`-ზე.
      პატიოსანი ფასი: სკინ-ტონები და სრული ~3700-იანი ნაკრები არ იქნება.
      backend-ს შეხება არ სჭირდება — `utf8mb4` და `type: 'emoji'` უკვე მუშაობს.
- [ ] **10.3** „ნანახია" — `conversation_user.last_read_at` **ორივე მხარისთვის უკვე
      ინახება** და კლიენტს არასდროს მიდის. ერთი ველი პასუხში და ბოლო ბუშტს
      ნიშანი უჩნდება. სქემა არ იცვლება.
- [ ] **10.4** მკვდარი `markThreadRead()` ექსპორტი წაიშალოს (გამომძახებელი არ ჰყავს;
      `GET`-ი ისედაც აღნიშნავს წაკითხულად).

### ტიერი 1 — თითო ერთი-ორი სვეტი

- [ ] **10.5** **დადუმება** — `conversation_user.muted_at` + `muted_until`
      (ორი სვეტი, **არა JSON**). მიზეზი: `unreadCounts()` ნედლი `DB::table()` შეკითხვაა
      და დადუმება სწორედ იქ უნდა მოქმედებდეს; JSON-ში ჩაწერილს ეს შეკითხვა ვერ
      გამოიყენებს დრაივერზე დამოკიდებული SQL-ის გარეშე (ეს ხაფანგი პროექტს უკვე
      დაუჯდა `jsonLike()`-ში). ორი სვეტი — რადგან „სამუდამოდ" სენტინელ-თარიღით
      არ უნდა გამოიხატოს. ⚠️ **ბეჯი გაჩუმდეს, რიგის რიცხვი კი პატიოსანი დარჩეს.**
- [ ] **10.6** **თემები** — `conversations.theme` (ერთი სვეტი, ორივე მხარისთვის
      საერთო, როგორც Messenger-ში), მნიშვნელობა **გასაღები და არა hex**.
      ფერები SPA-შია და DOM-ში inline CSS-ცვლადებით მიდის (`modAccent()`/`toolAccent()`-ის
      მექანიზმი). ⚠️ **მუქი თემის ვარიანტი სავალდებულოა** — ერთი ღია პალიტრა მუქზე
      მელნის ლაქაა.
- [ ] **10.7** **დაპინვა + პინების სია** — `messages.pinned_at` + `pinned_by`
      (ზუსტად `removed_at`/`removed_by`-ის ფორმით). პინი საუბრის დონეზეა, ე.ი.
      ორივე ხედავს; **ორივე მონაწილეს შეუძლია** (განსხვავებით „ორივესთვის წაშლისგან",
      რომელიც ავტორობას მოითხოვს — პინი დესტრუქციული არაა). სია
      `scopeVisibleTo()`-ს გადის, ე.ი. დამალული/წაშლილი წერილი **თავისით ცვივა**
      პინებიდან, მეორე ჩაწერის გარეშე. ლიმიტი + კოდი `lib/errors.ts`-ში.

### ტიერი 2 — ნამდვილი სამუშაო

- [ ] **10.8** ⚠️ **წინაპირობა, რომელიც არავის მოუთხოვია და ყველაფერს სჭირდება:
      SPA ბოლო 50 წერილზე მეტს ვერ კითხულობს.** `fetchThread(id)` ყოველთვის პირველ
      გვერდს იღებს, `meta.last_page` არსად გამოიყენება, „ძველის ჩატვირთვის" ღილაკი
      არ არსებობს. საჭიროა კურსორი: `before_id` („ძველი") და `around_id`
      („ნახტომი ძებნიდან ან პინიდან"). ⚠️ `around_id` **არ უნდა აღნიშნავდეს
      წაკითხულად** — ისტორიაში ჩახტომა არ ნიშნავს „ყველაფერი წავიკითხე".
- [ ] **10.9** **ძებნა ჩატში** — `GET /chat/{conversation}/search`. სამი უკვე
      არსებული ნაწილი: `scopeVisibleTo()` (დამალული წერილი არ უნდა იძებნებოდეს),
      `App\Support\Snippet::around()` (mb-უსაფრთხო კონტექსტი) და კლიენტზე
      `highlightParts()`. ⚠️ `GlobalSearch`-ის `escape()`/`like()` **`private`-ია** —
      გამოიტანოს `App\Support\Like`-ში და `GlobalSearch` მას დაეყრდნოს; მეორე ასლი
      სწორედ ის დუბლირებაა, რომლის გამოც `DictionaryKey` შეიქმნა. ეძებოს `body`-შიც
      და `attachment_name`-შიც.
- [ ] **10.10** **რეაქციები** — `message_reactions` ცხრილი,
      `unique(message_id, user_id)` (თითო კაცზე ერთი — Messenger/Instagram-ის
      სემანტიკა, ე.ი. სწორედ ის, რაც დაასახელე; გაფართოება მოგვიანებით უსაფრთხოა,
      შევიწროება — არა). `emoji` `varchar(32)` (ერთი ZWJ-თანმიმდევრობა 25 ბაიტია).
      ⚠️ `payload()`-ში ჩამატებისას `with('reactions')` აუცილებელია, თორემ 50-წერილიან
      გვერდზე 50 დამატებითი შეკითხვაა. ⚠️ `AuditRegistry`-ში **არ** დაემატოს —
      ლოგს დამარხავდა.

### ტიერი 3

- [ ] **10.11** **ნიკნეიმები** — ცალკე ცხრილი `conversation_nicknames`
      (`conversation_id`, `user_id` = ვისი ხედი, `target_user_id`, `nickname`).
      ⚠️ პივოტის სვეტი **არ გამოდგება** — ის მუშაობს მხოლოდ იმიტომ, რომ მონაწილე
      ზუსტად ორია, ხოლო სქემა სწორედ ამ დაშვებას გაურბის (`message_hides`-ის
      მიგრაციის დოკბლოკში ეს პირდაპირ წერია). პასუხში **ორივე სახელი** მიდის, ე.ი.
      ნამდვილი ვინაობა არ იკარგება; და ერთი `chatDisplayName()` ჰელპერი, არა სამი
      ადგილი, სადაც თითო თავისით წყვეტს.

---

## §11. ბაზის ასლი — ცხრილების ვიუერი და ნაწილობრივი აღდგენა

> შენი სიტყვები: „ბაზის ასლში, თუ არის შესაძლებელი, იყოს ასევე ნახო რა თეიბლებს
> შეიცავს და კონკრეტული თეიბლის ჩანაწერების ნახვა — რამე ვიუვერი რომ იყოს; ასევე
> შეგეძლოს კონკრეტული თეიბლის აღდგენა, ან კონკრეტული ჩანაწერის, ან სრული".

**შესაძლებელია, და გაზომილიც.**

- [ ] **11.1** **ცხრილების სია — 8 მილიწამი, აღდგენის გარეშე.** გაზომილია რეალურ
      დამპზე (4.2 MB SQL → 829 kB gz): `gzopen` + სრული გავლა → **83 ცხრილი,
      თითოს ბაიტები და `INSERT`-ების რაოდენობა, 4 MB მეხსიერებაში.** ⚠️
      `database_backups.tables` ამას **ვერ ჩაანაცვლებს** — ის **რიცხვია და არა სია**,
      და იმპორტირებულ ფაილზე საერთოდ `null`-ია. ახალი JSON სვეტი `table_map` და
      `countTables()`-ის ჩანაცვლება ერთი გავლით (მეორე გავლა არ დაემატოს).
- [ ] **11.2** **პირველი ~100 რიგის პრევიუ — ისევ აღდგენის გარეშე.** ⚠️ პატიოსანი
      ფასი: `mysqldump` `--complete-insert`-ს არ იყენებს, ე.ი. სვეტების სახელები
      `INSERT`-ში **არ წერია** და `CREATE TABLE`-ის პარსერიც სჭირდება; ტუპლების
      დაშლა კი ნამდვილი მდგომარეობის მანქანაა (escape-ები `\0 \n \r \\ \' \" \Z`,
      მძიმეები და ფრჩხილები ტექსტის შიგნით). ერთი ნუგეში: **ამ სქემაში ბინარული
      სვეტი საერთოდ არ არის** (0 blob/binary), ე.ი. ყველაზე მძიმე ნახევარი ცვივა.
      ⚠️ ეს კოდი ჩუმად ცდება — თავისი ტესტები ბოროტი ფიქსტურებით (escape-ული
      ბექსლეში ბრჭყალის წინ, `\n` მნიშვნელობაში, ქართული JSON-ში, `NULL` vs `'NULL'`).
- [ ] **11.3** **ნამდვილი ვიუერი — დროებით ბაზაში იმპორტით.** სრული აღდგენა ამ
      დამპისა **1.5 წამია** (გაზომილი). შემდეგ ყოველი კითხვა ნამდვილი SQL-ია:
      გვერდები, სორტირება, ფილტრი, `COUNT(*)`. ⚠️ პარსერის გზით იგივე კითხვები
      ყოველ გვერდზე მთელ ფაილს ახლიდან შლის (`gzseek` შემთხვევითი წვდომა არ არის).
      ⚠️ **გადამოწმებულია ცოცხლად: `root`-ს `GRANT ALL ... WITH GRANT OPTION` აქვს**
      (MariaDB 10.4.32), ე.ი. `CREATE DATABASE` და ჯვარედინი `INSERT … SELECT`
      მუშაობს. ⚠️ დროებითი ბაზა **კვოტას ვერ დაეთვლება** (ის ფაილი არაა) — ეს
      ცხადად უნდა ეწეროს ინტერფეისში.
- [ ] **11.4** **ერთი ცხრილის აღდგენა.** ⚠️ **აქ არის ყველაზე დიდი საფრთხე და ის
      გაზომილია:** ბაზაში **119 უცხო გასაღებია — 99 `CASCADE`, 20 `SET NULL`**.
      `users`-ზე **61 შემომავალი გასაღები** მიდის, ე.ი. „მხოლოდ users ცხრილის
      აღდგენა" (წაშლა+ჩაწერა) **ყველა ანგარიშის მთელ ბიბლიოთეკას წაშლიდა** და
      მხოლოდ users-ს დააბრუნებდა. `statuses`-ზე ექვსი `SET NULL` მიდის, ე.ი. მისი
      აღდგენა ჩუმად **ყველა ჩანაწერს სტატუსს მოხსნიდა**.
      ⚠️ **პოლიმორფულ სვეტებს FK საერთოდ არ აქვთ** (`gallery_images.imageable_*`,
      `castables`, `genreables`, `audit_logs.subject_*`) — ე.ი. გატეხილ მიმართებას
      SQL **ხმას არ გამოსცემს**; შემდგომი შემოწმება ჩვენი საქმეა.
      ⚠️ `TRUNCATE` უვარგისია (InnoDB უარს ამბობს FK-ით მითითებულ ცხრილზე).
      → ერთი რეესტრი `safe | warns | blocked`, `blocked` = `users`,
      `database_backups`, `migrations`, `sessions`, `cache`, `jobs`.
      → აკრეფილი დადასტურება **ცხრილის საკუთარი სახელი** და არა `RESTORE`
      (ღილაკიდან გადმოსაწერი სიტყვა სუსტია).
- [ ] **11.5** **ერთი ჩანაწერის აღდგენა.** ⚠️ **არასდროს `REPLACE INTO`** — ის
      `DELETE`+`INSERT`-ია და **კასკადებს ისვრის**; `REPLACE INTO users` ანგარიშის
      61 შვილეულ ცხრილს წაშლიდა. სწორია `INSERT … SELECT … ON DUPLICATE KEY UPDATE`.
      ⚠️ იდენტიფიკატორი **ყოველთვის `id` არაა** — `castables`, `genreables`,
      `module_user` შედგენილი გასაღებია, `cache`/`sessions` კი სტრიქონული.
      ⚠️ `ON DUPLICATE KEY` **ნებისმიერ** უნიკალურ ინდექსზეც ისვრის (მაგ.
      `unique(user_id, imdb_id)`), ე.ი. სხვა `id`-ს მქონე ჩანაწერმა შეიძლება
      **სხვა რიგი** გადააწეროს — წინასწარ უნდა შემოწმდეს და დასახელდეს.
      ⚠️ თუ მშობელი აღარ არსებობს — **უარი და მშობლის დასახელება**, და არასდროს
      `SET FOREIGN_KEY_CHECKS = 0`.
- [ ] **11.6** ყველა ეტაპზე პროექტის უკვე არსებული წესები: `super_admin`, უსაფრთხოების
      დამპი ჩაწერამდე, აუდიტის ჩანაწერი **დესტრუქციულ ნაბიჯამდე**, `message` *არის*
      კოდი (ახალი კოდები `CODES`-სა და ორივე ლოკალში), ფონური პროცესი `stale`-დარაჯით.
      ⚠️ და გასახსენებელი: **აღდგენა თვითონ `database_backups`-საც გადააწერს** — დღეს
      მხოლოდ ორი რიგი ნარჩუნდება, დანარჩენი ასლების რიგები კი იმ დამპისა ხდება,
      ე.ი. დისკზე მდგომი ფაილები ობლდება.

❓ **გადასაწყვეტია:** დროებითი ბაზა (11.3) მისაღებია? ის ერთადერთი გზაა ნამდვილი
ვიუერისა და იაფი ნაწილობრივი აღდგენისკენ, ფასად კი MySQL-ის დისკზე მონაცემის
მეორე ასლია, რომელსაც კვოტა ვერ ხედავს.

❓ **გადასაწყვეტია:** გვინდა მხოლოდ **პრევიუ** (11.2, პირველი 100 რიგი) თუ სრული,
გვერდებიანი და სორტირებადი ვიუერი (11.3)?

❓ **გადასაწყვეტია:** `users`-ის აღდგენა სრულად ავკრძალოთ თუ მეორე აკრეფილი
სიტყვის უკან დავტოვოთ?
