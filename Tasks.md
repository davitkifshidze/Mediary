# Tasks — Mediary

> მიმდინარე დავალებების ერთადერთი წყარო. **Claude-მ ეს ფაილი ყოველ სესიაზე უნდა წაიკითხოს.**
> **შესრულებული პუნქტი აქედან იშლება** — ფაილი მხოლოდ ღია სამუშაოს ინახავს.
> დეტალები რჩება git history-სა და `CLAUDE.md`-ში.
>
> **🙋 შესათანხმებელი და გადადებული [`docs/DECISIONS.md`](docs/DECISIONS.md)-შია.**

> ✅ **2026-09-11-ის ნაკრები (7 ეტაპი) და „გალერეა 2.0" სრულად დაიხურა 2026-09-12-ს.**
> აღწერა აქედან წაიშალა (ფაილის საკუთარი წესი: მხოლოდ ღია სამუშაო) — წესები
> `CLAUDE.md`-შია, დეტალები git history-ში.

---

## ✅ დახურულია 2026-09-12-ს

**ცხრა დღის სამუშაო დაკომიტდა.** 507 დაუკომიტებელი გზა (377 ახალი ფაილი,
118 შეცვლილი, 12 წაშლილი) ცხრა ფენობრივ კომიტად ჩაჯდა `af7953b`-დან
`6700fdf`-მდე — სქემა და რეგისტრები · მოდელები/პოლისები · სერვისები ·
API · ტესტები · ფრონტის data layer · ფრონტის UI · i18n · დოკუმენტაცია.
სამუშაო საქაღალდე სუფთაა და დაკომიტებულ მდგომარეობაზე **378 ტესტი და
`npm run build` გავიდა**.

⚠️ **ცალკეული კომიტი დამოუკიდებლად არ ეშვება** — `routes/api.php`,
`App.tsx` და ორივე ლოკალი ერთდროულად ყველა ფუნქციას ემსახურება, ე.ი.
ფუნქციებად დაჭრა 130 ფაილის დიფის ხელით დაჭრას მოითხოვდა. მწვანეა
**ბოლო** მდგომარეობა.

⚠️ **`push` არ გამიკეთებია** — გკითხავ.

**სიები გვერდებად დაიყო.** ცხრავე მოდულის `index()` ერთ დამხმარეზე გადავიდა
(`Controller::paginated()`), ნაგულისხმევი გვერდი 60-ია, „მეტის ჩვენება"
ღილაკი ყველა სექციაზეა. ცოცხალ ბაზაზე გაზომილი: `GET /api/movies`
**806 kB → 109 kB** (353 ფილმი). მთელი სია მხოლოდ ცხადი `all=1`-ით მოდის
და მას ოთხი გამომძახებელი აქვს — `/purge`, მასობრივი ცვლილება, ჩანაწერების
ამრჩევები და პლეილისტის ავზი; დამკვრელის რიგიც მთელი გაფილტრული სია დარჩა.
წესები `CLAUDE.md`-შია (*List pagination*), ხუთი ახალი ტესტი —
`ListPaginationTest`.

**ფრონტს ტესტები გაუჩნდა.** Vitest + jsdom, `npm test` — **34 ტესტი ხუთ
ფაილში**: `lib/embed` (iframe-ის allowlist და სამი პლეერის მოვლენები),
`lib/tags` (ბექენდის `normalizeTags()`-ის ტყუპი), `lib/statuses`,
`lib/dates` (`sv-SE`-ის წესი) და `lib/paged`. ⚠️ მოცული მხოლოდ `lib/`-ის
სუფთა ფუნქციებია — კომპონენტების ტესტს `@testing-library/react` სჭირდება
და ის ჯერ არ დამატებულა; ამიტომ `useDragReorder`-ის `moveBy()` დაუფარავია.

---

## 🩺 ჯანმრთელობის შემოწმება — 2026-09-12

ყველაფერი მწვანეა, ე.ი. ქვემოთ არაფერი „გატეხილს" არ ასწორებს:

| შემოწმება | შედეგი |
|---|---|
| `php artisan test` | **378 ტესტი · 2047 assertion · გავიდა** |
| `python frontend/src/i18n/audit.py` | **OK** — 0 დაკარგული, 0 ცალმხრივი, 0 ინტერპოლაციის შეუსაბამობა |
| `npm run build` (`tsc -b` + vite) | **გავიდა** |
| `npm run lint` (oxlint) | მხოლოდ `fast-refresh`-ის კოსმეტიკური გაფრთხილებები |

---

# 🟨 შემოთავაზებული ნაკრები — 2026-09-12

> ღია სამუშაო აღარ იყო, ამიტომ ეს ნაკრები **კოდიდან** ამოვიღე და არა თავიდან
> გამოვიგონე — თითოეულ პუნქტს ქვემოთ მტკიცებულება უწერია. **რიგი შენია:**
> მითხარი რომელი და რა თანმიმდევრობით, ან „ყველა".

### რიგი — ჩემი რჩევა

| # | ეტაპი | რატომ ასე |
|---|---|---|
| 4 | ბანდლის დაყოფა | გაზომილი, მაგრამ localhost-ზე მოგება მცირეა — ამიტომ მესამეზე დაბლა |
| 5 | მკვდარი კოდი | ორი ფაილი, ხუთწუთიანი |
| 6 | ღია კითხვები | `DECISIONS.md`-ს პასუხი აკლია, კოდი არა |

---

## 4. ბანდლი ერთი ნაჭერია — 1.69 MB (453 kB gzip)

**მტკიცებულება:** `npm run build` → `dist/assets/index-*.js  1,694.45 kB │
gzip: 453.16 kB`, ერთი chunk; vite-ის გაფრთხილება ჩანს. `App.tsx` ოცდაათივე
გვერდს **სტატიკურად** იმპორტავს (ხაზები 6–43).

**რას ვაკეთებ:** მარშრუტების დონეზე `React.lazy` + ერთი `<Suspense>`
(fallback უკვე არსებული `Splash`-ია). იზოლირებული ცვლილება — ერთი ფაილი.

**⚠️ პატიოსნად:** localhost-ზე მოგება **მცირეა** (453 kB ლოკალურად თითქმის
მყისიერია), ხოლო ფასი ნავიგაციაზე spinner-ის გაელვებაა. ამიტომ მესამეზე
დაბლა დავდე — თუ აპი ოდესმე გარე ჰოსტზე გავა, პირველ ადგილზე ადის.

---

## 5. მკვდარი კოდი — ორი ფაილი

**მტკიცებულება:** მთელ `frontend/src`-ში არავინ იმპორტავს:

- `components/ContentLangToggle.tsx` — `CLAUDE.md` მას უკვე „dead code"-ს
  უწოდებს; შიგთავსის ენა 2026-09-03-დან პარამეტრებიდან მოდის
- `components/ui/multi-select.tsx` — Radix-ის ვარიანტი; პროექტი
  `react-select`-ს იყენებს (`TagSelect` / `GenreSelect`)

**ჩემი რჩევა:** წაიშალოს — ახლა ისინი „ეს კომპონენტი გვაქვს" ტყუილს
ამბობენ და `ui/multi-select.tsx`-ს ახალი გვერდი ადვილად აიღებს, ე.ი.
პროექტი ორ multi-select-ზე გაიყოფა.

✅ ბლოკერი მოიხსნა — ორივე ფაილი ახლა კომიტშია, ე.ი. წაშლა უკან
დასაბრუნებელია.

---

## 6. ღია კითხვები — კოდი მზადაა, პასუხი არა

[`docs/DECISIONS.md`](docs/DECISIONS.md)-ში **ხუთი** უპასუხო კითხვაა.
ორი მათგანი ერთსტრიქონიანია და დღესვე ჩაიკეტება:

- **#2 Google Books როგორც წიგნების მეორე წყარო** — გასაღების გარეშეც
  მუშაობს, SerpApi-ის 250-იან ბიუჯეტს არ ეხება. *ჩემი რჩევა: კი, მხოლოდ
  fallback-ად.*
- **#3 სურათების სქრეიპინგი** — *ჩემი რჩევა: არა* (ToS, IP-ის ბლოკი,
  მარკაპზე ჩუმი ჩავარდნა); Wikimedia + SerpApi ამას ხურავს.
- **#4-ის სამი წვრილი დაზუსტება** (ჟანრის წაშლის მესამე ვარიანტი ·
  `file` ველის „მრავლობითი" ჩამრთველი · სიმღერაზე აუდიოფაილი) — სამივე
  ერთსტრიქონიანი, თუ მართლა გინდა.
- **#1 თარგმანის წყარო** — შენ „დაიკიდე"; ღიად რჩება (Azure Translator F0
  კვლავ ჩემი რჩევაა).

---

## 🟡 მანქანაზე გასაკეთებელი (ადმინის უფლებებს ითხოვს)

- **MariaDB Windows-ის სერვისად** — ახლა `mysqld.exe` ხელით იშვება, ე.ი.
  გამორთვა/გამოსვლა ყოველთვის **არასუფთა შეწყვეტაა** და crash-recovery-ის
  ციკლს უშვებს; სწორედ ეს ცვეთს datadir-ს (იხ. `CLAUDE.md`, 2026-09-09-ის
  ინციდენტი). გჭირდება ამაღლებული ტერმინალი — მე ვერ გავუშვებ.
- **`yt-dlp` (+ სურვილისამებრ `ffmpeg`) დაყენება** — §7.1 დაწერილია და
  დატესტილი, მაგრამ **ცოცხლად არასდროს გაშვებულა**: პროგრამა ამ მანქანაზე
  არ არის. დაყენების შემდეგ საკმარისია `YTDLP_BINARY`/`FFMPEG_BINARY`
  `.env`-ში (ან PATH-ზე) — კოდის ცვლილება არ სჭირდება.
