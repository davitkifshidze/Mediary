# Tasks — Mediary

> მიმდინარე დავალებების ერთადერთი წყარო. **Claude-მ ეს ფაილი ყოველ სესიაზე უნდა წაიკითხოს.**
> **შესრულებული პუნქტი აქედან იშლება** — ფაილი მხოლოდ ღია სამუშაოს ინახავს.
> მზიდი წესები `CLAUDE.md`-შია.
>
> ⚠️ **`docs/DECISIONS.md` წაშლილია (2026-09-14, შენი მითითებით).** ღია
> კითხვა აღარსად ინახება ცალკე — თუ რაიმე გადასაწყვეტია, იმ თასქშივე წერია,
> რომელსაც ეხება.

---

## 🔧 დარჩა ერთი მანქანური საქმე — **MariaDB Windows-ის სერვისად**

ყოველი არასუფთა გაჩერება აღდგენის ციკლია, და სწორედ ეს ცვეთს datadir-ს
(იხ. `CLAUDE.md`, „MariaDB crash recovery"). სერვისად დაყენება ამას წყვეტს:
OS თვითონ აჩერებს სუფთად გამორთვისას.

⚠️ **ეს ნამდვილად საჭიროებს ადმინისტრატორის shell-ს** — შემოწმებულია
2026-09-14-ს: მიმდინარე სესია ადმინი არ არის (`IsInRole(Administrator)` =
`False`) და `mysql*`/`mariadb*` სერვისი საერთოდ არ არსებობს.

ადმინისტრატორის PowerShell-ში:

```powershell
& "C:\xampp\mysql\bin\mysqld.exe" --install MariaDB --defaults-file="C:\xampp\mysql\bin\my.ini"
Start-Service MariaDB
Set-Service MariaDB -StartupType Automatic
```

⚠️ **ჯერ გააჩერე ხელით გაშვებული `mysqld`** — ორივე 3306-ს დაიკავებდა.
⚠️ XAMPP-ის პანელის „Service" ჩექბოქსი იმავეს აკეთებს, ისიც ადმინით.

---

## 🩺 მდგომარეობა — 2026-09-14

| შემოწმება | შედეგი |
|---|---|
| `php artisan test` | **452 ტესტი · 2386 assertion · გავიდა** |
| `npm test` (Vitest) | **94 ტესტი · გავიდა** |
| `npm run build` (`tsc -b` + vite) | **გავიდა** |
| `python frontend/src/i18n/audit.py` | **OK** (ka/en სინქრონში) |
| `npm run lint` (oxlint) | **0 შეცდომა**; მხოლოდ ძველი კოსმეტიკური გაფრთხილებები |
| `./vendor/bin/pint --test` | **გავიდა** |
| `yt-dlp` / `ffmpeg` | **დაყენებულია და ცოცხლად შემოწმებული** |

**თარგმანის ბლოკი დახურულია** — წყაროს არჩევანი, ლოგი, ლიმიტები და
`review` რეჟიმი; მზიდი წესები `CLAUDE.md`-შია.

**ვიდეოს ლოკალური ჩამოწერა (§7.1) ახლა მართლა მუშაობს.** ორივე პროგრამა
მომხმარებლის უფლებებით დაყენდა (ამაღლებული shell **არ** დასჭირვებია —
backlog-ის ვარაუდი მცდარი იყო): `yt-dlp` 2026.08.19 (`pip install --user`),
`ffmpeg` 9.0.1 (gyan.dev-ის static build, `%LOCALAPPDATA%\Programs\`).
ბილიკები `backend/.env`-შია, რადგან არცერთი PATH-ზე არ ჯდება.

### `review`-ის მოცულობა ცოცხალ ბაზაზე (გაზომილი 2026-09-14)

`movie_translations`: en — 357 ჩანაწერი / 97,782 სიმბოლო, ka — 312 / 67,368.
მთელი ბიბლიოთეკა ≈ **25k input / ~60k output ტოკენი**, ე.ი. ~357 მოთხოვნა —
Gemini-ის უფასო დღიური ლიმიტის (1,500) **მეოთხედი, ანუ 0 ხარჯი**.
გაზომილი ტემპით (1.6 წმ ერთი გადამოწმება) + 4000 ms პაუზა ≈ **35 წუთი**.
⚠️ გაშვება შენი გადასაწყვეტია — ეს ერთადერთი რეჟიმია, რომელიც არსებულ
ტექსტს გადაწერს.
