# Tasks — Mediary

> მიმდინარე დავალებების ერთადერთი წყარო. **Claude-მ ეს ფაილი ყოველ სესიაზე უნდა წაიკითხოს.**
> **შესრულებული პუნქტი აქედან იშლება** — ფაილი მხოლოდ ღია სამუშაოს ინახავს.
> დეტალები რჩება git history-სა და `CLAUDE.md`-ში.
>
> **🙋 შესათანხმებელი და გადადებული [`docs/DECISIONS.md`](docs/DECISIONS.md)-შია.**

---

## ✅ 2026-09-12 — რა დაიხურა

**2026-09-11-ის ნაკრები (7 ეტაპი) და „გალერეა 2.0"** სრულად დასრულდა; მათი
აღწერა აქედან წაიშალა — წესები `CLAUDE.md`-შია, დეტალები git history-ში.

შემდეგ ღია სამუშაო აღარ იყო, ამიტომ ახალი ნაკრები **კოდიდან** ამოვიღე
(თითოეულს გაზომილი მტკიცებულება ჰქონდა) და ხუთი პუნქტი შესრულდა:

| # | რა | შედეგი |
|---|---|---|
| 1 | **კომიტი** | ცხრა დღის სამუშაო — 507 დაუკომიტებელი გზა, 130 ფაილი `+15 978/−5 114` — ცხრა ფენობრივ კომიტად ჩაჯდა (`af7953b` → `6700fdf`). ⚠️ **ცალკეული კომიტი დამოუკიდებლად არ ეშვება** (`routes/api.php`, `App.tsx` და ორივე ლოკალი ერთდროულად ყველა ფუნქციას ემსახურება); მწვანეა **ბოლო** მდგომარეობა. |
| 2 | **სიები გვერდებად** | ცხრავე მოდულის `index()` ერთ დამხმარეზეა (`Controller::paginated()`), გვერდი 60, „მეტის ჩვენება" ყველგან. ცოცხლად გაზომილი: `GET /api/movies` **806 kB → 109 kB** (353 ფილმი). მთელი სია მხოლოდ ცხადი `all=1`-ით — ოთხი გამომძახებელი. |
| 3 | **ფრონტის ტესტები** | Vitest + jsdom, `npm test` — **34 ტესტი** `lib/embed` · `lib/tags` · `lib/statuses` · `lib/dates` · `lib/paged`-ზე. |
| 4 | **ბანდლის დაყოფა** | 37-ვე გვერდი `lazy()`-ზე: საწყისი ჩატვირთვა **1 697 kB ერთი ნაჭრიდან → 862 kB ორმოცად**. |
| 5 | **მკვდარი კოდი** | `ContentLangToggle.tsx` და `ui/multi-select.tsx` წაიშალა. |

**⚠️ სამი რამ, რაც გზადაგზა აღმოჩნდა და დამახსოვრების ღირსია** (სამივე
`CLAUDE.md`-შია გაშლილი):

1. **ბაზისურ კლასში ზოგადი სახელის კონსტანტა ადგილს იჭერს.** `Controller::MAX_PER_PAGE`
   `GalleryController`-ის `private const MAX_PER_PAGE`-ს დაეჯახა — მემკვიდრეობითი
   კონსტანტის ხილვადობის დავიწროება **ფატალური შეცდომაა** და მთელი ტესტების
   ნაკრები ერთბაშად ჩავარდა. სახელები `LIST_PER_PAGE`-ად გადაკეთდა.
2. **ერთმა სტატიკურმა იმპორტმა მთელ `lazy()`-ს აზრი დაუკარგა.** `Sidebar.tsx`
   `GALLERY_CUTS`-ს `pages/GalleryPage`-იდან კითხულობდა და საწყის ნაკრებში
   ლაითბოქსს, `react-select`-ს, თარიღის ამრჩევსა და ვებვიდეოს დიალოგს ათრევდა.
   სია `lib/galleryCuts.ts`-შია. ⚠️ ამ ფაილების შეხების შემდეგ ღირს
   `grep "from '@/pages/" src/components src/lib` — ერთი მოხვედრა ასეულობით კB-ია.
3. **დამკვრელის რიგი განზრახ დარჩა მთელი გაფილტრული სია** — „დაკვრაზე" ერთი
   `all=1` ჩამოდის (ქეშდება). 500 სიმღერიან ფილტრზე 60-ზე გაჩერება ჩუმი
   რეგრესია იქნებოდა.

**⚠️ ცნობილი ხარვეზები, რომლებიც განზრახ ღიაა:**

- **`lazy()` მარშრუტებს შეცდომის მცველი არ აქვს** — ჩამოუტვირთავი ნაჭერი
  გვერდს ცარიელს ტოვებს. ლოკალურ მანქანაზე დასაშვებია; ჰოსტზე გატანისას
  პირველი გასაკეთებელი.
- **კომპონენტების ტესტი არ არის** (`@testing-library/react` არ დამატებულა),
  ამიტომ `useDragReorder`-ის `moveBy()` დაუფარავია.

⚠️ **`push` არ გამიკეთებია** — გკითხავ.

---

## 🩺 ჯანმრთელობის შემოწმება — 2026-09-12 (ბოლო კომიტზე)

| შემოწმება | შედეგი |
|---|---|
| `php artisan test` | **383 ტესტი · 2066 assertion · გავიდა** |
| `npm test` (Vitest) | **34 ტესტი · გავიდა** |
| `npm run build` (`tsc -b` + vite) | **გავიდა** |
| `python frontend/src/i18n/audit.py` | **OK** — 0 დაკარგული, 0 ცალმხრივი, 0 ინტერპოლაციის შეუსაბამობა |
| `npm run lint` (oxlint) | მხოლოდ `fast-refresh`-ის კოსმეტიკური გაფრთხილებები |

---

# 🟨 ღია სამუშაო

## 1. ღია კითხვები — კოდი მზადაა, პასუხი არა

[`docs/DECISIONS.md`](docs/DECISIONS.md)-ში **ხუთი** უპასუხო კითხვაა.
ორი მათგანი ერთსტრიქონიანია და დღესვე ჩაიკეტება:

- **#2 Google Books როგორც წიგნების მეორე წყარო** — გასაღების გარეშეც
  მუშაობს, SerpApi-ის 250-იან ბიუჯეტს არ ეხება. *ჩემი რჩევა: კი, მხოლოდ
  fallback-ად.*
- **#3 სურათების სქრეიპინგი** — *ჩემი რჩევა: არა* (ToS, IP-ის ბლოკი,
  მარკაპზე ჩუმი ჩავარდნა); Wikimedia + SerpApi ამას ხურავს.
- **#4-ის სამი წვრილი დაზუსტება** (ჟანრის წაშლის მესამე ვარიანტი ·
  `file` ველის „მრავლობითი" ჩამრთველი · სიმღერაზე აუდიოფაილი) — სამივე
  ერთსტრიქონიანი, თუ მართლა გინდა.
- **#1 თარგმანის წყარო** — შენ „დაიკიდე"; ღიად რჩება (Azure Translator F0
  კვლავ ჩემი რჩევაა).

---

## 🟡 მანქანაზე გასაკეთებელი (ადმინის უფლებებს ითხოვს)

- **MariaDB Windows-ის სერვისად** — ახლა `mysqld.exe` ხელით იშვება, ე.ი.
  გამორთვა/გამოსვლა ყოველთვის **არასუფთა შეწყვეტაა** და crash-recovery-ის
  ციკლს უშვებს; სწორედ ეს ცვეთს datadir-ს (იხ. `CLAUDE.md`, 2026-09-09-ის
  ინციდენტი). გჭირდება ამაღლებული ტერმინალი — მე ვერ გავუშვებ.
- **`yt-dlp` (+ სურვილისამებრ `ffmpeg`) დაყენება** — §7.1 დაწერილია და
  დატესტილი, მაგრამ **ცოცხლად არასდროს გაშვებულა**: პროგრამა ამ მანქანაზე
  არ არის. დაყენების შემდეგ საკმარისია `YTDLP_BINARY`/`FFMPEG_BINARY`
  `.env`-ში (ან PATH-ზე) — კოდის ცვლილება არ სჭირდება.
