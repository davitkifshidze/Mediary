/* ============================================================
   z-index-ის ერთადერთი წყარო (Tasks §1.1/§1.2).

   ⚠️ **რატომ არსებობს ეს ფაილი.** ჩამოსაშლელი სიები (Radix `Select`,
   `Popover`, მენიუები) portal-ით `<body>`-ის ბოლოში ჯდება, ე.ი. მოდალთან
   ერთსა და იმავე stacking context-შია და მხოლოდ **z-index** წყვეტს, რომელი
   ჩანს. ყველა popup `z-50`-ზე იჯდა, მოდალები კი მოგვიანებით `z-60/61`-ზე,
   `useConfirm` `z-90/91`-ზე, drawer/lightbox `z-80/81`-ზე გადავიდა — ამიტომ
   მოდალის შიგნით გახსნილი სია **უკან ხატულობდა** და „არ იხსნებოდა"
   (ჩანაწერების კატეგორია/სტატუსი, ბუკმარკები, თამაშის სტატუსი, ბორდგეიმის
   ჟანრის წაშლის დიალოგი, ვიდეოს ტიპი, გალერეის „თემის გარეშე").

   ე.ი. ბაგი **ერთია** და აქ სწორდება, თითო ფორმაზე კი არა. ახალი popup-იც
   აქედან უნდა აიღოს კლასი.

   სართულები (ქვევიდან ზევით):

   | ფენა | z | სად |
   |---|---|---|
   | ბარათის ბეჯები        | 10     | `MovieCard` |
   | sticky save bar        | 30     | `SettingsSaveBar`, `RolePage` |
   | ჰედერი                 | 40     | `Header` |
   | **დამკვრელი**          | **50** | `PlayerBar` (§7.2) |
   | მოდალი                 | 60/61  | `ModalShell`, `dialog.tsx` |
   | რიგის ტოსტი            | 70     | `ui/queue.tsx`, `ui/feedback.tsx` |
   | drawer / lightbox      | 80/81  | `Sidebar`, `FilterPanel`, `GalleryPanel` |
   | დადასტურება (confirm)  | 90/91  | `ui/feedback.tsx` |
   | **popup**              | **100**| select · popover · მენიუები |
   | **tooltip**            | **110**| `ui/tooltip.tsx` |

   ⚠️ popup ყოველთვის **ყველა ფენაზე ზემოთაა** — მას სწორედ იმ ფენიდან ხსნიან,
   რომელიც ყველაზე მაღლა დგას (მოდალიდან, confirm-იდან, lightbox-იდან), ე.ი.
   „ზუსტად ერთი საფეხურით მაღლა" არ იმუშავებდა.

   ⚠️ Tailwind კლასებს **წყაროდან კითხულობს**, ე.ი. ეს სტრიქონები ლიტერალად
   უნდა იწერებოდეს — ცვლადიდან აწყობილი `` `z-[${n}]` `` არ დაგენერირდება.
   ============================================================ */

/**
 * ქვედა ზოლი — აპლიკაციის ავეჯია (ჰედერივით), ე.ი. მოდალს **ქვემოთ** დგას.
 * ⚠️ რიგის ტოსტი (`z-[70]`) მასზე მაღლაა, სამაგიეროდ თავის `bottom`-ს
 * `--player-h`-ით წევს ზემოთ — გადაფარვა ასე ვერ ხდება.
 */
export const LAYER_PLAYER = 'z-50'

/** ჩამოსაშლელი/popover/მენიუ — ყველა მოდალზე და lightbox-ზე ზემოთ */
export const LAYER_POPUP = 'z-[100]'

/** tooltip — popup-ზეც ზემოთ (მას popup-ის შიგნითაც ვიძახებთ) */
export const LAYER_TOOLTIP = 'z-[110]'

/** react-select-ის portal-ისთვის (inline style, არა კლასი) */
export const LAYER_POPUP_Z = 100
