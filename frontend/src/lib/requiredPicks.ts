/* ============================================================
   სავალდებულო არჩევანი — სტატუსი და „ტიპი" ყველა მოდულში
   ============================================================
   ⚠️ **ერთი განმარტება, რვა ფორმა.** „ცარიელია თუ არა არჩევანი" მარტივი
   კითხვაა და სწორედ ამიტომ იწერებოდა ყოველ ფორმაში ხელახლა — სამ სხვადასხვა
   ფორმას სამი სხვადასხვა წარმოდგენა აქვს „ცარიელზე": `Select`-ს ცარიელი
   **სტრინგი** აქვს, pivot-იან ჟანრებს — ცარიელი **მასივი**, ხოლო
   გასუფთავებულ `Select`-ს ზოგან `null` რჩება. ერთ-ერთის გამორჩენა ჩუმია:
   ფორმა ვალიდურად ჩათვლის, გაგზავნის და **backend-ის 422** დაბრუნდება
   ისე, რომ ველი გაწითლებული არც კი იქნება.

   ⚠️ **ეს backend-ის შემცვლელი არ არის და არც უნდა იყოს.** ნამდვილი კარიბჭე
   ვალიდაციაა (`$must = [...'required']` თითოეულ კონტროლერში); აქაური
   შემოწმება მხოლოდ იმისთვისაა, რომ მომხმარებელმა პასუხი **ქსელის გარეშე**,
   იმავე წამს დაინახოს და კურსორი სწორ ველზე მიდიოდეს.
   ============================================================ */

/** ცარიელია? — სამივე შესაძლო „ცარიელი" ერთ კითხვად */
export function isEmptyPick(value: unknown): boolean {
  if (value === null || value === undefined) return true
  if (typeof value === 'string') return value.trim() === ''
  if (Array.isArray(value)) return value.length === 0

  return false
}

/**
 * რომელი ველებია შეუვსებელი — გასაღებები იმავე სახელით, რითაც ფორმა
 * `errors`-ს კითხულობს, ე.ი. პასუხი პირდაპირ `setErrors`-ში ჯდება.
 */
export function missingPicks(picks: Record<string, unknown>): string[] {
  return Object.entries(picks)
    .filter(([, value]) => isEmptyPick(value))
    .map(([key]) => key)
}

/**
 * მზა `errors` ობიექტი ერთი და იმავე შეტყობინებით.
 *
 * ⚠️ ტექსტს **გარედან** იღებს (`t('validation.pickOne')`) და არა შიგნიდან:
 * ეს ფაილი სუფთა ფუნქციებია, i18n-ის იმპორტი მას ტესტირებადობას დაუკარგავდა.
 */
export function pickErrors(picks: Record<string, unknown>, message: string): Record<string, string> {
  return Object.fromEntries(missingPicks(picks).map((key) => [key, message]))
}
